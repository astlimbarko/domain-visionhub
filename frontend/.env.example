VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Opcional -- solo hace falta en el build de produccion (.env.production),
# no aca. Fija el dominio al que apuntan los enlaces de invitacion/recuperacion
# de contrasena (evita que salgan mal si quien hace la accion esta en
# www.somoscdv.com en vez de app.somoscdv.com). Sin setear, cae en
# window.location.origin (correcto para desarrollo local).
# VITE_APP_URL=https://app.somoscdv.com
