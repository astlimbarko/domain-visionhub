# Matías — 2026-09-09

- [x] Seguimiento del bug de "Ministerios por personas"/Sublíderes/Líderes invisibles del día anterior: el owner preguntó explícitamente **por qué** pasaba esto (no solo que se parcheara el roster). Investigación de la causa raíz en el código (no solo en los datos): el flujo de aceptar invitación de Líder/Sublíder de CdP (`fn_aceptar_invitacion_lider`, `fn_resolver_invitaciones_pendientes_extra`) y el de asignación directa vía Constructor (`fn_asignar_cargo_cdp`) **solo insertan en `casa_de_paz_cargo`** -- nunca en `casa_de_paz_membresia`. Es una omisión de diseño (el cargo se modeló como algo separado de "ser miembro de la congregación"), no un bug de lógica puntual.
- [x] **Decisión consultada con el owner** (impacto real: cambia qué cuenta como "Miembro" en dashboard/reportes) -- eligió corregir la causa raíz en vez de solo dejar el parche del roster. Fix: helper nuevo `private.fn_asegurar_membresia_cdp_por_cargo` (solo crea la membresía si la persona no tiene YA una membresía principal vigente en NINGUNA CdP -- no mueve a nadie de su CdP actual, eso es una decisión aparte) llamado desde los 3 puntos de entrada (`fn_aceptar_invitacion_lider`, `fn_resolver_invitaciones_pendientes_extra`, `fn_asignar_cargo_cdp`) -- migración `20260909000000_fn_asegurar_membresia_cdp_por_cargo_lider.sql`.
- [x] **Backfill incluido en la misma migración** para los 65 Líderes/Sublíderes ya afectados (el fix de las funciones solo cubre altas futuras) -- mismo criterio de seguridad, sin mover membresías existentes. Verificado tras aplicar: **0 cargos de Líder/Sublíder ya invisibles** (antes 65), 64 membresías nuevas creadas (1 de los 65 ya tenía membresía en otra CdP, correctamente no tocado), y las personas con ministerio invisible bajaron de 37 a 4 (las 4 restantes no son ni Líder ni Sublíder de CdP -- caso distinto, más chico, fuera de este alcance). Aplicado directo a producción, sin cambios de frontend.
- [x] Pregunta propia (no del owner) sobre por qué la card "Ministerios por personas" del dashboard de CdP "no contabiliza" en general. Análisis a fondo con consultas directas a producción: la lógica de conteo está bien (41 de 45 personas con ministerio del sistema entero caen correctamente en el roster de su CdP; las 4 restantes son Líder de Red, sin ninguna CdP asociada -- no es bug). El número se ve bajo porque el dato de origen es escaso: de 371 miembros formales en 57 CdPs, solo 45 (12%) tienen algún ministerio cargado -- el campo recién se agregó el 25-ago y nadie hizo carga retroactiva. No requiere fix de código, es un tema de captura de datos (asignar ministerios desde el módulo Ministerios) -- pendiente de plantearle esta lectura al owner.
- [x] Bug reportado ("me comentan que..."): en Reportes de CdP, el buscador de "Diezmos por persona" (Finanzas) no dejaba escribir ni buscar. Causa: `BuscadorPersonaCampo` estaba cableado con `valor=""` fijo y `onCambiarTexto={() => {}}` (placeholder sin terminar, nunca se creó el estado) -- cada tecla se borraba sola porque el input controlado nunca reflejaba lo escrito. Fix: se agregó el estado `textoBuscadorDiezmante`, mismo patrón que ya usa el buscador de Disertador (que sí estaba bien armado). Verificado con `tsc`/`oxlint` limpios. Mergeado a `master` (`fix/diezmante-buscador-no-escribe`) y desplegado por SSH a `app.somoscdv.com` a pedido explícito -- confirmado que el bundle nuevo (`Reportes-DPNbn4mA.js`, `index-BOD0nQMS.js`) está sirviendo en producción. **Sin confirmación escrita de prueba en vivo en el navegador** (se ofreció probarlo en `localhost:5174` antes de desplegar, no llegó a confirmarse por escrito). Falta ticket Jira (bloqueado por OAuth de Atlassian, se le pidió autorización al usuario).
- [x] Pedido propio (Matías, sin confirmar con el owner todavía): el buscador de diezmante permitía buscar en toda la iglesia, no solo entre miembros de la propia CdP. Encontré que ya existía casi exactamente esto -- `buscarPersonas` tiene un parámetro `cdpId` (decisión de Gonzalo, Q-MR-12 del 2026-08-15: priorizar la CdP propia con fallback a toda la iglesia, para poder anotar visitantes de otra CdP) que `BuscadorPersonaCampo` nunca terminaba de conectar. Antes de tocar código le señalé a Matías que una restricción dura (sin fallback) revertiría esa decisión explícita del owner para el mismo escenario (diezmante visitante) -- eligió la opción segura: reusar el patrón ya aprobado (priorizar + fallback), solo en el campo de Diezmante, sin tocar Disertador ni el resto. `tsc`/`oxlint` limpios, mergeado a `master` (`fix/diezmante-buscar-solo-cdp`) y desplegado por SSH -- confirmado `index-DKJ_9RYI.js` sirviendo en producción. Falta ticket Jira (mismo bloqueo de OAuth) y prueba en vivo.
- [x] Rediseño visual del módulo Evangelismo (pedido con spec detallada, estilo "SaaS pastel" tipo Apple/Linear/Notion) para `pages/Evangelismo.tsx` (vista Líder/Sublíder de CdP) -- alcance acotado tras aclarar con Matías: **solo esa pantalla** (no Red ni Supervisor/Pastor, que tienen banners distintos: `EvangelismoBanner.tsx` amarillo con foto, mockup pixel-exacto del owner, no tocado). Banner superior (hero navy/color de Red) 100% intacto. Reusé `CardIndicadorPastel` (ya existe, aprobado por el owner para el Dashboard de CdP el 2026-09-08) para las 3 métricas protagonistas en vez de inventar una card nueva -- consistencia entre pantallas. "Mi testimonio" del spec original: **omitida**, no existe esa feature en el sistema (confirmado con Matías, no se inventó backend). Calendario (`CalendarioEvangelismo.tsx`, compartido con `EvangelismoRed.tsx`) con indicador de actividad más sutil (puntito/píldora verde en vez de píldora ámbar sólida) y día seleccionado con tinte azul -- cambio puramente visual, mismo componente que usa la vista de Red también hereda el look nuevo. Sin tocar ningún hook/query/mutación. `tsc`/`oxlint`/`build` limpios. Sin verificación visual en navegador (sin credenciales de Líder/Sublíder de CdP) -- Matías eligió deployar directo confiando en la compilación limpia. Mergeado a `master` (`feature/rediseno-evangelismo-cdp-pastel`) y desplegado por SSH -- confirmado `index-B6unWvyO.js` / `Evangelismo-2I6Xuup8.js` sirviendo en producción. Falta ticket Jira y confirmación visual en vivo.
- [x] Matías miró el resultado en vivo ("está bien" en general) y reportó demasiado scroll -- muchos cuadros apilados. Refactor de consolidación en `pages/Evangelismo.tsx`, sin tocar lógica: intro (título/subtítulo/cita bíblica) se fusiona con la barra de navegación de mes en una sola fila (se sacó la cita, era decorativa); la sección "Meta" (editor de meta propia/de Red) pierde el `section`+`TarjetaHeader` completo, queda un label chico + las 2 mini-cards de siempre; la tarjeta motivacional final pasa de card con borde/ícono circular a una tira de una línea. `tsc`/`oxlint`/`build` limpios. Mergeado (`fix/evangelismo-menos-scroll`) y desplegado por SSH -- confirmado `index-Cu4K-2iA.js` / `Evangelismo-Bt--lkxa.js` en producción. Falta confirmación visual de que el scroll bajó lo suficiente.
- [x] Seguía largo, sobre todo en móvil -- Matías pidió pasar a pestañas. Se agregó `Tabs` (mismo componente que ya usa el Dashboard de CdP) con 2 pestañas: "Resumen" (métricas, editor de meta, por tipo de persona, gráfico+lista) y "Calendario" (calendario + resumen del día, el bloque más alto, ya no compite con el resto). Efecto secundario anotado (no es bug, mismo comportamiento que ya tiene el Dashboard de CdP con pestañas): "Descargar PDF" ahora captura solo la pestaña activa en el momento del clic. `tsc`/`oxlint`/`build` limpios. Mergeado (`feature/evangelismo-pestanas`) y desplegado por SSH -- confirmado `index-DSc0nvkU.js` / `Evangelismo-CsHZx2Pc.js` en producción. Falta confirmación visual y ticket Jira de todo el rediseño del día.
- [x] Pedido de optimización de carga ("lenta, poco fluida"). Diagnóstico real: `EvangelismoTrendChart` se importaba estático, así que `pages/Evangelismo.tsx` arrastraba `vendor-charts` (recharts, ~129 kB gzip) al camino crítico de render -- había que bajar todo recharts antes de pintar cualquier cosa, incluso si el usuario entraba directo a la pestaña Calendario (que no usa el gráfico). Fix: `lazy()` + `Suspense` en el gráfico, mismo patrón que ya usa `DashboardLiderCdp.tsx` -- la página ahora pinta con ~16 kB gzip, recharts se difiere, y si no hay evangelizados este mes el gráfico ni siquiera monta (no baja recharts). Cero cambios de lógica/datos. `tsc`/`oxlint`/`build` limpios. Mergeado (`perf/evangelismo-lazy-recharts`) y desplegado por SSH -- confirmado `index-QA1dqXWX.js` / chunk separado `EvangelismoTrendChart-CLvj3-Iv.js` en producción. Falta confirmación de que se sintió más fluido en vivo (esto ataca el peso del bundle, no mide si además hay RPCs lentas del backend).
- [x] Pedido nuevo: mismo tipo de accesos rápidos que ya tiene el Dashboard de CdP, pero para Líder de Red/Supervisor de Red (comparten el mismo componente, `DashboardLiderRed.tsx`, confirmado en `pages/Dashboard.tsx`), con base en `REPORTE 2026 VISION.xlsx` (hoja "DATOS FINALES 2026", 21 métricas). Análisis previo con el owner: crucé los 21 contra el esquema real -- 14 calculables con dato real y sin ambigüedad (censo autodeclarado, cargos reales, discípulos, bautizados, milagros, evangelizados 1+1), 4 necesitan definir criterio (afirmados/influencia creyentes/influencia simpatizantes/evangelizados en calles), 1 es manual sin fuente en la BD ("Cuánto corre la visión" -- el mismo 45% se repite igual en las 4 columnas de período del Excel, señal de que no es calculado), y 2 no aplican a una Red individual (# de Redes/# de Subsedes, son de toda la organización). Implementados los 14 (confirmado por el owner arrancar por ahí): `fn_personas_de_red` extendida con censo/cargos/ministerios/milagros por persona (migración `20260909010000`, aplicada y verificada contra producción antes del deploy de frontend); `DashboardLiderRed.tsx` pasa a 2 pestañas (Indicadores intacto + "Membresía y Ministerio" con las 13 cards pastel, `CardIndicadorPastel`); `PersonasDeRedVista.tsx` suma soporte de filtro inicial por link (mismo mecanismo que ya usa CdP), así cada card lleva directo a la lista ya filtrada -- sin pantallas nuevas. `tsc`/`oxlint`/`build` limpios. Mergeado (`feature/dashboard-red-accesos-rapidos`) y desplegado por SSH -- confirmado `index-CT7EgrmY.js` en producción. Falta confirmación visual en vivo y ticket Jira. Las 4 definiciones pendientes y las 2 fuera de alcance quedan para una siguiente ronda.
- [x] Pedido de ajuste sobre lo anterior: las 6 cards de "Indicadores" pasan de `KpiMosaico` (color pleno) a `CardIndicadorPastel` -- convivían 2 estilos distintos en la misma pantalla. Las 13 cards de "Membresía y Ministerio" se separan en 3 pestañas temáticas (decisión propia, el owner dijo "no importa mucho la agrupación"): "Liderazgo" (Líderes de CdP/Sub líderes/Líderes de ministerio, cargos reales), "Censo espiritual" (Efesios/Ministros/Ancianos/Diáconos/Sub mentores/Mentores, autodeclarados), "Crecimiento" (Discípulos/Bautizados/Milagros/Evangelizados 1+1). Dashboard queda en 4 pestañas totales. Mismos datos y accesos directos de antes, solo reorganizados. `tsc`/`oxlint`/`build` limpios. Mergeado (`feature/dashboard-red-4-pestanas-pastel`) y desplegado por SSH -- confirmado `index-BWCcioh_.js` en producción.
- [x] El owner notó que las 6 cards de "Indicadores" habían quedado sin acceso directo (mudas), a diferencia de las otras 13. Hallazgo real antes de conectar los clicks: Historial de Reportes/Asistencia y Finanzas **no soportan el alcance de Red** (solo manejan CdP o Supervisor explícitamente) -- un Líder de Red que entrara ahí vería un placeholder roto de "sin CdP asignada". Evité mandar a esas páginas rotas: "Casas de Paz activas" → Gestión de Red (ya soporta el alcance); "Miembros totales" → Personas de Red sin filtro; "Asistencia promedio/total" y "Reportes al día" → scroll suave a la sección "Casas de Paz" de la misma pantalla (ya muestra ese dato por CdP); "Ingresos" → scroll a "Contabilidad de la Red". `tsc`/`oxlint`/`build` limpios. Mergeado (`feature/dashboard-red-indicadores-clickables`) y desplegado por SSH -- confirmado `index-BmZeBErN.js` en producción.
- [x] El owner reportó que el censo espiritual "no jalaba bien" para gente real de su Red (Fares) -- investigado caso por caso, sin tocar código. Martha Caballero Rivera (Sublíder, Diaconisa): datos 100% correctos en cada capa (membresía, cargo, censo, función SQL, deploy) -- no es bug. "Line" (Lineme Bravo Bravo, Líder de Casa de Paz real en Fares): **cero filas de censo espiritual guardadas**, ni un campo. Medido el alcance real: **48 de 58 Líderes de CdP del sistema (83%) no tienen ninguna fila de censo** -- correlaciona con la fecha de creación de la persona (los 10 que sí tienen censo se crearon entre el 27-ago y el 6-sep; los 48 sin censo, mayormente antes) -- el campo se agregó al formulario recién el 20-21 de agosto, así que la mayoría de los líderes reales nunca vio esa pregunta. No es un bug de código, es un vacío de datos estructural (mismo patrón ya visto con "Ministerios por personas"). El owner ya lo habló con el equipo -- pidió un CSV de situación (correo primero, censo por campo) para Centro de Vida 4 Anillo + Centro de Vida Montero (537 personas) para poder gestionar la carga manual/campaña. Entregado por archivo, sin cambios de código ni deploy.
- [x] Reconciliado y desplegado trabajo de **Gonzalo** (KAN-207/209/210, rama `feature/fotos-perfil-compresor-storage`, avisado por el owner que ya estaba listo para deploy): compresor de imágenes reusable, Storage+RLS para fotos de perfil, `EditorFotoPerfilDialog` (recorte 1:1 con `react-easy-crop`) integrado en Cuenta -- autoservicio de foto de perfil. Verifiqué antes de mergear: la rama estaba 30 commits atrás de `master` pero sus cambios reales (desde el punto real donde divergió) no tocaban ningún archivo modificado hoy -- merge sin conflictos. Las 3 fases ya estaban verificadas en vivo por Gonzalo (según su propia bitácora del 08-09: compresión real, aislamiento RLS cross-tenant con 2 iglesias distintas, upload/quitar foto, tiempos de carga con throttling real). La migración `20260908050000_fotos_perfil_storage.sql` ya estaba aplicada a producción -- confirmé contra la base real (bucket + columna + 4 políticas existen) antes de mergear, así que el merge fue solo para dejar el código de frontend en `master`, sin tocar nada de base de datos. `npm install` (dependencia nueva `react-easy-crop`), `tsc`/`oxlint`/`build` limpios. Desplegado por SSH -- confirmado `index-4h8v808f.js` en producción. **Alcance parcial ya documentado por Gonzalo** (KAN-353 creado aparte): la foto todavía no aparece en listados/tarjetas de otras pantallas, solo autoservicio en Cuenta. Quedan 2 filas huérfanas inofensivas en `storage.objects` de las pruebas de Gonzalo, pendiente limpieza manual sin apuro.
