# Matías — 2026-08-21

- [x] Investigué 3 bugs reportados en Casa de Paz: selector de moneda vacío en Reportes, botón "Enviar reporte" sin feedback para Sublíder de CdP, buscador del header
- [x] Buscador del header: no es un bug, es KAN-74 (placeholder intencional, sin funcionalidad todavía) -- queda fuera de este trabajo
- [x] Causa raíz moneda: `fn_crear_iglesia` nunca insertaba la fila en `iglesia_moneda` -- toda iglesia (incluida la actual) quedó con 0 monedas activas desde el día uno
- [x] Causa raíz envío de reporte: (a) `Reportes.tsx` no mostraba el error de `moneda_id`, botón fallaba sin aviso; (b) si el guardado fallaba a mitad de camino, el rollback (`UPDATE fecha_eliminacion`) lo bloqueaba silenciosamente la RLS para Sublíder (`SUBLIDER_PUEDE_EDITAR_REPORTE` apagado por defecto), dejando reportes huérfanos sin ningún aviso
- [x] Migración `111_fix_moneda_iglesia_y_revertir_reporte.sql`: fix de `fn_crear_iglesia` + backfill de iglesias existentes + `fn_activar_moneda` (nuevo, faltaba UI/RPC para activar monedas del catálogo) + `fn_revertir_reporte_cdp` (rollback seguro acotado a creador + 10 min, sin reabrir el hueco de RLS)
- [x] Frontend: Panel Supervisor ahora permite activar monedas del catálogo; Reportes.tsx muestra el error de moneda; `reporte.service.ts` usa el RPC nuevo para el rollback en vez del UPDATE directo
- [x] `tsc -b`, `oxlint` y `npm run build` del frontend, todo limpio
- [x] Migración 111 aplicada en producción (SQL Editor de Supabase)
- [x] Deployeé el frontend en `app.somoscdv.com` vía SSH/SFTP (script Node con `ssh2`/`ssh2-sftp-client`, sin terminal interactiva) -- backup del `public/` anterior en `app.somoscdv.com/public_deploy_backup_20260821_164238`, subida de `frontend/dist/` nuevo. Verificado que el bundle nuevo (`index-C-oLGTox.js`) quedó sirviendo y que raíz + `/reportes` responden 200 sin 404
- [ ] Falta: probar en el navegador que el selector de moneda ya no está vacío y que un Sublíder puede enviar su reporte sin que quede huérfano
- [x] Bug de notificaciones (Líder de CdP no debería notificar a Sublíder, Sublíder sí debería notificar a Líder, nadie debería autonotificarse): quedó pausado esperando precisión del owner sobre en qué acción lo veía -- resuelto más tarde en la sesión, ver KAN-240 más abajo (era exactamente el caso límite de doble cargo que había identificado)
- [x] Auditoría de performance de las secciones de Líder/Sublíder de CdP (Dashboard, Reportes, Historial de Reportes, Historial de Asistencia, Evangelismo, Calendario, Perfil de CdP) -- KAN-235
- [x] Optimización aplicada (En revisión, KAN-235): `obtenerHistorialAsistencia` de 4 round-trips en serie a 2 en paralelo; `crearReporte` sin N+1 al guardar visitas nuevas (tipo de teléfono una sola vez + inserts en paralelo); evangelizados nuevos en paralelo en `Reportes.tsx`; `staleTime` en `useMisCasasDePaz`/`useCargoVigenteCdp`/`useDomicilioCdp`/`useCdpPerfil` para no repetir waterfalls en cada navegación; `useMemo` en los 4 conteos de `DashboardLiderCdp`. `tsc -b`/`oxlint`/`build` limpios
- [ ] Falta: confirmación en vivo de que las páginas se sienten más rápidas (KAN-235)
- [x] Gestión de Casas de Paz (Líder de Red) reorganizada en pestañas (Casas de Paz / Fusiones y Multiplicaciones / Invitaciones) para que no haya que bajar tanto -- `GestionRedVista.tsx`, reusa el `Tabs` que ya usa Panel Supervisor. `tsc -b`/`oxlint`/`build` limpios -- KAN-237 en En revisión
- [ ] Falta: confirmación visual en vivo de las pestañas nuevas (KAN-237)
- [x] `git pull` de master: Gonzalo mergeó PR #33 (feature/membresia) y #34 (docs/bitacora-cierre-membresia) mientras yo trabajaba -- 55 archivos, 17 migraciones SQL nuevas de Afirmación/membresía. Sin conflicto con mis archivos sin commitear. Confirmado con Gonzalo que esas migraciones ya estaban aplicadas en producción -- no las toqué
- [x] Re-deployeé el frontend en `app.somoscdv.com` combinando master actualizado + mis 3 cambios sin commitear (KAN-234/235/237) -- backup previo en `app.somoscdv.com/public_deploy_backup_20260821_172956`. Verificado bundle nuevo (`index-DJ51YVj8.js`) sirviendo, raíz + `/casas-de-paz` responden 200
- [x] Botón "Eliminar" para invitaciones pendientes en Gestión de Casas de Paz (Líder de Red y Supervisor de Red) -- KAN-238. El backend ya soportaba esto (`fn_cancelar_invitacion_lider` ya autoriza a ambos roles, hook `useCancelarInvitacionLider` ya existía, usado en el Constructor); solo faltaba el botón en `GestionRedVista.tsx`, con confirmación. Solo para invitaciones Pendientes (el RPC no permite cancelar Completadas). `tsc -b`/`oxlint`/`build` limpios -- En revisión
- [ ] Falta: confirmación visual en vivo del botón de eliminar invitación (KAN-238) y re-deploy con este último cambio
- [x] Bug urgente (bloqueaba alta de gente nueva, KAN-213 seguimiento): cuenta con varios roles en la misma iglesia pedía la Membresía más de una vez, y en mobile faltaba/desbordaba el botón "Saltar". Causa 1: `fn_mi_membresia_incompleta` miraba invitaciones pendientes ANTES de chequear si la persona ya estaba completa -- se invirtió el orden (persona completa = corta ahí, sin importar invitaciones viejas). Causa 2 (la real, no solo el modal molestando): `fn_completar_membresia` solo resolvía la invitación más reciente -- si había 2+ pendientes, la segunda se quedaba sin cargo otorgado. Se agregó `fn_resolver_invitaciones_pendientes_extra`, llamada desde `fn_completar_membresia` y `fn_completar_membresia_general`, que otorga TODAS las pendientes de la cuenta al completar una vez. Migración nueva `20260821180000_kan213_membresia_una_vez_multiples_roles.sql`. Mobile: el pie de botones de `FormularioPaginado.tsx` no tenía `flex-wrap` (3 botones sin achicarse desbordaban el modal, "Saltar" quedaba cortado) -- se agregó wrap + ancho completo por botón en mobile. `tsc -b`/`oxlint`/`build` limpios
- [x] Le pasé el SQL completo de la migración a Matías para aplicar en el SQL Editor de Supabase (yo no tengo credenciales de la base)
- [x] Re-deployeé el frontend con el fix de membresía incluido -- backup previo en `app.somoscdv.com/public_deploy_backup_20260821_182941`, bundle nuevo (`index-DjHksh1w.js`) confirmado sirviendo en raíz
- [x] Migración `20260821180000_kan213_membresia_una_vez_multiples_roles.sql` aplicada en producción (confirmado por Matías, SQL Editor de Supabase)
- [ ] Falta: confirmación en vivo del fix de membresía (misma cuenta con 2 roles, y en mobile) -- código y migración ya están en producción, falta solo probarlo
- [x] Jira se liberó del captcha -- creado KAN-239 (fix de membresía, En revisión) y ya actualizado con el detalle
- [x] Bug real confirmado por el owner: Líder de CdP con doble cargo (también Sublíder vigente de la misma CdP) se autonotificaba "Tu sublíder cargó el reporte" al enviar su propio reporte -- KAN-240. Causa: `fn_notificar_reporte_sublider` (`57_notificaciones.sql`) solo chequeaba si el actor tenía cargo SUBLIDER_CDP, no si TAMBIÉN tenía LIDER_CDP de la misma CdP. Fix en `harness/11-esquema-bd/sql/112_fix_notificacion_reporte_doble_cargo.sql`: si tiene ambos cargos, cuenta como acción del Líder y no dispara el aviso (más chequeo defensivo de que el destinatario no sea el propio actor). Solo backend, sin cambios de frontend -- En revisión, falta aplicar en Supabase
- [x] Migración `112_fix_notificacion_reporte_doble_cargo.sql` aplicada en producción (confirmado por Matías, SQL Editor de Supabase) -- solo backend, no requiere deploy de frontend
- [ ] Falta: confirmar en vivo (KAN-240) que la cuenta de doble cargo ya no se autonotifica al enviar su reporte
- [x] Ícono de mostrar/ocultar contraseña (el "ojo") en los 5 campos de contraseña real del sistema -- KAN-241. Componente nuevo `frontend/src/components/ui/password-input.tsx` (envuelve `Input` con el toggle), aplicado en Login.tsx, CompletarCuenta.tsx (nueva + confirmar) y Cuenta.tsx (nueva + confirmar). No se tocaron los campos de PIN/OTP (6 dígitos, otro propósito). `tsc -b`/`oxlint`/`build` limpios
- [x] Deployeé el frontend con el ojo de contraseña -- backup previo en `app.somoscdv.com/public_deploy_backup_20260821_220912`, bundle nuevo (`index-DGaVM_61.js`) confirmado sirviendo
- [ ] Falta: confirmación visual en vivo del ojo de contraseña en los 3 formularios (KAN-241)
- [x] Constructor visible en el sidebar para Líder/Supervisor de Red -- KAN-242. Investigado primero: el lienzo (`EstructuraOrganizacional.tsx`) ya soportaba estos roles desde KAN-78 (ven todo, editan solo su propia Red), pero nada los llevaba ahí. Destrabé 3 puntos: `paneles-contexto.ts` (ítem de nav "Constructor" también para rolUI LIDER_RED), `ConstructorResumen.tsx` (guard que los redirigía a Dashboard) y `permisos.ts` (`RUTAS_LIDER_RED` sin `ROUTES.CONSTRUCTOR_RESUMEN`, bloqueaba el guard `<RequiereRol>` de App.tsx). Sin cambios de backend. `tsc -b`/`oxlint`/`build` limpios
- [x] Deployeé el frontend con el Constructor para Líder/Supervisor de Red -- bundle nuevo (`index-Cs4Vrde3.js`) confirmado sirviendo
- [x] Bug real en vivo reportado por el owner: "No se puede cargar" el Constructor para Líder/Supervisor de Red. Causa raíz: `obtenerEstructuraOrganizacional()` hace un solo `Promise.all`, y `fn_listar_usuarios` (uno de los llamados) exige ser Pastor/Supervisor/Super Admin -- para Líder/Supervisor de Red tira `ADMIN_FUERA_DE_ALCANCE` y tumba TODA la carga, no solo esa lista. También `ConstructorResumen.tsx` llamaba a `fn_mis_iglesias_hijas` (mismo requisito) sin chequear el rol antes
- [x] Fix frontend: `ConstructorResumen.tsx` ya no llama a `fn_mis_iglesias_hijas` si el rol no es Pastor/Supervisor -- deployado (bundle `index-ClDEZlnk.js`)
- [x] Fix backend: migración `113_fix_constructor_lider_red_listar_usuarios.sql` -- agrega `fn_es_lider_de_red_en_iglesia` como alternativa válida en `fn_listar_usuarios` (la lista solo se usa para mostrar de forma read-only quién es Pastor/Supervisor en el lienzo). Le pasé el archivo a Matías para aplicar -- **sin esto el Constructor sigue sin cargar** para Líder/Supervisor de Red
- [x] La migración 113 falló al aplicarse (42P13, "cannot change return type"): había tomado como base `harness/11-esquema-bd/sql/63_pastor_gestion_supervisor.sql`, desactualizado respecto a lo real desplegado (le faltaba la columna `es_principal` de KAN-154, y tenía una regresión en el JOIN con `persona` ya corregida en producción, `20260810300000_fix_regresion_listar_usuarios_persona_iglesia.sql`). Corregí la migración tomando esa versión real como base -- reenviada a Matías. El intento fallido no aplicó nada (falló antes del CREATE)
- [ ] Falta: confirmar que se aplicó la migración 113 corregida, y recién ahí confirmación visual en vivo con una cuenta Líder o Supervisor de Red (KAN-242)
- [x] Pedido del owner (llegó como "no pueden ver los departamentos", aclarado después como requisito): Líder/Supervisor de Red NO deben ver Departamentos (Evangelismo/Afirmación/Discipulado/Envío) en el lienzo -- otra rama administrativa que no les corresponde -- KAN-243. `crearGrafoEstructura` (layout.ts) acepta `ocultarDepartamentos`; `EstructuraOrganizacional.tsx` lo activa para `rolUI === 'LIDER_RED'`. Pastor/Supervisor/Super Admin sin cambios. Solo frontend. `tsc -b`/`oxlint`/`build` limpios
- [x] Deployeé el frontend con Departamentos ocultos para Líder/Supervisor de Red -- bundle nuevo (`index-CI2Nb7Lv.js`) confirmado sirviendo
- [ ] Falta: confirmación visual en vivo con una cuenta Líder o Supervisor de Red de que Departamentos ya no aparece (KAN-243)
- [x] Diagnóstico de cuenta huérfana (medinaeguezjuli@gmail.com): tenía `usuario_rol` LIDER_CDP asignado hoy pero sin `persona` (nunca completó la Membresía) -- al intentar invitarla de nuevo daba "ya existe una cuenta con ese correo, sin Persona vinculada". Le pasé a Matías un script de eliminación completa en una sola transacción (desactiva temporalmente `trg_no_delete_invitacion_lider`/`trg_no_delete_usuario_rol`, borra ambas filas, borra `auth.users`, reactiva los triggers) para volver a invitarla limpio desde el Constructor -- sin confirmar todavía si se ejecutó
- [x] Pedido del owner, ampliando KAN-243: Líder/Supervisor de Red solo deben ver SU propia Red (o Redes) y sus Casas de Paz -- ni Pastor/Supervisor de la Visión, ni el resto de las Redes de la iglesia -- KAN-244. `crearGrafoEstructura` suma `ocultarPastorSupervisor` y `soloRedesIds` (usa el mismo `redesEditablesIds` que ya se calculaba para decidir qué se puede editar). Solo frontend. `tsc -b`/`oxlint`/`build` limpios
- [x] Deployeé el frontend con el lienzo acotado a la propia Red -- bundle nuevo (`index-XQOpXPJw.js`) confirmado sirviendo
- [ ] Falta: confirmación visual en vivo con una cuenta Líder o Supervisor de Red de que solo ve su propia Red (KAN-244)
- [ ] Reporte del owner tras KAN-244: "carga bien pero se sigue viendo todo (Pastor, otras Redes, Departamentos)" con cuenta real Líder/Supervisor de Red -- contradice el fix ya deployado. Sugerí hard refresh (caché de navegador); no llegó confirmación antes de pasar al siguiente pedido -- **sigue sin confirmar, revisar en la próxima sesión si persiste**
- [x] Pedido del owner: extender el límite de Supervisor de Red de 1 a 2 simultáneos -- confirmado que es **solo para el Constructor** (Gestión de Redes ya permite Supervisores ilimitados) -- KAN-245
- [x] Backend (migración nueva `20260821190000_kan245_supervisor_red_limite_dos.sql`, no se tocó ninguna ya aplicada): `fn_estructura_asignar_cargo_red` ahora solo cierra-y-reemplaza para LIDER_RED, para SUBLIDER_RED cuenta vigentes y rechaza el 3ro; `fn_estructura_quitar_cargo_red` gana `p_persona_id` (DROP+CREATE, cambia de firma) para poder quitar a uno específico de los 2; `fn_estructura_invitar_supervisor_red` (camino "por correo") gana el mismo límite de 2. Hallazgo: `invitacion_lider.rol` guarda `'LIDER_RED'` tanto para Líder como para Supervisor de Red (el cargo real vive en `cargo_id`) -- `fn_completar_membresia`/`fn_resolver_invitaciones_pendientes_extra` ramificaban sobre ese campo y hubieran roto el límite al confirmar una invitación por correo; corregido para resolver el código real del cargo primero
- [x] Frontend: `PanelRedEstructura.tsx` ahora renderiza una tarjeta por cada Supervisor de Red vigente/invitado (hasta 2) más una vacía para agregar el siguiente; `confirmandoQuitar` pasa a llevar la persona específica; `NodoEstructura.tsx` apila hasta 2 Supervisores en el nodo de Red del lienzo. `tsc -b`/`oxlint`/`build` limpios
- [x] Le mandé el archivo de migración a Matías para aplicar (sin credenciales de base desde acá)
- [x] Deployeé el frontend con el límite de 2 Supervisores de Red -- backup previo en `app.somoscdv.com/public_deploy_backup_20260821_205034`, bundle nuevo (`index-DMAp9TGC.js`) confirmado sirviendo
- [ ] Falta: aplicar la migración `20260821190000_kan245_supervisor_red_limite_dos.sql` y confirmar en vivo (asignar 2do, bloquear 3ro, quitar uno sin afectar al otro, camino por correo) -- KAN-245 en En revisión
- [ ] Falta: commitear los cambios de los 11 trabajos de hoy (quedan sin commitear hasta revisarlos) -- KAN-234, KAN-235, KAN-237, KAN-238, KAN-239, KAN-240, KAN-241, KAN-242, KAN-243, KAN-244, KAN-245, todos en En revisión
