# Gonzalo — 2026-09-21

- [x] KAN-405 (Colaboradores temporales por código, Afirmación) -- trabajo de Lisa: permiso temporal sobre sesión ya logueada (no cuenta nueva), tabla separada `colaborador_codigo`/`colaborador_sesion` (no toca `usuario_rol`). Ítem "Colaborar" en sidebar, pantalla propia sin sidebar mientras se colabora (pedido en vivo del owner), panel de gestión en Afirmación (generar código, extender/pausar/finalizar, auditoría). Corte automático al vencer sin cron. Verificado en vivo (generar/canjear código, formulario completo, historial, las 3 acciones del líder, aviso de 10 min, corte automático) con `test@somoscdv.com`. 2 bugs reales encontrados y corregidos en vivo (regresión en 3 funciones de Afirmación por copiar versión vieja; trigger de auditoría rompía UPDATE en `colaborador_sesion`). Edición del propio historial quedó pausada (backend listo, UI diferida) porque `RegistrarPersonaAfirmacion` está en rediseño paralelo del owner en otra sesión -- se revirtió el intento de tocar ese archivo. PR #96, KAN-405 en "En revisión" (falta probar caso madre/satélite y 2 cuentas distintas).
