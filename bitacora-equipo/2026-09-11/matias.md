# Matías — 2026-09-11

- [x] Pedido del owner: faltaba un discipulado en la membresía, "Identidad de Hijo" -- debe aparecer de ahora en adelante en los 3 formularios de membresía (registro público, invitación, Afirmación), sin tocar membresías ya cargadas. Investigado antes de tocar nada: `tipo_discipulado` es un catálogo 100% global y dinámico (`fn_listar_tipos_discipulado`), y el guardado (`fn_guardar_membresia_extendida`) no hardcodea códigos -- un simple `INSERT` alcanza, sin tocar frontend ni el backend de guardado. Migración `20260911000000_tipo_discipulado_identidad_hijo.sql` (código `IDENTIDAD_HIJO`, orden 7, al final de los 6 existentes). Verificado con una prueba real contra producción (transacción con rollback) llamando al mismo `fn_guardar_membresia_extendida` que usan los 3 formularios: crea la fila en `persona_discipulado` correctamente. Aplicado a producción.
- [x] En el mismo pedido, agregar "Concubinato" a la lista de estado civil. Distinto caso al de arriba: `estado_civil` es un ENUM real de Postgres (`estado_civil_enum`), no una tabla catálogo -- y el frontend lo tiene **hardcodeado en 7 lugares distintos** (sin un único punto de verdad, deuda técnica preexistente, no introducida hoy): 2 definiciones de tipo (`persona.types.ts`, `registro-publico.types.ts`), `ESTADO_CIVIL_LABELS`, 3 validaciones `z.enum(...)` (Afirmación, registro público, Membresía Obligatoria) y 2 bloques de `SelectItem` (`CamposMembresiaFields.tsx`, compartido por registro público+Afirmación; `MembresiaObligatoria.tsx`, independiente). `FichaIdentidad.tsx` y las tarjetas KPI de `AfirmacionPersonas.tsx` ya leían `ESTADO_CIVIL_LABELS` dinámicamente, no hizo falta tocarlos. Backend: migración `20260911010000_estado_civil_concubinato.sql` con **solo** `ALTER TYPE estado_civil_enum ADD VALUE 'CONCUBINATO'` -- Postgres no permite usar un valor de enum nuevo en la misma transacción en que se crea, así que quedó en su propio archivo sin nada más (no hay ningún `ALTER TYPE` previo en el historial de este proyecto, quedó como precedente para la próxima vez que haga falta). Verificado con una prueba real contra producción (rollback): el cast `'CONCUBINATO'::estado_civil_enum` funciona y el `UPDATE` en `persona_detalle` guarda bien. `tsc`/`oxlint`/`build` limpios sobre los 7 archivos de frontend. Aplicado a producción. **Importante:** agregar un valor a un ENUM de Postgres no se puede deshacer con un simple `DROP` (no existe `ALTER TYPE ... DROP VALUE`) -- si algún día hay que sacarlo, hace falta recrear el tipo entero.
- [x] Pedido del owner: faltaba la pestaña "Testimonios Elite" también en Evangelismo de Líder de Red / Supervisor de Red (`EvangelismoRed.tsx`, mismo componente para ambos cargos), mostrando los testimonios de toda la Red agrupados por Casa de Paz, y conectada a un acceso directo del Dashboard. Antes de tocar código confirmé 2 decisiones con el owner: (1) qué card del Dashboard usar -- eligió repurpose de la card existente "Milagros registrados" (censo espiritual, `persona_milagro`) en vez de agregar una nueva, aun sabiendo que esa card era la **única** forma de llegar al filtro "Con milagro registrado" en Personas de Red (dato distinto a Testimonios Elite) -- ese filtro queda sin acceso directo (el dato en sí sigue existiendo en la BD, solo se pierde el atajo); (2) el listado agrupado por CdP solo muestra Casas de Paz con al menos un testimonio, no todas. Backend: `fn_evangelismo_testimonios_red(p_red_id)` nueva, mismo criterio de join por `casa_de_paz_red` y mismo chequeo de permiso que ya usa `fn_evangelismo_red` (cubre Líder Y Sublíder de Red -- confirmado que `fn_es_lider_de_red` ya incluye ambos cargos). Verificado el join contra datos reales de producción (rollback) antes de aplicar. Frontend: 3ra pestaña en `EvangelismoRed.tsx` (con `min-w-0`+truncate desde el arranque en los 3 triggers, aplicando ya el fix del bug de mobile de ayer en vez de repetirlo); la card del Dashboard ahora navega con `location.state.tabInicial` (mismo patrón que `filtroInicial` de `PersonasDeRedVista.tsx`, primera vez que se usa para elegir pestaña en vez de un filtro). Limpieza directa del repurpose (código que quedó sin uso, no dejado a medias): se sacó el conteo `milagros` de `conteosAccesoRapido` en `DashboardLiderRed.tsx` y el caso `'MILAGRO'` de `FiltroInicialPersonasRed`/`etiquetaFiltroRapido`/`cumpleFiltroRapido` en `PersonasDeRedVista.tsx` (nada más los seguía usando). `tsc`/`oxlint`/`build` limpios. Aplicado a producción.
- [x] Bug real reportado por el owner: al "inspeccionar" una Casa de Paz ajena desde el Dashboard de Red/Supervisor (`pages/Dashboard.tsx`, pila local que renderiza `DashboardLiderCdp.tsx` con `casaDePazId` explícito), los botones de esa pantalla (ej. "Creyentes") no llevaban a la info de ESA CdP puntual -- `navigate(ROUTES.PERSONAS, ...)` nunca mandaba el `casaDePazId`, y `Personas.tsx` miraba el contexto real del usuario (toda su Red) en vez de la CdP inspeccionada. Encontré el mismo síntoma en Evangelismo, Historial de Reportes y Testimonios de CdP. Le pregunté al owner si arreglaba solo Personas (su ejemplo) o las 4 -- eligió las 4. Verifiqué primero que el backend ya soporta esto sin cambios: `fn_es_rol_superior_de_cdp` (ya en producción) ya cubre Pastor/Supervisor/Líder-Sublíder de la Red de esa CdP para leer sus datos -- era 100% bug de frontend. Fix: `DashboardLiderCdp.tsx` manda `casaDePazId` + `casaDePazEtiqueta` en el `state` de las 4 navegaciones; `Personas.tsx`/`HistorialReportes.tsx`/`TestimoniosCdp.tsx` lo priorizan sobre el contexto del usuario si está presente. `Evangelismo.tsx` fue el más delicado (3 ramas tempranas por rol a saltear cuando se inspecciona) y ahí además oculté "Nuevo evangelizado" para Líder/Sublíder de Red inspeccionando -- a diferencia de las otras 3 páginas (solo lectura), el INSERT en `evangelismo` exige `fn_puede_reportar_cdp`, que NO incluye cargos de Red, así que mostrar el botón hubiera sido una acción que el backend iba a rechazar. `tsc`/`oxlint`/`build` limpios. Sin cambios de backend, el permiso ya existía. Falta confirmación visual en vivo.
