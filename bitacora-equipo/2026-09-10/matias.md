# Matías — 2026-09-10

- [x] El owner pidió que volviera la cita bíblica (Marcos 16:15) en la barra de intro de Evangelismo CdP, que se había sacado ayer para bajar el scroll en móvil. Se agregó de vuelta pero **solo visible en desktop** (`lg:block`), oculta en celular, para no reabrir el problema de scroll que motivó sacarla. Al mergear a `master` se trajeron 18 commits nuevos de Gonzalo (KAN-354 miniatura de anuncios, KAN-339 Super Admin solo lectura + Pastor/Supervisor control total en Afirmación/Evangelismo, banner de modo lectura) -- verificadas ambas migraciones nuevas contra producción antes de deployar (ya estaban aplicadas a mano, coincide con el patrón de siempre). `tsc`/`oxlint`/`build` limpios sobre el combinado. Mergeado (`feature/evangelismo-cita-biblica-desktop`) y desplegado por SSH -- confirmado `index-D-mhz8Ky.js` en producción (incluye mi cambio + todo el trabajo de Gonzalo).
- [x] Pedido de extender el rediseño pastel de Evangelismo a Líder de Red y Supervisor de Red. Hallazgo antes de tocar código: **ambos ya comparten exactamente la misma pantalla** (`EvangelismoRed.tsx`) -- `contexto.rolUI` es `'LIDER_RED'` para los dos, `cargoRed` (`LIDER`/`SUPERVISOR`) solo cambia la etiqueta (mismo patrón ya confirmado en el Dashboard). Confirmado con el owner que el alcance es solo ese archivo, sin tocar `EvangelismoSupervisorVista.tsx` (panel de toda la iglesia, banner pixel-exacto, fuera de alcance a propósito). Aplicado: barra combinada intro+navegador de mes (no tenía hero propio), 2 pestañas (Resumen: métricas pastel + evangelismo propio + metas por CdP + tendencia; Calendario: calendario + detalle del día), `KpiCard`/`KpiMosaico` → `CardIndicadorPastel`. Intenté lazy-loadear `TendenciaEvangelismo` (mismo patrón de ayer) pero Vite avisó `INEFFECTIVE_DYNAMIC_IMPORT` porque `EvangelismoSupervisorVista.tsx` todavía lo importa estático -- revertido a import simple en vez de dejar una optimización que no cumplía nada. Cero cambios de lógica/datos. `tsc`/`oxlint`/`build` limpios. Mergeado (`feature/evangelismo-red-rediseno-pastel`) y desplegado por SSH -- confirmado `index-CaR6cCL0.js` en producción.
- [x] El owner reportó que en celular no se veía la cita bíblica de Evangelismo CdP (estaba `hidden lg:block`, oculta a propósito por el bloque de 2 líneas que ocupaba en desktop). Se integró como una 3ra línea truncada bajo el subtítulo (mismo bloque de título, no una fila aparte) -- visible en toda pantalla, agrega una sola línea de alto en vez del bloque completo. `tsc`/`oxlint`/`build` limpios. Mergeado (`fix/evangelismo-cdp-versiculo-mobile`) y desplegado por SSH -- confirmado `index-D0QVCgYP.js` en producción.
- [x] **Autopsia de rendimiento de carga (pedido del owner: "carga demasiado lento, gama media y baja").** Método: analizar el `modulepreload` del `index.html` (lo que se descarga en la primera pantalla) y rastrear qué chunks pesados caían ahí sin necesidad. Hallazgos y fixes, todos verificados contra el build real: (1) **clsx** (el helper `cn`, usado en TODA la app) quedaba bundleado dentro de `vendor-charts` porque recharts también lo usa -- resultado: **53 chunks de páginas sin ningún gráfico** importaban vendor-charts (125 kB gzip de recharts) solo para usar `cn`, y se precargaba en el arranque. Nuevo grupo `vendor-utils` lo saca → bajó a 13 chunks (solo los gráficos reales). (2) Nuevo grupo `vendor-core` (sonner/next-themes/zustand/react-easy-crop) para sacar las libs eager del catch-all. (3) `jspdf-autotable` movido a `vendor-pdf`. (4) `DescargarPdfButton`: jspdf+html-to-image (~144 kB gzip) ahora cargan al hacer clic en "Descargar", no en el arranque vía los dashboards eager. (5) `ModalAnuncios` (montado siempre en `PrivateLayout`): react-zoom-pan-pinch ahora lazy. (6) `build.modulePreload.resolveDependencies`: saca del `<head>` del arranque el preload de los vendors pesados que SOLO usan páginas lazy (recharts/jspdf/framer-motion/@xyflow/zoom) -- rolldown-vite los hoisteaba por ser compartidos entre rutas lazy; quitar el hint no rompe nada (el chunk carga solo al abrir su ruta). **Neto: ~270 kB gzip menos en la primera pantalla** (recharts 125 + jspdf 144). Verificado: el `modulepreload` del arranque ya no incluye ninguno de los 5 vendors pesados; los chunks de gráficos/PDF siguen importando su vendor (funcionan al abrir la ruta); smoke test de `vite preview` OK. `tsc`/`oxlint`/`build` limpios. Mergeado (`perf/quitar-vendors-pesados-del-arranque`) y desplegado por SSH -- confirmado en producción (`index-kf36s0g7.js`, sin vendors pesados en el preload). **Falta confirmar la mejora percibida en un dispositivo de gama baja real.**
- [x] Pedido del owner: "hay mucho color morado" -- regla nueva: un color por sección, sin repetir tonos entre secciones sin relación. Auditoría del sidebar (`utils/permisos.ts`): **Calendario y Departamentos compartían exactamente `#af52de`, URL de membresía y Gestión de Redes compartían `#5e5ce6`**. Reasignados (Calendario→`#ffd60a`, Departamentos→`#ff3b30` -- coincide con el rojo institucional de Discipulado, que Departamentos justamente lista; URL de membresía→`#32ade6`; Gestión de Redes se queda con el `#af52de` que quedó libre) -- también actualicé los headers propios de `Departamentos.tsx`/`GestionRedes.tsx` que tenían otro hex a mano, descoordinado con su ícono del sidebar. Dentro del módulo Evangelismo: `pages/Evangelismo.tsx` (CdP) tenía morado en 2 lugares -- bajado a 1 sola card ("Meta vigente"). `EvangelismoRed.tsx` (Líder/Supervisor de Red) tenía morado en 5 lugares -- bajado a **cero**, todo a NARANJA (ya estaba en la paleta del módulo, casi sin usar), incluido el ícono de "casa" (Home) de cada fila en "Metas por Casa de Paz" que el owner señaló puntualmente (tinte morado sin relación con el resto de las cards pastel). Mismo ajuste de color en el botón "Asignar a todas" de `EvangelismoSupervisorVista.tsx` (sin tocar su banner/layout, que sigue fuera de alcance). Cero cambios de lógica. `tsc`/`oxlint`/`build` limpios. Mergeado (`fix/armonizar-colores-sidebar-evangelismo`) y desplegado por SSH -- confirmado `index-DNg4l8Sp.js` en producción.
- [x] Pedido del owner: quitar la sección "Sin reporte esta semana" (listado de Casas de Paz sin reportar) del Dashboard de Líder de Red / Supervisor de Red (`DashboardLiderRed.tsx`, ambos comparten el componente). Se sacó solo esa sección visual -- `sinReporte`/`reportadas` se mantienen intactos porque siguen alimentando la card "Reportes al día" de Indicadores. `tsc`/`oxlint`/`build` limpios. Mergeado (`fix/dashboard-red-quitar-aviso-sin-reporte`) y desplegado por SSH -- confirmado `index-4fXzJG1_.js` en producción.
- [x] **Bug real reportado por el owner: el Líder de CdP aparecía en Personas, en la lista de asistencia de Reportes y en el Historial de Asistencia, "no debe figurar en ningún momento".** Investigado antes de tocar nada: es un efecto secundario directo del fix de membresía del 2026-09-09 (`fn_asegurar_membresia_cdp_por_cargo`) -- verificado contra producción que los 64 Líderes recibieron su fila en `casa_de_paz_membresia` exactamente el 09-09 a las 04:09:09 (el backfill de ayer). Ese fix fue intencional y consultado con el owner en su momento ("cambia qué cuenta como Miembro en dashboard/reportes"), así que antes de tocar código le pregunté el alcance exacto -- confirmó que **Personas y los Dashboards (censo/ministerios/milagros) se quedan igual** (eso sí era la intención de ayer), pero el Líder no debe contarse para asistencia semanal ni en el Historial. Implementado sin tocar `obtenerMiembrosCdp` (compartida con `MultiplicarCdpDialog.tsx`, donde el Líder sí debe poder elegirse al dividir una CdP) -- nuevo helper `obtenerIdsLiderCdp`/`useIdsLiderCdp`, filtrado puntual dentro de `Reportes.tsx` (a nivel de componente) y dentro de `obtenerHistorialAsistencia` (único consumidor, seguro filtrar ahí). Sublíder NO se excluye, sigue contando para asistencia normal. Verificado contra un caso real (Líder de una CdP con cargo y membresía activos, coincide). `tsc`/`oxlint`/`build` limpios. Mergeado (`fix/lider-cdp-no-figura-asistencia`) y desplegado por SSH -- confirmado `index-B1MA_lk8.js` en producción.
- [x] Pedido del owner: el ícono "Home" (casa) encerrado en un círculo se repetía con el mismo color fijo (marino/naranja/teal según la pantalla) sin importar a qué Red pertenece cada Casa de Paz -- "muy soso". Antes de tocar código pregunté dónde exactamente, porque el patrón aparece en pantallas con alcances distintos (algunas ven una sola Red, otras varias a la vez) -- confirmó **solo las vistas de Líder de Red / Supervisor de Red y "Gestión de Casa de Paz"** (`GestionRedVista.tsx`, la pantalla que ve `rolUI === 'LIDER_RED'`, cubre ambos cargos, confirmado en `CasasDePaz.tsx`), no el panel iglesia-wide de Afirmación/Supervisor que mezcla varias Redes en la misma lista (quedaría para otra ronda, requiere sumar el color de Red a una función SQL que hoy no lo trae). 3 círculos con color fijo encontrados y corregidos: `GestionRedVista.tsx` (badge de cada CdP en la lista, TEAL fijo), `EvangelismoRed.tsx` ("Metas por Casa de Paz", naranja fijo), `Visitas.tsx` (tarjeta de cada visita, marino fijo). Los 3 ya tenían o pudieron reusar el color de la Red (`red.color` / `misRoles.redes_lider` / Constructor) con el mismo criterio "blanco = sin elegir" que ya usa el hero de esas mismas pantallas -- se cambió el círculo de un tinte plano a `degradadoIdentidadColor(colorRed)` (mismo lenguaje visual que `GestionSubliderVista.tsx`/`DashboardLiderRed.tsx`, gradiente en vez de tinte plano), con fallback al color fijo de antes cuando la Red no tiene color elegido. Cero cambios de datos/lógica. `tsc`/`oxlint`/`build` limpios. Mergeado y desplegado por SSH.
- [x] El owner vio el resultado y pidió ajustar el estilo: mantener el color de la Red pero sin relleno sólido -- eligió (con preview) la opción de círculo con tinte suave (`color-mix(... 12%, transparent)` de fondo + ícono pintado con el color de la Red, sin blanco ni degradado), el mismo patrón que ya usa el resto de la app para íconos de fila (TarjetaHeader, listas), en vez del degradado sólido tipo "sello de identidad" que había puesto antes. Revertido en los 3 lugares del punto anterior. De paso, en `GestionRedVista.tsx` el badge de la fila ya no reusa `IconoBadge` (esa función es del estilo "sello" sólido, se sigue usando tal cual en el hero de la misma pantalla) -- al sacarla de ahí quedó sin ningún otro uso, así que se borró la función completa en vez de dejarla muerta. `tsc`/`oxlint`/`build` limpios. Mergeado y desplegado por SSH.
- [x] Pedido nuevo del owner: pestaña "Testimonios Elite" en Evangelismo de CdP (`pages/Evangelismo.tsx`) para cargar milagros o testimonios, **solo** de evangelizados registrados con tipo Elite (`tipo_evangelismo`, codigo `ELITE`, ya existía en el catálogo junto a "1+1"/"Semilla" -- confirmado contra producción antes de tocar nada). Aclarado con el owner: una sola entrada de texto libre (sin distinguir milagro/testimonio), siempre ligada a un evangelizado puntual, y sin edición/borrado desde el frontend (registro histórico) -- mismo permiso de carga que el resto de Evangelismo (Líder y Sublíder de la CdP vigente, vía `fn_puede_reportar_cdp`, ya existente). Tabla nueva `evangelismo_testimonio` (migración `20260910000000_evangelismo_testimonio_elite.sql`): trigger `fn_validar_evangelismo_testimonio_elite` valida en el INSERT que el evangelizado exista, pertenezca a la MISMA Casa de Paz y sea de tipo Elite **por código** (no por nombre, que puede editarse) -- no se puede expresar con un CHECK simple porque mira otra tabla. RLS: SELECT igual de amplio que `evangelismo` (misma iglesia), INSERT con `fn_puede_reportar_cdp`, sin políticas de UPDATE/DELETE (decisión explícita, además `trg_no_delete` bloquea el DELETE físico). Frontend: nuevo componente `TestimoniosElite.tsx` (selector de evangelizado Elite de esa CdP + textarea + lista de lo ya cargado), 3ra pestaña en Evangelismo de CdP. Aplicado y **verificado con pruebas reales en una transacción con rollback**: rechaza evangelizado de otra CdP, rechaza tipo no-Elite, acepta el caso válido -- los 3 casos confirmados contra datos reales de producción antes de dar por buena la migración. `tsc`/`oxlint`/`build` limpios. Migración aplicada a producción. Pendiente: mergear a `master` y deployar frontend, ticket Jira, prueba visual en vivo.
- [x] **El owner corrigió el diseño anterior**: no quería una pestaña con su propio formulario de carga -- el campo de testimonio/milagro debía vivir directo en el formulario "Nuevo evangelizado" (opcional, visible solo con tipo Elite elegido), y la pestaña queda solo como listado de lectura. Confirmé el alcance (pestaña 100% solo lectura, sin forma de completarlo después para alguien ya registrado) antes de tocar código. Reescrito: `fn_registrar_evangelizado` (misma RPC transaccional de KAN-277) ahora acepta `testimonio` opcional y, si el tipo elegido es Elite, inserta en `evangelismo_testimonio` **en la misma transacción** que la persona/evangelismo -- ningún paso queda a medias si otro falla, mismo criterio que ya tenía la RPC. `NuevoEvangelizadoDialog.tsx` suma un Textarea condicional ("Testimonio o milagro para esta persona") que solo aparece con tipo Elite. Se dieron de baja por completo (no dejar código muerto) el selector de evangelizado + formulario propio de la pestaña, `obtenerEvangelizadosElite`/`useEvangelizadosElite`, `crearTestimonioEvangelismo`/`useCrearTestimonioEvangelismo` y los tipos `EvangelizadoElite`/`NuevoTestimonioEvangelismo` -- `TestimoniosElite.tsx` quedó solo con el listado. De paso encontré (sin tocar, fuera de alcance de este pedido) que `fn_registrar_evangelizado` no guarda `segundo_nombre`/`segundo_apellido` al crear una persona nueva desde este formulario -- parece un bug preexistente, no relacionado con este cambio, a reportarle al owner. Verificado con 3 pruebas reales en transacción con rollback: Elite+testimonio crea la fila, Elite sin testimonio no crea nada, tipo distinto a Elite con texto se ignora sin romper el registro. `tsc`/`oxlint`/`build` limpios. Migración aplicada a producción.
