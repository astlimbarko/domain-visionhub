# Gonzalo — 2026-08-14

- [x] KAN-195: scrollbar de "Seleccionar rol" pasó de invisible (KAN-194) a una línea delgada gris con puntas redondeadas, estilo ChatGPT (.scrollbar-minimal). Pantalla de ayuda recortada para que entre sin scroll. Verificado en vivo, Finalizada
- [ ] KAN-196: filas de rol, círculo de iniciales, textos y paddings más compactos en "Seleccionar rol" para que quepan más roles sin scroll (con 7 roles no cabía). Faltó verificar en vivo (sin navegador conectado esta sesión), En revisión. Si no alcanza, sigue la grilla de 2 columnas (analizada, no implementada)
- [ ] KAN-197: saludo "Bienvenido" del selector de rol ahora cae al correo (truncado sin romper el dominio) si la persona todavía no cargó su nombre. Faltó verificar en vivo (navegador MCP caído), En revisión
