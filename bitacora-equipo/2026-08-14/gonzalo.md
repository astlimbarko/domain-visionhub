# Gonzalo — 2026-08-14

- [x] KAN-195: scrollbar de "Seleccionar rol" pasó de invisible (KAN-194) a una línea delgada gris con puntas redondeadas, estilo ChatGPT (.scrollbar-minimal). Pantalla de ayuda recortada para que entre sin scroll. Verificado en vivo, Finalizada
- [x] KAN-196: filas de rol, círculo de iniciales, textos y paddings más compactos en "Seleccionar rol" para que quepan más roles sin scroll. Verificado en vivo (5 roles sin scroll), Finalizada
- [x] KAN-197: saludo "Bienvenido" del selector de rol ahora cae al correo (truncado sin romper el dominio) si la persona todavía no cargó su nombre. Verificado en vivo, Finalizada
- [x] KAN-198: fondo de "Seleccionar rol" pasó de manchas tenues a un degradado diagonal real (violeta→blanco→verde), más fiel a multirol.jpeg. Botón "?" ahora alterna ayuda/lista de roles. Verificado en vivo, Finalizada
