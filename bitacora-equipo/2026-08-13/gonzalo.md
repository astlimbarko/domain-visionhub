# Gonzalo — 2026-08-13

- [x] Retomada la sesión del 2026-08-11 (KAN-181) tras reconectar los MCP (Atlassian, Slack, chrome-devtools)
- [x] Merge de `master` hacia `codex/refactorizacion-multirol` hecho localmente (commit `c096bdd`) — los 12 archivos en conflicto se resolvieron a favor de la rama (ya la absorbía y superaba, verificado en KAN-181). De paso entraron sin conflicto 3 features de Matías que no tocaban nuestra zona: KAN-144/145/146 (vincular Google a invitación existente) y KAN-151 (búsqueda global de personas con iglesiaId/iglesiaNombre)
- [x] Un import sin usar (`Network`) en `AppShell.tsx` tras la resolución — sacado. `tsc -b` y `oxlint` limpios, build de producción verificado
- [x] Confirmado con `git merge-tree` que un futuro merge real contra `origin/master` da 0 conflictos — la rama queda lista para el PR
- [x] Rama pusheada a origin (pedido explícito: "que todos hagan pull y comencemos de 0")
- [x] **KAN-181 mergeado a `master` de verdad**: PR #25 (`codex/refactorizacion-multirol` → `master`) creado y aceptado, commit `bcaf74f`. Toda la épica KAN-174 a KAN-181 ya está en `master` real — el equipo trabaja directo desde ahí, no hace falta pasar por la rama de feature. Ticket KAN-181 movido a Finalizada
- [x] Repasado con el owner: 21 ramas remotas ya mergeadas a `master` (candidatas a limpieza) — decisión de borrarlas queda en pausa, la está pensando
- [x] Decisión tomada: se borran las 21 ramas ya mergeadas, queda solo `master` como rama de trabajo (`codex/refactorizacion-multirol` se borra después, aparte). Respaldo con el hash/mensaje del último commit de cada una en `ramas-borradas-2026-08-13.md`, por si hace falta ubicarlas de nuevo
- [x] Limpieza total: además de las 21, se borraron `backup-master-local` (respaldo local viejo de 2026-07-19, un mes atrasado, nunca pusheado), 3 ramas locales más ya mergeadas (`feat/KAN-116-orden-selector-lider-red`, `feature/arquitectura-multirol`, `revision-new-frontend`) y finalmente `codex/refactorizacion-multirol` (remota y local, pedido explícito del owner: "que solo quede master"). El repo queda con una sola rama
- [x] `CLAUDE.md`: 3 reglas nuevas de Jira (comentar cada movimiento de estado, nunca avanzar estado sin trabajo real, y dejar constancia si el trabajo real lo hizo Matías o Daniel aunque el ticket quede a nombre de Gonzalo). PR #28 mergeado a `master`
- [x] Rama local `generales` creada para trabajo del día a día que no encaja en una épica puntual
- [x] KAN-186: el selector multirol no mostraba "Supervisor de Red" cuando la misma persona tenía Líder y Supervisor de Red a la vez en la misma Red (`DISTINCT ON` en `fn_mis_roles_dashboard` colapsaba a una sola fila). Corregido, verificado en vivo en Red KARMA, Finalizada
- [x] KAN-187: botón "Descargar" del Constructor, ahora solo ícono sin la palabra al lado. Finalizada
- [x] KAN-188: Líder de Red vuelve a verse arriba de Supervisor de Red en el selector multirol (revierte el orden de KAN-116). Finalizada
- [x] KAN-184: label de Casa de Paz "Eliminada" → "Cerrada" (cambio solo visual, la BD no cambia). En revisión — falta que se pruebe en vivo con más calma, quedó con una duda de visibilidad RLS sin resolver del todo
- [x] KAN-189: "Eliminar Casa de Paz" (borrado definitivo) ya no bloquea por tener líder/sublíder asignado, ahora bloquea si tiene miembros o reportes/reuniones reales. El diálogo de confirmación nombra al líder/sublíder si existen. Verificado en vivo (RPC directo + UI), Finalizada
- [ ] KAN-190: eliminar por completo (CdP y Red) restringido a solo Super Admin (antes también Supervisor); se agregó el botón "Eliminar Casa de Paz" que faltaba en la vista de una CdP ya cerrada. Verificado en vivo que Supervisor ya NO puede (botón oculto + RPC rechaza). Falta que se pruebe con una cuenta Super Admin real (no tengo esas credenciales) — queda en "En revisión"
- [x] KAN-191: rediseño visual de "Seleccionar rol" (fondo degradado tipo gradient mesh + fila superior con logo de la iglesia + avatar con iniciales). Solo estética, verificado en vivo desktop y mobile, Finalizada
- [x] KAN-192: modal "Completa tu Membresía" ahora saluda con el nombre, y si todavía no lo llenó muestra el correo. Verificado en vivo (test@somoscdv.com, sin nombre → mostró el correo), Finalizada
- [x] KAN-193: icono "?" del selector de rol ahora abre la pantalla de ayuda (según multirol-help.jpeg) dentro de la misma tarjeta, con botón para volver. Verificado en vivo, Finalizada
- [x] KAN-194: la tarjeta de "Seleccionar rol" no cabía en pantalla con varios roles (sin tope de alto). Se le agregó max-h-[90dvh] + scroll propio (mismo patrón que el Dialog compartido) y se ocultó la barra de scroll visualmente (nueva utilidad .scrollbar-none). Verificado en vivo achicando el viewport, Finalizada
