# Matías — 2026-09-13

- [x] Pedido del owner: el "Dashboard de Afirmación" (`DashboardAfirmacion.tsx`, distinto de "Membresía") tenía su propio estilo de tarjeta -- color sólido pleno + texto blanco, decisión explícita de una sesión anterior ("colores fuertes para notarse", divergente a propósito del resto del proyecto). Alineado al estándar actual: las 6 tarjetas (Líderes de CdP, URLs activas/inactivas, Redes cubiertas, Registrados por URL/formulario) pasan a `CardIndicadorPastel`, mismo componente que ya usan los Dashboards de CdP/Red y (como variante clickeable) Membresía de Afirmación. Sin `onClick` -- no se pidió que filtren ni naveguen, siguen siendo solo informativas. Se unificaron los 2 grids (4+2) en uno solo de 6 con el mismo patrón responsive ya usado en "Indicadores" de `DashboardLiderRed.tsx`. `tsc`/`oxlint`/`build` limpios.
