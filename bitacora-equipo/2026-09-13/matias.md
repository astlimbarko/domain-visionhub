# Matías — 2026-09-13

- [x] Pedido del owner: el "Dashboard de Afirmación" (`DashboardAfirmacion.tsx`, distinto de "Membresía") tenía su propio estilo de tarjeta -- color sólido pleno + texto blanco, decisión explícita de una sesión anterior ("colores fuertes para notarse", divergente a propósito del resto del proyecto). Alineado al estándar actual: las 6 tarjetas (Líderes de CdP, URLs activas/inactivas, Redes cubiertas, Registrados por URL/formulario) pasan a `CardIndicadorPastel`, mismo componente que ya usan los Dashboards de CdP/Red y (como variante clickeable) Membresía de Afirmación. Sin `onClick` -- no se pidió que filtren ni naveguen, siguen siendo solo informativas. Se unificaron los 2 grids (4+2) en uno solo de 6 con el mismo patrón responsive ya usado en "Indicadores" de `DashboardLiderRed.tsx`. `tsc`/`oxlint`/`build` limpios.
- [x] Pedido del owner sobre las pestañas de Membresía de ayer: que las 15 cards de "Indicadores" funcionen como accesos directos de verdad -- hoy aplicaban el filtro pero se quedaban en la misma pestaña, sin saltar a ver el resultado (pensado explícitamente para mobile: "que no esté demasiado cargado"). Antes de tocar código confirmé con el owner un detalle: al quitar un filtro ya activo (segundo click sobre la misma card) también salta a Membresía -- comportamiento uniforme en los 15 botones, no solo al aplicar uno nuevo. `Tabs` pasó de `defaultValue` a controlado (`value`/`onValueChange`, estado `tab`); cada uno de los 15 `onClick` (directos, o vía las funciones compartidas `limpiarFiltros`/`alternarEstadoPorSigla`) ahora también hace `setTab('membresia')` después de aplicar su filtro. Botón nuevo "Volver a Indicadores" en la pestaña Membresía (mismo estilo que `VolverAlDashboard.tsx`) para volver a probar otro indicador sin depender de la barra de pestañas de arriba, que puede quedar fuera de pantalla si ya se hizo scroll dentro de la tabla en mobile. `tsc`/`oxlint`/`build` limpios.
