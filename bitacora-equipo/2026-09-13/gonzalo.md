# Gonzalo — 2026-09-13

- [x] PR #50 (spinner KAN-365) volvió a quedar `CONFLICTING`: Matías mergeó directo a `master` sin PR otras 3 veces más hoy (tabs controlado + auto-salto a Membresía al filtrar, fix de anillo de foco mobile, KAN-369/373/371/374/375 "emergencia roja"), 2 de esas tocando el mismo archivo del spinner
- [x] Confirmado que el spinner de KAN-365 NO estaba duplicado -- nunca se había mergeado a `master`, seguía solo en la rama
- [x] Resuelto el conflicto de nuevo (7 bloques, cada card combina `setFiltroEnCurso` + el `setTab('membresia')` nuevo de Matías), verificado en vivo que ambos comportamientos conviven bien, PR #50 otra vez MERGEABLE
- [x] Decisión del owner: revertir el rediseño de pestañas/tarjetas de Matías de esta pantalla (sin esperar a hablar con él primero) -- rama nueva `fix/kan365-spinner-vista-original` desde antes del rediseño (commit 854e1b6), archivo restaurado a chips simples con filtro inline + spinner reaplicado encima. Verificado en vivo: clic filtra la tabla sin cambiar de pantalla, varios filtros se combinan (Hombres + Bautizados), spinner aparece y se apaga solo sin quedar pegado. PR #51 abierta, PR #50 vieja cerrada con comentario explicando el porqué. Jira KAN-365 comentado, sigue Finalizada
- [x] PR #51 mergeada a `master`. Deploy manual a producción (app.somoscdv.com), con backup previo (`public_deploy_backup_20260913_pre_kan365_revert`) -- checklist de `harness/DEPLOY.md` completo. Encontrado y corregido de paso un error real de build (`Spinner` no acepta prop `style`, `tsc --noEmit` local no lo detectaba por node_modules desactualizado en el host vs el contenedor) -- PR #52, mergeada. Verificado en producción sin login: bundle servido coincide con el build nuevo, ruta profunda no da 404, `.htaccess` presente. Jira KAN-365 comentado, Finalizada
- [ ] Sigue pendiente: el owner va a hablar directo con Matías sobre el rediseño (no descartado para siempre, solo revertido de esta pantalla por ahora)
- [x] KAN-376 (bug real, no solo el caso del ticket): `fn_invitar_lider` creaba invitaciones/`usuario_rol` duplicados sin chequear pendientes existentes -- al cancelar una duplicada, `fn_cancelar_invitacion_lider` borraba el `usuario_rol` de TODAS (esa tabla no distingue destino) y disparaba un baneo de 100 años por error. Afectó a 3 cuentas reales, 2 baneadas. Corregido en origen + índice único de defensa + limpieza de datos + desbaneo de las 2 cuentas, aplicado a producción. PR #54 mergeada. Jira "En revisión" -- falta que el owner confirme en vivo que "Reenviar" funciona para leonosinagafelipa@gmail.com
- [x] KAN-377: Super Admin no tenía forma de cerrar sesión en mobile (el drawer donde vive "Salir" estaba excluido del todo por el tema oscuro). Reutilizado el mismo Sheet/logout de los demás roles. De paso encontrados y corregidos 2 bugs reales invisibles hasta ahora (fondo claro en vez de oscuro por conflicto de especificidad CSS, encabezado mostraba el nombre de una iglesia en vez de "VisionHub"). Verificado en vivo de punta a punta con Playwright (login logueado por el owner, yo nunca toco contraseñas). PR #55 mergeada, Jira Finalizada
