# Gonzalo — 2026-08-15

- [x] KAN-199: filas de rol del selector pasaron de tarjetas con fondo de color a lista plana con divisores (m.png/m_hover/m_click) -- hover con tinte gris, click con borde de color + ripple, delay de 150ms antes de navegar para que se vea. Avatar con punto verde "en línea", saludo en 2 líneas. Verificado en vivo, Finalizada
- [x] KAN-200: bug real -- "+ Añadir sublíder" en una Casa de Paz cerrada (Constructor) abría igual el diálogo de asignación, el backend rechazaba con CDP_INEXISTENTE pero se mostraba como "Ya existe una cuenta con ese correo" (mensaje sin relación). Corregido en NodoEstructura.tsx + PanelCasaDePazEstructura.tsx. Verificado en vivo con network inspection, Finalizada
- [x] KAN-201: bug grave -- el hook Before User Created (KAN-138) bloqueaba TODAS las invitaciones por correo a cuentas nuevas en todo el sistema (CdP, Red, invitar-usuario, crear-iglesia) con "registro público cerrado". El "salvavidas" que debía dejarlas pasar nunca funcionaba (consultaba una fila que todavía no existe). Corregido en la BD (migración) + redesplegadas 4 edge functions en producción. Verificado en vivo, Finalizada
- [x] KAN-202: 3 bugs en el diálogo de asignar cargo (CdP/Red) -- quitar un cargo con la X borraba al instante sin confirmar si la iglesia no tenía OTP activado (ahora siempre confirma), el nombre vacío no caía a correo (ahora sí, cuando hay dato), y Enter no funcionaba en los inputs (faltaba un `<form>`). Verificado en vivo, Finalizada
- [ ] KAN-203: bug real -- Super Admin no podía asignar Líder/Sublíder de CdP ni Red (fn_asignar_cargo_cdp/red nunca revisaban fn_es_super_admin, a diferencia de Departamento que sí). El error quedaba enmascarado como "Ya existe una cuenta con ese correo". Corregido y aplicado a producción. Falta que el owner lo confirme con su cuenta Super Admin real, En revisión
- [ ] KAN-204: mismo hueco de permiso que KAN-203 pero al quitar -- la política RLS de UPDATE en casa_de_paz_cargo/red_cargo tampoco revisaba Super Admin, el X confirmaba pero nunca borraba (sin error visible). Corregido y aplicado a producción. Falta confirmación con cuenta Super Admin real, En revisión
- [x] KAN-205: el diálogo de asignar cargo mostraba "Sin nombre" en vez del correo real (persona.correo es un campo de perfil aparte, casi siempre vacío -- el correo de login vive en auth.users). 2 RPC nuevas resuelven el correo real. Regla para todo VisionHub: sin nombre, mostrar correo, siempre. Verificado en vivo, Finalizada
- [ ] KAN-206 (anotado, sin implementar): al invitar líder/sublíder de CdP por correo a una cuenta existente, reemplazar el mensaje chico de arriba por una confirmación dentro del mismo modal + cierre automático, sin botón extra
- [x] KAN-63 (retomado): repaso de código de responsividad móvil del Constructor -- 2 huecos reales corregidos (botón "Quitar" de AsignarCargoDialog sin área táctil de 44px; DomicilioAnfitrionDialog sin `<form>`, Enter no hacía nada). tsc/lint limpios. Pasa a En revisión, solo falta prueba en dispositivo físico real
- [x] KAN-135 (retomado): verificación en vivo contra Supabase real de las 3 migraciones de hardening RLS/RPC -- encontrada y corregida una regresión real (una migración posterior del 11/08 recreó `fn_mi_membresia_incompleta` con DROP+CREATE, reabriendo ejecución pública sin querer). Resto de funciones/políticas intactas. Assignee corregido (estaba vacío)
- [x] Q-MR-12 decidido y implementado: buscar sublíder/anfitrión de CdP ahora prioriza los propios miembros de esa Casa de Paz, cae a toda la iglesia si no hay coincidencia (para poder anotar visitas de otra CdP). `buscarPersonas` con `cdpId` opcional, propagado en Constructor + las 3 pantallas viejas de gestión de Red/CdP. tsc/lint limpios, sin probar en vivo (sin navegador esta sesión) -- KAN-135 Finalizada por confirmación del owner
- [x] KAN-87: investigado -- estaba en "Tareas por hacer" desde el 06/08 pero ya lo había resuelto Matías el 08/08 como parte de KAN-111 (mismo pedido: días de retención configurables de CdP eliminada). Verificado hoy contra Supabase real que sigue vigente (config `DIAS_RETENCION_CDP`, RLS con período de gracia, función de reactivar, panel del Supervisor genérico ya lo muestra). Vinculado como duplicado de KAN-111, pasa a En revisión (mismo estado que KAN-111, sin confirmación visual del panel todavía)
- [x] KAN-142: investigado -- ya implementado por completo (duplicado de KAN-155). Checkbox "Iglesia raíz" en CrearIglesiaDialog.tsx, marcado por defecto, `fn_crear_iglesia` acepta padre nulo. Verificado con datos reales en Supabase: 4 iglesias raíz existen hoy con `iglesia_padre_id IS NULL`, incluida Iglesia Sion (el caso de referencia del propio ticket). Finalizada
- [x] Revisión en lote de KAN-65/143/144/146/148/153/168/172/182/183/185/206: KAN-143 (vinculado a KAN-149/152/157, ya resueltos, pasa a En revisión) y KAN-146 (duplicado de KAN-144, pasa a En revisión) solo necesitaban vincularse. KAN-65 sigue bloqueada en despliegue manual al hosting (sin acceso). KAN-153 queda intacta (spec sin decidir). KAN-148 corregido: el correo de invitación no mencionaba la opción de Google -- ajustado el texto vía API de Supabase, Finalizada. Sigue con KAN-172/182/183/185/206, que sí requieren código nuevo
- [x] KAN-168: cron `fn_purgar_otp_antiguos()` vía pg_cron (diario 03:00), mismo patrón que el barrido de Redes. Aplicado y verificado en producción, En revisión (no hay forma de probar el borrado real sin esperar filas de 90+ días)
- [x] KAN-185: "Miembros totales" → "Creyentes totales" en el KPI del dashboard del Supervisor, único lugar de ese panel que lo mostraba. tsc limpio
- [x] KAN-206: AsignarCargoDialog ahora muestra confirmación inline + cierre automático (1.8s) cuando invitar por correo a alguien que ya tenía cuenta la asigna directo, en vez del toast con el modal quedando abierto. Aplicado en los 4 lugares que asignan Líder/Sublíder de CdP (Constructor + 3 pantallas viejas). tsc/lint limpios
- [x] KAN-182: 3 puntos reales -- Líder/Supervisor de Red ya no puede editar nombre/color de su Red (backend endurecido, quitado el fallback en fn_estructura_actualizar_red; UI oculta el picker/botón); OTP obligatorio siempre para ese nivel de rol sin importar el switch (fn_estructura_exigir_otp); trigger nuevo audita quién creó/modificó cada cargo de Red/CdP (creado_por/actualizado_por, cubre tanto las RPC de asignar como la baja directa). El punto 4 (texto "Cerrada") ya estaba hecho vía KAN-184, vinculado. tsc/lint limpios, aplicado y verificado en producción
- [x] KAN-183: clasificación automática de Simpatizante por ausencias consecutivas en la misma CdP. Umbral configurable desde el panel del Supervisor (categoría CDP, default 1, mismo motor que DIAS_RETENCION_*). `fn_recalcular_estados_cdp_reporte` corre después de guardar cada reporte (best-effort, no revierte el reporte si falla): si faltó las últimas N reuniones consecutivas de su CdP pasa a Simpatizante; si vuelve a asistir estando como Simpatizante, pasa solo a Creyente. Aplicado a producción, sin ejecutar contra reportes reales (cambiaría clasificaciones de gente real sin que nadie lo pidiera), tsc/lint limpios
- [x] KAN-172: tabla `auditoria_email_envios` (separada de `usuario_otp`) + 3 funciones SECURITY DEFINER para escribirla (RLS sin policies, no expuesta al cliente). Switch de 3 modos (OFF/ERRORES/COMPLETA) vía secret `AUDITORIA_EMAIL_MODO` de la edge function `solicitar-otp`, sin setear todavía (default OFF). `fn_generar_otp` ahora devuelve también el `id` de la fila -- exigió DROP+CREATE, con su revoke/grant explícito para no repetir la regresión de KAN-135. Desplegado a producción, verify_jwt sin cambios. Fuera de alcance: el webhook de "Delivered" de Brevo necesita que el owner registre una URL en el panel de Brevo -- columna lista, sin endpoint todavía. Con esto se completó toda la cola de tickets pedida hoy (65/87/135/142/143/144/146/148/153/168/172/182/183/185/206 + KAN-63)
- [x] KAN-147: duplicado literal de KAN-145 (login con Google sobre cuenta ya activada por contraseña). Verifiqué contra la documentación oficial de Supabase + `auth.users` real (8 cuentas invitadas) que el auto-linking por correo verificado es comportamiento de fábrica de Supabase Auth -- no hace falta código nuestro. El seguimiento real (falta prueba en vivo con Gmail real) queda en KAN-145; KAN-147 cerrado directo como Finalizada por ser duplicado, con comentario. Mismo criterio aplicado a KAN-146 (duplicado de KAN-144), también cerrado como Finalizada
- [x] Rama `generales` mergeada a `master` (PR #29), limpia y en la nube. Nueva rama `feature/sistema-anuncios` para retomar KAN-101 (T1-T9, ya con backend de Matías del 08/08)
- [x] Sistema de Anuncios -- spec (`anuncios.txt`) actualizado con 3 decisiones nuevas: alcance múltiple (varias Redes o Casas de Paz puntuales, no solo una), estado Borrador real, paridad Pastor con Supervisor de la Visión en Acción + nuevo cargo delegado "Encargado de Anuncios" (0..N personas, designado por Supervisor/Pastor con OTP). T1-T7 (KAN-102 a 108) implementados y **verificados en vivo de punta a punta** (navegador real, cuenta Pastor/Líder de Red): crear/publicar/eliminar anuncio, modal automático al ingresar para el destinatario real, cerrar (no reaparece), Encargados de Anuncios. En el camino se encontraron y corrigieron 2 bugs reales que llevaban desde el código original de Matías (08/08): `/anuncios` no estaba en ninguna ruta permitida por rol (bloqueaba el acceso incluso por URL directa) y las funciones `private.fn_anuncio_*` usadas dentro de políticas RLS nunca tuvieron permiso de ejecución otorgado a `authenticated` (fallaban con "permission denied" al publicar la primera imagen real). Pedido adicional del owner: formulario pasó de modal a página propia (`/anuncios/nuevo`, `/anuncios/:id/editar`) y se agregó poder subir/bajar el orden de cada anuncio -- 2 bugs más encontrados y corregidos en el camino (empate de prioridad, sintaxis FOR UPDATE + array_agg). Todo Finalizada en Jira menos T8/T9 (responsive fino, rol Miembro, sin implementar). Ítem de nav agregado al final (pedido explícito del owner): visible para Pastor/Supervisor/Líder de Red/Supervisor de Red, Casa de Paz sin acceso "por ahora" -- verificado en vivo con ambos roles
- [x] KAN-104/105 (retomado): rediseño de "quién lo ve" pedido por el owner -- los campos separados "Alcance"/"Destinatarios" confundían. Reemplazados por una oración en lenguaje llano ("Este anuncio lo van a ver los <roles> de <zona>", roles como chips) + lista real de personas en vivo debajo (`fn_anuncio_previsualizar_destinatarios` nueva). De paso, las Casas de Paz (sin nombre propio hace tiempo) pasan a identificarse por Líder + zona del anfitrión en vez de un nombre casi siempre vacío. Verificado en vivo
- [x] KAN-109 (T8): bug real -- el modal usaba `object-cover` + aspect-ratio fijo, recortaba cualquier imagen que no calzara exacto en 1:1 o 3:4, contra lo que pide el spec (nunca recortar/deformar). Corregido a `object-contain` sin aspect-ratio forzado. Verificado en vivo con capturas en mobile (390x844), desktop (1440x900) y pantalla baja/ancha (700x380) -- imagen completa sin recortes en los 3 casos. Finalizada
- [x] KAN-110 (T9): revisado -- el modelo ya estaba preparado para rol Miembro desde el trabajo de Matías (constraint + roles disponibles); confirmado que el trabajo de hoy (previsualización de destinatarios) no lo rompió, sigue devolviendo 0 personas de forma consistente para MIEMBRO en vez de fallar. Sin código adicional posible sin la épica completa de acceso Miembro, no pedida. Finalizada
- [x] **Épica Sistema de Anuncios (KAN-101, T1-T9) completa**: los 9 tickets (KAN-102 a 110) quedan Finalizada en Jira, todos verificados en vivo contra producción real
- [x] Retoques finales pedidos por el owner sobre el formulario/lista de Anuncios (no ticket propio, parte de KAN-104): se sacó el campo "Mensaje" (el sistema es solo imágenes) y cada sección del formulario pasa a llevar franja de color (antes se veía plano); la vista previa de la imagen al subir pasó de chica/recortada a grande y completa (`object-contain`); los botones de duración rápida (1/3/7/21/30 días) ahora muestran claramente cuál está seleccionado; en la lista de gestión la miniatura pasó de 56px a 112px y ahora se puede hacer clic para verla ampliada. Todo verificado en vivo y pusheado a `origin/feature/sistema-anuncios` (sin mergear a `master` todavía, a la espera del owner)
- [x] **Hipótesis confirmada**: verificado con datos reales (RPC/REST directo contra Supabase) que el `fecha_cierre` de la fila `anuncio_visto` de `test@` coincidía exacto con el momento en que yo había clickeado "Cerrar anuncio" durante mis propias pruebas del día anterior. No era bug de permisos/roles
- [x] **Rediseño de comportamiento** (pedido explícito del owner, no un simple revertido de mi cierre accidental): "son anuncios de inicio de sesión" -- deben verse en cada login mientras sigan activos, nunca quedar cerrados para siempre. Nueva columna `anuncio_visto.sesion_id` (Supabase trae `session_id` real en el JWT); `fn_anuncios_pendientes` ahora solo excluye un anuncio si fue cerrado en la MISMA sesión de login actual. Migración `20260816010000` aplicada a producción y verificada en vivo
- [x] Rediseño visual pedido junto con lo anterior: modal e imagen ampliada más grandes, esquinas rectas (antes redondeadas, ocultaban detalle cerca del borde), sombra/contorno marcado hacia el fondo, botón de cerrar (X) elegante y bien visible arriba, el modal ahora se muestra siempre por delante del formulario de membresía cuando coinciden (antes esperaba a que no hubiera membresía pendiente y podía no mostrarse en toda la sesión). Sidebar: "Anuncios" → "Anuncios de Sesión" (evita que suene a que anuncia en otro lugar). Verificado en vivo con capturas, tsc/lint/build limpios. Pusheado a `origin/feature/sistema-anuncios` (commit `c4dc26d`)
- [x] Borde blanco a los costados del modal: el contenedor forzaba el ancho fijo del modal aunque la imagen (acotada por el alto máximo) rindiera más angosta, dejando el fondo del marco como franja visible. Corregido para que el marco se ajuste al tamaño real de la imagen ya escalada. Mismo fix replicado en la vista ampliada de gestión (`Anuncios.tsx`), que tenía el mismo problema sin corregir. Verificado en vivo con login nuevo en el rol "Supervisor de Red" (commits `04609b2`, `d0171b2`)
- [x] Zoom real en ambas vistas (rueda del mouse, pellizco táctil, doble clic/toque) vía `react-zoom-pan-pinch` + componente compartido nuevo `ImagenAnuncioZoom` (mide el tamaño real de la imagen para mantener el encaje sin bordes). Verificado en vivo: zoom centrado en el puntero, doble clic alterna zoom, responsive correcto en mobile (390px) y tablet (820px) con recálculo al cambiar tamaño de pantalla. Nota técnica: la librería corre en el contenedor Docker del frontend, que tiene su propio `node_modules` (volumen separado del host) — instalar una dependencia nueva requiere `docker exec visionhub-frontend-1 npm install` además del `npm install` en el host, y reiniciar el contenedor para que Vite re-optimice dependencias. tsc/lint/build limpios, commit `099f59f`
- [x] Pregunta real del owner: si el servidor falla algún día, ¿estorba al resto del software? La app nunca quedaba bloqueada (el modal se cierra igual con X/Escape/clic afuera), pero se encontró un hueco real: si la imagen fallaba al cargar, el visor se quedaba con el spinner girando para siempre sin ningún aviso (`img.onload` sin `onerror`). Corregido con estado de error explícito + mensaje corto "No se pudo cargar la imagen" en las 3 vistas (modal de login, vista ampliada, miniatura de lista). Verificado en vivo que `onerror` dispara de forma confiable ante una URL rota real. tsc/lint/build limpios, commit `8c8a7d1`
