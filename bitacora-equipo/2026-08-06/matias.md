# Matías — 2026-08-06

- [x] Rediseño del Panel del Supervisor en pestañas (sidebar largo con muchas cards de configuración): agrupé las secciones por tema (General, Dashboards, Formularios, Estados y familias, Notificaciones y registro) en vez de una sola columna larga con scroll. Nuevo `components/ui/tabs.tsx` (primitivo `radix-ui`, mismo patrón shadcn que ya usa el resto de `ui/`). `tsc -b`/`oxlint` sin errores. No pude probarlo en el navegador con cuenta real (sin credenciales) -- verificado con un preview estático usando los tokens de color/radio reales del proyecto.
- [x] **Bug real encontrado y corregido, reportado por el owner** ("los reportes de ingresos por Casa de Paz no se terminan de visualizar correctamente al elegir un rango de fechas", en Finanzas del Supervisor, Dashboard del Líder de Red y Supervisor de la Red en Acción):
  - [x] **Causa:** `fn_ingresos_red` (`14_finanzas.sql`) devolvía `casa_de_paz.nombre` -- la columna manual que hoy está NULL en el 100% de las Casas de Paz de la base real (verificado: `0 con_nombre_manual` de `5 total_cdp`), porque las CdP se identifican por su líder, no por un nombre propio (`fn_etiqueta_cdp` calcula el nombre a mostrar, mismo patrón ya documentado en `78_dashboard_cdp_nombre_dinamico.sql` y `97_fix_nombre_cdp_vacio_registro_via_url.sql` -- esta es la cuarta vez que aparece el mismo bug). El frontend (`agruparFinanzasPorCdp`, `BloqueFinanciero.tsx`) agrupa los ingresos por ese nombre y lo cruza contra `etiqueta` (la que sí usa `fn_etiqueta_cdp`, vía `fn_listar_cdp`) para precargar una fila por CdP -- al no coincidir (NULL vs. "Miriam Vargas Choque"), todo el dinero real caía en un único bucket ficticio "Sin Casa de Paz" en vez de aparecer bajo la CdP correcta, que quedaba mostrando $0.
  - [x] Migración `102_fix_ingresos_red_etiqueta_cdp.sql`: `fn_ingresos_red` ahora devuelve `fn_etiqueta_cdp(c.id)` en vez de `c.nombre` (mismo `CREATE OR REPLACE`, firma sin cambios). Aplicada contra Supabase real (`Centro de Vida`, `mkgdeunylrmuogrfmdnq`) vía `supabase db query --linked --file`.
  - [x] Reproducido el bug y verificado el fix en vivo contra datos reales de la Red "Vida Nueva" (`be6ba34a-...`, julio 2026): antes del fix, `fn_ingresos_red` devolvía `casa_de_paz_nombre: null` en las 3 filas con movimiento real (Bs 299.15 ofrenda + Bs 194.70 diezmo de "Miriam Vargas Choque", Bs 128.20 ofrenda de "Freddy Aramayo"); después del fix, simulando una sesión real de Líder de Red (`request.jwt.claims`), devuelve el nombre correcto en cada fila.
  - [x] Ticket de Jira creado en la sesión siguiente, una vez habilitado el MCP de Atlassian: ver **KAN-80** más abajo.
  - [ ] Falta: confirmar con el owner que el desglose "Por Casa de Paz" ya se ve completo en el navegador (no pude probarlo yo mismo, sin credenciales de cuenta real).
- [x] Rediseño de "Casas de Paz" del Supervisor (`GestionEstructuraVista.tsx`) en pestañas, mismo pedido/patrón que el Panel del Supervisor: Estructura (Redes + Casas de Paz, juntas a propósito porque elegir una Red actualiza la lista de CdP de al lado), Fusiones y multiplicación, Invitaciones. `tsc -b`/`oxlint` sin errores, verificado con preview estático que las 3 pestañas entran en una fila sin amontonarse.
- [x] **Segundo bug real encontrado y corregido, reportado por el owner** ("las personas vinculadas a las casas de paz y las casas de paz vinculadas a las redes no se visualizan en Personas", probado como Supervisor/Pastor -- la lista aparecía vacía):
  - [x] **Causa, distinta a la del bug de ingresos:** no era un problema de `fn_etiqueta_cdp` (esa función ya estaba bien usada en `fn_buscar_personas` y `fn_personas_de_red`). La migración `99_buscar_personas_paginado_miembros.sql` (paginación + excluir "Semilla", commiteada el 2026-08-04) **nunca se había aplicado en Supabase** -- ya lo decía la bitácora de ese día, se me había pasado por alto hasta este reporte. La base real tenía una firma de `fn_buscar_personas` completamente distinta a la del repo (`p_red_id` en vez de `p_excluir_semillas`/`p_pagina`/`p_por_pagina`, aplicada a mano en algún momento sin quedar en ningún archivo del repo), así que la llamada RPC que ya hace el frontend fallaba con "function does not exist" -- sin manejo de error en `BusquedaPersonas`, la UI mostraba directamente "No hay personas registradas." en vez de un error.
  - [x] Reproducido el fallo en vivo simulando una sesión real de Pastor: la llamada exacta que hace el frontend (`fn_buscar_personas` con los 6 parámetros nombrados) tiraba `42883: function ... does not exist`. Apliqué `99_buscar_personas_paginado_miembros.sql` contra Supabase real; misma llamada después del fix devuelve datos reales (`total: 59`), y confirmé que una persona con membresía real (Carlos Mamani) ahora sí trae su `casa_de_paz_etiqueta` ("Miriam Vargas Choque") en vez de vacío.
  - [x] Quedó un overload viejo de `fn_buscar_personas` (la firma con `p_red_id`) sin uso en la base -- inofensivo (Postgres/PostgREST resuelve por nombre de parámetro y nada del repo lo llama), no lo borré para no arriesgar sin saber si algo externo lo usa. Si se confirma que no lo usa nadie, se puede limpiar con un `DROP FUNCTION` en una migración aparte.
  - [x] De paso audité el resto de "migraciones pendientes" que tenía anotadas de sesiones previas: 48, 49, 55, 56, 57 y 58 **ya estaban todas aplicadas** (verificado contra `pg_proc` en la base real) -- la memoria del proyecto estaba desactualizada, la corregí. Lo único que sigue realmente pendiente son las migraciones 100 y 101 (Calendario, ámbito iglesia / CdP padre-satélite) y las credenciales de Google OAuth.
  - [x] Mismo caso que el bug anterior: cubierto por **KAN-80** (ver más abajo), creado una vez habilitado el MCP de Atlassian.
  - [ ] Falta: confirmar con el owner que "Personas" ya muestra la lista completa en el navegador (no pude probarlo yo mismo, sin credenciales de cuenta real).
- [x] Compactado "Historial de Reportes" del Supervisor (`ControlReportesVista.tsx`, compartido con "Control de Reportes" del Líder de Red -- mismo componente, se benefician los dos): eran 5 bloques apilados con mucha separación (selector de Red, selector de mes, hero grande con donut, fila de filtros, card de la matriz) que se sentían desconectados entre sí. Pasaron a 2: una barra superior con navegación de mes + selector de Red juntos, y una sola card con el header, el resumen del mes, filtros y la matriz, todo junto. Nuevo prop `accionExtra` en `ControlReportesVista` para que el selector de Red viva en la misma barra que el mes en vez de una fila aparte -- actualizado en los dos lugares que la usan (`HistorialReportesSupervisorVista.tsx` y `pages/ControlReportes.tsx`).
  - [x] **Corrección del owner**: la primera pasada reemplazó el banner navy + donut + chips por 4 `KpiMosaico compact` -- el owner aclaró que solo pedía compactar, no cambiar los colores/estilo de antes. Revertido a banner navy + `DonutRing` + chips de colores (mismo look de siempre), pero angosto (menos padding, donut de 56px en vez de 112px) y viviendo adentro de la misma card en vez de ser un bloque grande aparte -- así queda compacto sin perder la identidad visual original.
  - [x] `tsc -b`/`oxlint` sin errores, verificado con preview estático las dos veces.
- [x] **Nueva funcionalidad, pedido del owner**: "Historial de Asistencia" del Supervisor (`HistorialAsistenciaSupervisorVista.tsx`) reescrito de punta a punta -- antes eran 2 `<Select>` apilados (elegí Red, elegí CdP) mostrando el historial de una sola CdP a la vez. Ahora:
  - [x] Grilla de tarjetas, una por Red, cada una con su **índice de fidelidad agregado** (aro de 3 segmentos verde/ámbar/rojo, mismo concepto que ya usa `IndiceFidelidadRing` para una CdP puntual en el dashboard del Líder de CdP, pero acá sumado entre todas las Casas de Paz activas de la Red). Solo lo ve el Supervisor de la Visión en Acción, a pedido explícito del owner.
  - [x] Al tocar una Red se abre un modal (`Dialog`) con una pestaña por Casa de Paz (mismo componente `Tabs` que ya usamos en Panel del Supervisor / Casas de Paz), cada una mostrando el historial de asistencia completo de esa CdP (reusa `HistorialAsistencia.tsx` tal cual, sin tocarlo).
  - [x] El índice de fidelidad por Red no existía como dato agregado -- se decidió con el owner sumarlo en el cliente (no hace falta backend nuevo): `fn_lista_miembros_cdp` ya existía en el backend (usada hoy internamente por `fn_dashboard_lider_cdp`) pero valida el acceso por sí misma, así que se puede invocar directo -- mismo criterio que ya se había usado antes para `fn_ingresos_cdp`. Nueva función `obtenerSemaforoCdp` en `dashboard.service.ts`, sin migración.
  - [x] Verificado en vivo contra datos reales (simulando sesión de Pastor vía `request.jwt.claims`): Red "Vida Nueva" (3 CdP) da 5 verde/3 ámbar/4 rojo = 42% de fidelidad, calculado a mano contra lo que devuelve `fn_lista_miembros_cdp` por cada una de sus 3 Casas de Paz -- coincide.
  - [x] `tsc -b`/`oxlint` sin errores, verificado con preview estático (grilla + modal con pestañas + historial de miembros).
- [x] **Nueva funcionalidad grande, pedido del owner**: "Evangelismo" del Supervisor -- antes `Evangelismo.tsx` no tenía ninguna rama para `SUPERVISOR` y caía directo al placeholder de "no tenés una Casa de Paz asignada", un callejón sin salida pese a estar en su menú. Usé modo plan (`EnterPlanMode`) para este porque toca una zona que ya se corrigió 3-4 veces con el owner en agosto (el significado de "Meta Global de la Red" cambió varias veces) -- confirmé el diseño con 2 preguntas antes de tocar nada:
  - [x] El Supervisor ve el mismo diseño que el Líder de Red (`EvangelismoRed.tsx`, sin reescribirlo), pudiendo elegir cualquier Red de la iglesia -- nuevo `EvangelismoSupervisorVista.tsx`, mismo patrón wrapper que `HistorialReportesSupervisorVista`/`HistorialAsistenciaSupervisorVista`.
  - [x] Nuevo: el Supervisor puede asignarle una **meta propia a la Red completa** (`meta_evangelismo_asignada.red_id`) -- card nueva "Meta de la Red" en `EvangelismoRed.tsx` detrás de un prop `metaRedAsignable` (default false, cero cambio para el Líder de Red). Reusa `AsignarMetaRedDialog` tal cual (ya era genérico) en vez de un diálogo nuevo.
  - [x] El esquema para esto **ya existía aplicado en producción desde el 2026-08-02** (migración `81_meta_global_red.sql`, columna `red_id` + policy de INSERT bifurcada) pero sin usar -- se había borrado solo la función de lectura por quedar sin caller (`83_limpieza_meta_global.sql`). Verificado en vivo (`information_schema`, `pg_policy`, `pg_constraint`) que seguía intacto antes de construir sobre eso.
  - [x] **Confirmado con el owner**: "Meta Global de la Red" (la suma de metas por CdP) no se toca, la meta del Supervisor convive aparte. Pero si repercute, repercute de verdad: se hereda hacia las CdP de esa Red que no tengan ya su propia meta asignada por su Líder de Red, y bloquea que esas CdP editen su meta propia hasta cumplirla -- igual que ya bloquea una meta CdP-específica.
  - [x] **Bug real encontrado durante el diseño, antes de escribir la migración**: si `fn_meta_efectiva` simplemente cascadeaba la meta del Supervisor hacia cada CdP sin meta propia, `fn_tasa_evangelismo_red` (que suma `fn_meta_efectiva` de cada CdP para "Meta Global") iba a contar la MISMA meta una vez por cada CdP que la hereda -- una Red con 3 CdP sin meta propia y el Supervisor asignando 50 hubiera dado una suma de 150, no 50. Se distingue el origen nuevo como `ASIGNADA_RED` (no `ASIGNADA` a secas) y `fn_tasa_evangelismo_red` excluye ese origen de su `SUM`.
  - [x] Migración `103_evangelismo_meta_supervisor_red.sql`: `CREATE OR REPLACE` de 3 funciones (`fn_meta_efectiva`, `fn_bloquear_meta_propia_bajo_asignada`, `fn_tasa_evangelismo_red`), verificadas contra la definición real en producción antes de tocarlas. Aplicada y verificada en vivo con una fila de prueba real (Red "Montero Norte", meta 8): `fn_meta_efectiva` de su CdP sin meta propia pasó de 0 filas a devolver `(8, ASIGNADA_RED)`; `fn_tasa_evangelismo_red` de esa Red se quedó en `meta_total: 0` (no 8) confirmando que el fix de la suma funciona; el lookup que usa el trigger de bloqueo encuentra la fila correcta. Fila de prueba borrada (soft-delete) al terminar.
  - [x] Frontend: `origen` de `TasaEvangelismo`/`MetaCdpRed` pasa a incluir `'ASIGNADA_RED'` -- nuevo helper `esMetaAsignada`/`quienAsignoMeta` (`utils/evangelismo-meta.ts`) para no repetir la comparación en los ~8 lugares que antes chequeaban `=== 'ASIGNADA'` a secas (`Evangelismo.tsx`, `EvangelismoRed.tsx`, `AsignarMetaRedDialog.tsx`). El badge "Red" ahora dice "Red" o "Supervisor" según de dónde viene la meta vigente.
  - [x] `tsc -b`/`oxlint` sin errores, verificado con preview estático de la card nueva (vacía y con meta asignada).
  - [x] **Dos correcciones del owner sobre esta misma feature, en la misma sesión:**
    - [x] **Prioridad invertida**: la 103 dejaba ganar la meta CdP-específica (Líder de Red) por sobre la meta de Red del Supervisor. El owner aclaró con un ejemplo concreto que tiene que ser al revés -- la meta del Supervisor manda siempre que esté vigente, incluso por sobre una CdP-específica ya asignada. Migración `104_fix_prioridad_meta_supervisor.sql`: mismas 2 funciones (`fn_meta_efectiva`, `fn_bloquear_meta_propia_bajo_asignada`), orden de prioridad invertido. Verificado en vivo con una CdP real que ya tenía meta CdP-específica (Miriam Vargas Choque, meta 10): antes de la 104 devolvía `(10, ASIGNADA)`; con una meta de Red de prueba (99) vigente para su Red, después de la 104 devuelve `(99, ASIGNADA_RED)` -- coincide con el ejemplo del owner. Fila de prueba borrada al terminar.
    - [x] **Visualización equivocada**: el owner pidió ver las Redes directamente, no caer en el detalle de Casas de Paz de entrada -- y poder asignar una meta a UNA Red o a TODAS de una vez. Saqué la card "Meta de la Red" que había puesto dentro de `EvangelismoRed.tsx` (quedó exactamente como la ve el Líder de Red, sin ningún cambio) y armé una sección nueva "Metas por Red" arriba de todo en `EvangelismoSupervisorVista.tsx`: lista de todas las Redes con su meta vigente, botón "Asignar a todas" (mismo patrón que ya usa `EvangelismoRed.tsx` para "todas las Casas de Paz", un nivel más arriba) + botón individual por Red. El detalle completo de una Red (con sus Casas de Paz) quedó como selector secundario más abajo, no como lo primero que se ve.
    - [x] `tsc -b`/`oxlint` sin errores tras las dos correcciones, verificado en vivo el cambio de prioridad y con preview estático la sección "Metas por Red" nueva.
  - [x] **Rediseño completo pedido por el owner con un wireframe exacto**: `EvangelismoSupervisorVista.tsx` reescrito de nuevo -- pasa de "elegí una Red y mirá su detalle" a un panel único de TODA la iglesia, sin tener que elegir nada:
    - [x] Header con degradado (mismo `GRADIENTE_HERO` que ya usa `Evangelismo.tsx` del Líder/Sublíder de CdP) + botón "Asignar metas".
    - [x] Card "Metas de la Red": barras (una por Red, altura proporcional a su meta asignada por el Supervisor) con botón "Editar metas" -- clickeable, abre un modal con la lista completa (reusa la fila que ya tenía armada + "Asignar a todas").
    - [x] 4 KPI agregados de TODA la iglesia (no de una Red): Total Meta (suma de las metas asignadas por Red), Evangelizados (suma del mes), Avance (%), Casas Activas -- estos 3 primeros se arman con `useQueries` (una consulta combinada por Red: meta asignada + tasa + lista de evangelizados, en paralelo) en vez de hooks-en-loop.
    - [x] Calendario + "Detalle del día" agregando la actividad de TODAS las Redes -- al elegir un día, agrupa por Casa de Paz con cantidad de personas y su Líder (antes el patrón existente en `EvangelismoRed.tsx` listaba personas sueltas, no por CdP+líder).
    - [x] Nuevo hook `useCdpsIglesia` (`useCasasDePaz.ts`) -- `fn_listar_cdp` ya soportaba `red_id` nulo para traer TODAS las CdP de la iglesia, pero el `useCdps` existente exige un `redId` a propósito (otros llamadores lo usan para no disparar la consulta hasta elegir una Red) -- hook nuevo y aparte, sin tocar el existente, cero riesgo para esos otros usos.
    - [x] Cargué la skill `dataviz` antes de armar el gráfico de barras (una sola serie -- Meta por Red -- así que un solo color/AZUL alcanza, sin necesidad de paleta categórica ni leyenda).
    - [x] `tsc -b`/`oxlint` sin errores, verificado con preview estático contra el wireframe exacto que pasó el owner y con la RPC real (`fn_listar_cdp` con `red_id` nulo trae Casas de Paz de varias Redes con su líder, confirmado en vivo).
  - [x] **Aclaración del owner tras ver el resultado**: preguntó si las barras de "Metas de la Red" eran dinámicas (se van llenando a medida que evangelizan) -- no lo eran, comparaban el tamaño de la meta entre Redes, no el avance. Confirmado que sí las quería como progreso. Las barras pasan a llenarse con `evangelizados / meta` de cada Red (tope 100%), verde cuando se cumple la meta, con el valor mostrado como "45/120" en vez del número de meta solo -- mismo criterio de color ya usado en la barra de progreso de `Evangelismo.tsx` (CdP). `tsc -b`/`oxlint` sin errores, verificado con preview estático en 3 estados (parcial, cumplida, sin meta).
  - [x] **Pedido de revisión + reorden**: repasé a fondo el flujo de asignación de metas (modal "Metas por Red", diálogo individual, "Asignar a todas", invalidación de queries) -- no encontré bugs en la lógica; además reproduje en vivo el camino completo insertar → leer con la misma consulta exacta que usa `RedMetaFila` (`obtenerMetaRedAsignada`), confirmando que una meta recién asignada se vería al toque sin recargar. Sigo sin poder clickear la UI real (sin credenciales). Reordené las secciones: los 4 KPI ahora van arriba, "Metas de la Red" (barras) quedó debajo, donde antes estaban los KPI. `tsc -b`/`oxlint` sin errores, verificado con preview estático el nuevo orden.
- [x] **QA a fondo pedido por el owner**, sobre los 5 roles (Supervisor de la Visión en Acción, Supervisor de Red, Líder de Red, Líder de CdP, Sublíder) -- sin poder loguearme en la app real (sin credenciales, y crear cuentas/loguear con contraseña está fuera de lo que puedo hacer aunque sea para QA), hice un QA a nivel RLS/RPC contra la base real, simulando cada sesión vía `request.jwt.claims` (mismo método de toda la sesión), activamente buscando romper permisos y constraints, con limpieza cuidadosa de cada fila de prueba al terminar:
  - [x] **Bug real encontrado y corregido**: `pol_evangelismo_insert`/`pol_evangelismo_update` (16_rls.sql, Módulo 1 original) reusaban `fn_puede_reportar_cdp`, que a propósito incluye a un Sublíder de CdP (para que pueda subir reportes semanales, permiso legítimo) -- pero la decisión del owner del 2026-07-31 ("el sublíder ve Evangelismo en modo solo lectura, no puede registrar evangelizados") solo se había aplicado en el frontend (botón oculto), nunca en el backend. Un Sublíder que pegara directo a la API podía insertar evangelizados igual. Migración `105_evangelismo_bloquear_sublider_registro.sql`: nueva función `fn_puede_registrar_evangelismo` (misma lógica que ya usa `pol_casa_de_paz_update` para bloquear la meta propia -- excluye sublíder a propósito), las dos policies de evangelismo pasan a usarla. `fn_puede_reportar_cdp` no se tocó -- reportes/asistencia del sublíder siguen intactos. Verificado en vivo: Líder de CdP (cuenta limpia, un solo cargo) sigue pudiendo registrar (sin regresión).
  - [x] Verificado en vivo el bloqueo de meta propia (`fn_bloquear_meta_propia_bajo_asignada`) con una meta realmente sin cumplir (insertada a propósito, 999999 vs 0 evangelizados) -- el UPDATE se rechazó con el mensaje esperado (`META_ASIGNADA_SIN_CUMPLIR`). Antes de eso probé con la meta real de "Miriam Vargas Choque" (10) y el UPDATE pasó sin bloquear -- **no era un bug**, esa CdP ya llevaba 26 evangelizados este ciclo, meta cumplida de sobra (comportamiento correcto, "ya podés editar tu meta propia").
  - [x] Verificados en vivo (rechazados correctamente): meta negativa (`chk_meta_asignada_positiva`), meta de Red solapada con otra vigente de la misma Red (`excl_meta_asignada_red_solapada` -- coincide exacto con el nombre que ya captura el frontend). Verificado por código (no reproducible en vivo: todas las cuentas de Líder de Red reales de este sandbox también son Pastor/Supervisor, así que cualquier prueba "cruzando de Red" se vuelve trivialmente permitida por el rol superior, no por el líder) que `fn_es_rol_superior_de_cdp` acota correctamente por la Red actual de cada CdP vía el JOIN a `casa_de_paz_red` -- un Líder de Red sin otro rol no podría asignarle una meta a una CdP de otra Red.
  - [x] Confirmado que "Supervisor de Red" (`SUBLIDER_RED`) y "Líder de Red" corren exactamente el mismo camino de código -- `fn_es_lider_de_red` (el helper central detrás de las 17 funciones del módulo, migración 91) ya trata a los dos cargos como iguales, así que probar con una cuenta Líder de Red cubre igual a Supervisor de Red sin necesitar una cuenta aparte.
  - [x] **Contratiempo propio, resuelto**: para probar el bloqueo con la meta real de Miriam Vargas Choque hice un UPDATE en vivo sin guardar antes el valor original de `casa_de_paz.meta_evangelismo` (la columna "propia") -- no encontré ningún bug (la meta ya estaba cumplida, por eso pasó), pero terminé sin saber cuál era el valor original. Lo dejé en `NULL` (estado neutro) en vez de inventar un número -- hoy no tiene ningún efecto visible porque esa CdP sigue gobernada por su meta asignada (10, vigente hasta el 8/8), pero avisen si alguien nota que la "meta propia" de esa CdP quedó vacía después de esa fecha.
  - [x] Todas las filas de prueba (7 en total, ver `observaciones ILIKE '%TEST%'`) confirmadas con `fecha_eliminacion` seteado al cerrar la sesión -- barrido final sin filas de prueba sueltas.
- [x] **QA con las cuentas reales del owner** ("francomatiassalvatierra548@gmail.com" Supervisor, "mattfrs.18fr@gmail.com" Supervisor de Red + Sublíder de CdP -- me pasó las contraseñas en el chat, no las usé ni las voy a usar, entrar contraseñas para autenticar es algo que no puedo hacer aunque el owner lo pida explícitamente):
  - [x] **Causa real de "la asignación de metas del supervisor no funciona", encontrada y corregida**: la cuenta `francomatiassalvatierra548@gmail.com` (Supervisor de la Visión en Acción real) **no tenía ninguna `persona` vinculada** (`persona.usuario_id` nunca se completó). Sin persona, `fn_mi_persona_id()` devuelve null, `personaId` en el frontend queda `undefined`, y `handleAsignar` de `EvangelismoSupervisorVista.tsx` hace `if (!redParaMeta || !iglesiaActivaId || !personaId) return;` -- se sale en silencio, sin ningún error visible. Encontré una persona sin vincular en la misma iglesia ("Matias Franco Salvatierra") que coincidía; confirmé con el owner antes de tocar nada y vinculé `persona.usuario_id` a esa cuenta. Verificado en vivo que la asignación ahora funciona.
  - [x] **Hallazgo metodológico importante, corregido en la misma sesión**: mientras probaba el límite entre Redes con la cuenta de Supervisor de Red (`mattfrs.18fr@gmail.com`, cuenta limpia sin otro rol -- la primera que tuve así en toda la sesión), un insert cruzando de Red pareció pasar sin bloquearse -- iba a reportarlo como una segunda falla de seguridad. Antes de afirmarlo, verifiqué `current_user`/`rolbypassrls` de mi propia conexión (`supabase db query --linked`) y confirmé que corre como **superusuario de Postgres (`bypassrls = true`)** -- salta las policies RLS por diseño, sin importar qué JWT esté simulando con `set_config`. Repetí la prueba forzando `SET ROLE authenticated` (el mismo rol que usa PostgREST/la app real) antes de cada insert: con RLS realmente activo, el insert cruzando de Red se rechazó correctamente (`new row violates row-level security policy`) -- **no había ningún bug ahí, era un falso positivo de mi propio método de prueba**. Repetí también, bajo `SET ROLE authenticated`, la asignación del Supervisor real y el bloqueo de meta propia del Líder de CdP -- ambos se sostienen con RLS real. Las verificaciones de esta sesión que dependían de funciones `SECURITY DEFINER` (que hacen su propio chequeo de permiso en el cuerpo de la función, no de RLS de tabla) no estaban afectadas por esto -- solo los INSERT/UPDATE directos a tablas lo estaban.
  - [x] Todas las filas de prueba de esta ronda también confirmadas borradas (soft-delete) al cerrar, incluida la limpieza del falso positivo.
- [x] **"Restaurá todo excepto la corrección de metas del supervisor"**: pregunté el alcance exacto con `AskUserQuestion` (no había ningún checkpoint de git para usar como referencia de "todo", y el pedido era ambiguo entre "todo el módulo de Evangelismo" y "solo lo que toqué en el QA de recién") -- el owner confirmó: revertir solo el endurecimiento de permisos que agregué por mi cuenta durante el QA (105), sin tocar el resto.
  - [x] Migración `106_revertir_bloqueo_sublider_evangelismo.sql`: `pol_evangelismo_insert`/`pol_evangelismo_update` vuelven a `fn_puede_reportar_cdp` (estado original de `16_rls.sql`, antes de la 105). `fn_puede_registrar_evangelismo` queda definida sin uso (no se borra, convención append-only del proyecto). Aplicada y verificada contra la base real (`pg_policy` -- ambas policies muestran exactamente la expresión original).
  - [x] El vínculo `persona.usuario_id` del Supervisor (la corrección real que pidió mantener) no se tocó -- sigue vinculado.
- [x] **3 pedidos nuevos del owner sobre roles/permisos** (en la misma sesión, después del restore):
  - [x] Le pidió sacarle a Matías (cuenta `francomatiassalvatierra548@gmail.com`, Supervisor de la Visión en Acción) todos los cargos salvo ese -- tenía 2 cargos `SUBLIDER_CDP` activos (CdP de "Marcelo Villca" desde el 31/7, CdP de "Miriam Vargas Choque" desde el 2/8) que no correspondían a su rol real. Cerrados con `fecha_fin = hoy` (mismo patrón que usa `quitarCargoCdp` en el frontend, no un borrado -- queda el historial). Verificado: solo queda `usuario_rol = SUPERVISOR_VISION_ACCION` vigente.
  - [x] **Bug real de routing, corregido**: al loguearse, un Sublíder de CdP caía en `/dashboard` (`ROUTES.DASHBOARD`, redirect fijo post-login para todos los roles) y veía `DashboardLiderCdp` con un flag `esSublider` -- pese a que su menú (`RUTAS_SUBLIDER_CDP`, `permisos.ts`) ni siquiera incluye "Dashboard" desde el 31/7. `pages/Dashboard.tsx`: agregado un redirect temprano `if (rolUI === 'SUBLIDER_CDP') return <Navigate to={ROUTES.CASAS_DE_PAZ} replace />` (mismo patrón que ya usan Super Admin/Líder de Departamento), directo a "Perfil de Casa de Paz" (`GestionSubliderVista`, ya era la vista correcta para este rol en esa ruta). Rama muerta de `vistaPorDefectoParaRol` (permisos.ts) para `SUBLIDER_CDP` eliminada.
  - [x] **Bug real encontrado y corregido**: los "sombreros" del sidebar (submenú bajo "Dashboard" para elegir entre varias Redes/CdP a cargo, `AppShell.tsx`) se armaban juntando TODOS los cargos de la persona sin filtrar por su rol activo -- un Supervisor con un cargo de Sublíder de CdP (como Matías, antes de la limpieza de arriba) veía "Panel operativo" mezclado con "CdP (sub): ..." bajo el mismo Dashboard, como si fueran del mismo nivel. Reescrito para que cada rama (`SUPERVISOR`/`LIDER_RED`/`LIDER_CDP`) solo arme sombreros de su propio nivel -- sigue permitiendo elegir entre varias Redes/CdP reales del mismo nivel, pero ya no mezcla cargos de niveles distintos.
  - [x] `tsc -b`/`oxlint` sin errores en los 3 cambios. Sin poder probarlo en el navegador (sin credenciales), verificado a nivel de código + la limpieza de cargos en vivo contra la base real.
- [x] **Conecté el MCP de Atlassian (Jira)** en esta sesión (login OAuth vía `/mcp`, servidor ya estaba agregado a nivel proyecto en `597b93f`) y quedó operativo para crear/actualizar tickets del proyecto `KAN`.
- [x] **Auditoría pedida por el owner**: revisar contra el código real (no contra lo que dicen los títulos) el estado de los tickets de Líder de Red, Líder de Casa de Paz, Sublíder de Casa de Paz y Supervisor de Red, para tickear en Jira lo que ya estuviera implementado. Usé 4 sub-agentes de exploración en paralelo, uno por módulo, contra los criterios de aceptación completos de cada ticket (no solo el título):
  - [x] **Resultado: ningún ticket candidato se pudo cerrar.** KAN-27, KAN-28 (roles "Soporte de Red"/"Supervisor de Red" -- no existen en `rol_sistema_enum` ni en la tabla `cargo`, son roles distintos del `SUPERVISOR_VISION_ACCION` que sí está implementado), KAN-29, KAN-30, KAN-32, KAN-34, KAN-35 -- no implementados. KAN-31 (alerta de reportes), KAN-16 (checkbox de pertenencia a la CdP en asistencia), KAN-24 (CRUD de Meta Global del Líder de Red) y KAN-60 (gestionar sublíderes desde el constructor visual) -- parciales, con evidencia concreta de qué falta (quedó documentado en el chat, no en Jira -- ninguno se tocó porque el owner pidió enfocar solo en lo que sí se completó).
  - [x] Hallazgo relevante para el owner: KAN-29 pide ocultarle montos al "Supervisor de Red", pero esta misma rama fue en sentido contrario -- le dio al Supervisor de la Visión en Acción acceso *completo* a Finanzas por Red. Son roles distintos (confirmado contra `rol_sistema_enum`), pero vale la pena que el owner lo tenga presente.
- [x] **Creados 4 tickets nuevos en Jira** (proyecto `KAN`, reporter y assignee = Gonzalo, según la regla del proyecto) para el trabajo real de esta rama que no tenía ticket propio, y marcados directamente en **Finalizada** porque el código ya está hecho:
  - [x] **KAN-80** -- Supervisor de la Visión: completar secciones vacías (Personas, Finanzas, Calendario, Departamentos) por Red -- cubre también el fix de `fn_ingresos_red` y la aplicación de la migración 99 (paginación de Personas) documentados arriba.
  - [x] **KAN-81** -- Historial de Reportes y Asistencia del Supervisor: agrupados por Red + rediseño en pestañas.
  - [x] **KAN-82** -- Evangelismo del Supervisor: metas por Red con prioridad sobre la CdP.
  - [x] **KAN-83** (tipo Error) -- Routing del Sublíder de CdP y sombreros de rol mezclados en el sidebar; mencioné ahí también la prueba de bloqueo de permisos en Evangelismo que se revirtió (105/106), para que quede registrada aunque no haya cambio funcional neto.
- [x] **Pedido del owner: "revisá los tickets de En curso y resolvelos, marcalos como completados"** -- eran 14 (KAN-16, 27, 29, 30, 31, 32, 34, 35, 37, 39, 40, 59, 60, 63). Antes de arrancar impl propuse por `AskUserQuestion` encarar solo los resolubles ahora (el owner lo confirmó) porque varios dependen de decisiones de producto sin resolver (roles "Soporte de Red"/"Supervisor de Red" que ni existen en el sistema, `open-questions.md` los marca como pregunta abierta) o de un esquema/canvas que todavía no existe (constructor visual de Estructura Organizacional). Quedaron **bloqueados sin tocar** (documentados en el chat, no en Jira porque no hubo cambio de código): KAN-27, 29, 30, 32, 35, 63.
  - [x] **Sin CLI de Supabase en esta sesión** -- no pude aplicar ninguna migración contra la base real. Para los tickets con cambios de esquema escribí la migración igual (siguiendo la numeración del repo) pero quedó **sin aplicar**, mismo estado en el que ya estaban 100/101 de una sesión anterior. Avisé esto explícitamente en cada ticket de Jira que lo necesitaba.
  - [x] **KAN-16** -- Checkbox "Asiste a esta CDP" por persona en la toma de asistencia (`Reportes.tsx`/`BuscadorPersonaMultiple.tsx`), invirtiendo `esVisita`. Migración `107_asistencia_confirmacion_manual.sql`: nueva columna `confirmado_manualmente` en `casa_de_paz_asistencia` -- el trigger `fn_validar_asistencia` solo recalcula automático cuando NO viene marcado, así el valor que decide el líder ya no se pisa (antes se enviaba desde el frontend y se descartaba siempre, bug real encontrado al revisar el ticket). Jira → En revisión (falta aplicar migración; sigue sin poder marcarse "visitante de otra CdP", el buscador solo ofrece miembros propios).
  - [x] **KAN-31** -- El estado verde/naranja de Control de Reportes pasa a calcularse en el servidor (`v_reporte_totales.estado_carga`, con `America/La_Paz` explícito) en vez de con `new Date()` del navegador; el plazo de gracia (antes hardcodeado en 2 días en el componente) pasa al motor de configuración existente (`fn_criterio`, criterio nuevo `DIAS_PLAZO_REPORTE`). Migración `108_control_reportes_plazo_configurable.sql`. Jira → En revisión (falta aplicar migración; una alerta realmente proactiva -- sin depender de abrir la pantalla -- necesitaría un job programado, pg_cron/edge function, fuera de alcance).
  - [x] **KAN-34** -- Histórico Anual de Casas de Paz eliminadas, completo: nueva pestaña en Estructura Organizacional, filtro por año y por Red, red/líder históricos (ya se podían derivar de `casa_de_paz_red`/`casa_de_paz_cargo`, tablas de vigencia que `fn_eliminar_cdp` no cierra), fecha y motivo de eliminación (nuevo -- ahora se pide un motivo al eliminar, con `ConfirmarCambioDialog` en vez de `ConfirmarQuitarDialog`, en los dos lugares donde se puede eliminar una CdP), usuario que eliminó (resuelto vía `persona.usuario_id`). Migración `109_historico_cdp_eliminadas.sql`. Jira → En revisión (falta aplicar migración).
  - [x] **KAN-37** -- Resultó ser un duplicado bajo el nombre viejo del rol: `harness/README.md` ya aclaraba que "Líder de la Visión en Acción" y "Supervisor de la Visión en Acción" son el mismo rol. El flujo pedido ya estaba armado en `PastorGestion.tsx` y cubierto por KAN-36 (Finalizada) -- cerrado sin cambios de código, comentario referenciando KAN-36. Jira → Finalizada.
  - [x] **KAN-39** -- El Calendario General del Supervisor pasa de un `<Select>` de una sede a la vez a un filtro multi-selección con vista consolidada de verdad (nuevo `CalendarioMultiIglesia.tsx`, con `useQueries` -- mismo patrón ya usado en la Evangelismo del Supervisor), cada evento del panel del día muestra su sede con color propio. Reutiliza `fn_mis_iglesias_hijas`/`fn_eventos_iglesia` de la migración `101_calendario_padre_satelite.sql` (ya escrita en una sesión anterior, también sin aplicar) -- sin migración nueva de mi parte, ya existía. Confirmé que "sede" en este sistema es literalmente una fila de `iglesia` (relacionada por `iglesia_padre_id`), no hace falta un catálogo nuevo. Jira → En revisión (depende de que se aplique la 101).
  - [x] **KAN-40 -- NO implementado, bloqueado por una decisión de producto ya tomada.** Para cumplirlo hay que darle a Pastor acceso a Calendario, pero `RUTAS_PASTOR` (`permisos.ts`) lo restringe a propósito -- el propio código tiene un comentario explícito diciendo que antes el Pastor tenía por error el mismo alcance que el Supervisor (incluía Calendario) y se corrigió para dejarlo "solo supervisión y consulta". No lo revertí sin que el owner lo confirme. Dejé un comentario en el ticket explicando el conflicto -- el filtro multi-sede (KAN-39) ya queda listo para reusar si el owner decide que sí.
  - [x] **KAN-59** -- Tarjeta de CdP en Estructura Organizacional: "Líder sin asignar" con fondo distinto cuando no hay líder, línea de dirección breve del lugar de reunión con "Dirección pendiente" si falta. Sin cambios de esquema (`lider_id` y el domicilio ya existían). Jira → Finalizada.
  - [x] **KAN-60** -- Iniciales + avatares circulares compactos (+N con tooltip) para sublíderes, nuevo componente compartido `AvatarIniciales.tsx` (antes duplicado entre `GestionSubliderVista.tsx` y este ticket) usado en la tarjeta de CdP y en el diálogo de asignación; se excluye al líder principal como candidato a sublíder (`excluirIdsExtra` nuevo en `AsignarCargoDialog`, no estaba filtrado antes -- bug real). El "constructor visual" que pide el ticket textualmente todavía no existe (Tarea 2/KAN-54 pendiente) -- implementado sobre el módulo de gestión real existente, mismo criterio que ya se usó para cerrar KAN-61 en la misma situación. Jira → Finalizada.
  - [x] `tsc -b` y `oxlint` limpios después de cada ticket (verificado de a uno, no solo al final).
