# Gonzalo — 2026-08-06

- [x] Verifiqué en vivo el ítem 9 (caso "correo ya existe", KAN-61): ya estaba resuelto desde 2026-08-02, sin cambios de código. Comenté KAN-61.
- [x] KAN-78 quedó sin asignado (reporter astlimbark) porque es para que Matías lo implemente después, no trabajo de esta sesión.
- [x] Empecé a mover tickets por todo su ciclo (no solo comentar): KAN-56, KAN-57, KAN-58 y KAN-61 pasaron a Finalizada, ya verificados en vivo. Actualicé CLAUDE.md con ambas reglas.
- [x] Departamento ya no abre directo el modal de asignar: primero abre un panel lateral (`PanelDepartamentoEstructura.tsx`, mismo patrón que Red/CdP); Afirmación tiene botón Asignar/Cambiar adentro, los otros 3 muestran "Próximamente".
- [x] Probado en vivo: Afirmación abre el modal anidado correctamente; Evangelismo muestra el panel sin acción. Comenté KAN-57.
- [x] El hex directo del color de Red ya funcionaba (clic en el swatch abre el selector nativo), pero no se notaba. Ícono final: gotero (Pipette) al final de la misma fila de colores, con tooltip.
- [x] Corregí una barra de desplazamiento fea que aparecía al pasar el mouse sobre los colores (el hover agrandaba las muestras y desbordaba la fila).
- [x] "Guardar cambios" del panel de Red ahora queda deshabilitado hasta que se cambie realmente el color. Comenté KAN-58.
- [x] Agregué "Quitar cargo" al panel de Afirmación (faltaba) y agregué confirmación (modal rojo + OTP) tanto ahí como en Quitar cargo de Líder/Supervisor de Red — antes quitaban al instante sin preguntar.
- [x] Extendí ConfirmarQuitarDialog (compartido) con OTP opcional, sin romper a quien ya lo usa (Calendario, Casas de Paz). Casas de Paz ya tenía confirmación propia, no necesitó cambios.
- [x] Probado en vivo ambos casos. Comenté KAN-57 y KAN-58.
- [x] Agregué "Eliminar Red" / "Reactivar Red" (soft-delete, agrisada 1 año, confirmación roja + OTP si aplica). Creé y finalicé KAN-79.
- [x] Bug encontrado en el camino: la política RLS de `red` ocultaba del todo las eliminadas en vez de agrisarlas — corregido con una migración nueva.
- [x] Ese fix reveló ~13 Redes de prueba viejas (de otras sesiones) que quedaron invisibles por el mismo bug — las dejé intactas (tienen referencias en fusión/multiplicación/invitaciones que no tocamos hoy), solo borré las 2 filas de prueba que creé yo mismo hoy.
- [x] Auditoría de contraste de texto en Red/Departamento (pedido explícito): encontré que varias etiquetas usaban opacidad reducida encima del color ya calculado, bajando el contraste real por debajo de lo legible en colores saturados. Quité esas opacidades — verificado con cálculo WCAG real, los 36 textos visibles ahora cumplen el mínimo.
- [x] Empecé responsividad móvil (KAN-63, queda en curso): encontré botones-texto ("Eliminar Red", "Quitar cargo", "Reenviar", etc.) con área táctil de solo ~16px, muy por debajo del mínimo. Ampliado a ~44px sin cambiar nada visual. Falta todo lo grande del ticket (bottom sheet, tablet, tooltips táctiles, dispositivos reales).
- [x] Antes de cerrar, revisé si estaba lista para subir a `master`: build y lint limpios, todo comiteado. Con `supabase db lint` encontré un bug real: `fn_puede_invitar_lider` y `fn_invitar_lider` quedaron duplicadas (2 versiones cada una) por un `CREATE OR REPLACE` que agregó un parámetro nuevo — Postgres no reemplaza, crea otra función. Hoy no rompe nada porque las Edge Functions siempre pasan el parámetro nuevo, pero cualquier llamado sin él explota ("function is not unique"), y la versión vieja además difiere en comportamiento (Sublíder de CdP). No lo arreglé, solo lo dejé documentado.
- [x] Arreglé la duplicación de `fn_puede_invitar_lider`/`fn_invitar_lider`: migración nueva que borra los 2 overloads viejos. Verificado con `supabase db lint` (error desaparecido), una llamada de prueba (ya no da "function is not unique") y una invitación real de punta a punta en el lienzo (Red de prueba → asignar Líder por correo → "Designación enviada" → cancelar y agrisar la Red al terminar).
- [x] Auditoría completa del módulo cruzando `requirements.md`, código y Jira: encontré 4 tickets desactualizados en Jira (KAN-54, 76, 77 ya estaban hechos en código pero seguían en "Tareas por hacer"; KAN-75 y 55 con avance real pero no completo). Actualicé los 5 con comentario, asignado y estado real (KAN-54/76/77 → Finalizada, KAN-75/55 → En curso).
- [x] Pedido del owner: purgar por completo (hard-delete, no soft-delete) las 13 Redes de prueba de sesiones anteriores en "Centro de Vida El Eden" (sin cargo ni Casa de Paz real designados) y sus 15 Casas de Paz asociadas — quedaron invisibles primero y luego agrisadas por el bug de RLS de hoy, decisión anterior de "dejarlas" quedó revertida por pedido explícito. Verificado: solo quedan las 4 Redes reales (Central, Norte, Svalmar, Vida Nueva), triggers de protección reactivados, nada real tocado.
- [ ] Bug reportado por el owner (KAN-84, no arreglado): quitar Líder de Afirmación pidió OTP (correcto por diseño), código correcto pero dio error igual. Aparte: falló crear la cuenta de prueba test@somoscdv.com. Quitar a Gonzalo Joaquin de Afirmación queda pendiente hacerlo manual cuando lleguemos a ese punto.
- [x] KAN-55: agregué tooltips (`title` nativo) a los 7 controles de solo-ícono del toolbar (zoom, centrar, organizar automático, desktop y móvil). Verificado en vivo.
- [ ] Pendiente: terminar KAN-55 (cierre seguro del panel con datos sin guardar), KAN-63, persistir cámara local (KAN-75), revisión visual autenticada, pruebas E2E de cierre (KAN-62).
