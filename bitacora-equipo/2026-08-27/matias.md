# Matías — 2026-08-27

- [x] KAN-271: nueva feature -- editar reportes de Casa de Paz ya enviados (antes solo existía el alta). Pueden editar Líder de Red, Supervisor de Red, Líder de CdP y Sublíder de CdP, con ventana de 7 días desde la fecha de reunión
- [x] Backend: `fn_puede_editar_reporte_cdp` (rol + ventana de 7 días) reemplaza la policy de UPDATE que estaba dormida sin usarse; extendido a asistencia e ingresos (con fix de cambio de moneda al editar, para no duplicar filas)
- [x] Notificación nueva: si edita el Sublíder, avisa al Líder de esa CdP (nunca al revés), mismo patrón que ya existía para el alta
- [x] Frontend: `Reportes.tsx` ahora tiene modo edición (`/reportes/:reporteId/editar`), precarga todo lo ya guardado; botón "Editar" en Historial de Reportes (Líder/Sublíder CdP) y en la grilla de Control de Reportes (Líder/Supervisor de Red), solo dentro de la ventana de 7 días
- [x] `tsc -b`, `oxlint` y `vite build` limpios; lógica de permiso verificada contra datos reales de producción (reporte de 4 días permitido, de 13 días rechazado)
- [x] Mergeado a `master` y desplegado en producción (verificado que el bundle en vivo coincide)
- [x] Ajuste: el Sublíder de CdP no tenía acceso a "Historial de Reportes" (donde vive el botón Editar) -- se le agregó esa ruta a su navegación
- [x] Verificado que la notificación de edición Sublíder→Líder funciona (prueba controlada con roles genuinamente separados); el caso donde "no notificaba" era una cuenta que es Líder Y Sublíder a la vez de la misma CdP -- por diseño no se autonotifica (mismo criterio que KAN-240)
- [x] KAN-266: el modal "Actualiza tu membresía" (KAN-252 Parte B) le pedía teléfono/ministerios a los Super Admin -- `fn_mi_actualizacion_membresia_pendiente` no los excluía. Fix: devuelve NULL si es Super Admin (migración 20260827100000). Verificado en vivo: los 3 Super Admin dan NULL, un miembro normal sigue recibiendo el modal, y el guardado funciona bien para miembros normales (el error de guardado que veía el SA era artefacto de que el modal le aparecía indebidamente)
- [x] Auditoría multitenant de KAN-271: `fn_puede_editar_reporte_cdp` está bien acotado por iglesia/CdP/Red -- prueba cruzada en vivo confirma que un Líder de CdP no puede editar reportes de otra iglesia (puede_editar=false)
- [x] Control de Reportes (Líder/Supervisor de Red): indicador visual de que las casillas con reporte se pueden editar -- ícono de lápiz + anillo en cada casilla editable + línea de ayuda arriba de la matriz. Desplegado
- [ ] Falta: probar en vivo en el navegador con las 4 cuentas de rol (edición de reportes) y confirmar que a los Super Admin ya no les aparece el modal, antes de pasar KAN-271/KAN-266 a Finalizada
- [x] KAN-251 (compartir roles madre/satélite): investigación de código de los 4 puntos del ticket, sin tocar nada. Punto 1 (asignar cargo) parcialmente resuelto solo del lado "quién administra" en el Constructor; Líder/Sublíder de Red y CdP siguen bloqueados por diseño explícito ya documentado. Puntos 2 (trg_validar_rol), 3 (buscador de personas) y 4 (visitas/asistencia) sin resolver, sin cruce madre/satélite en ninguno
- [x] Hallazgo: el caso puntual que disparó el ticket (Supervisor de la Visión en Acción cruzado 4 Anillo/Montero) probablemente ya funciona hoy -- desde el 2026-08-09 la asignación de Pastor/Supervisor ya no filtra Persona por iglesia y usa búsqueda global, y `fn_mis_iglesias()` de Super Admin ya incluye todas las iglesias. Nada de esto lo tocó este ticket
- [x] Aclarado el origen real: el incidente de Nely (bitácora Gonzalo 2026-08-22) fue un `red_cargo` huérfano de otra iglesia, no un conflicto madre/satélite -- lo resuelve KAN-250 (panel "mudar de tenant", sin implementar todavía), no KAN-251
- [x] Jira KAN-251 movido a "En curso" con comentario detallado de la investigación
- [ ] Falta: confirmar en vivo en Centro de Vida Génesis (par madre/satélite simulado) si el caso puntual de Supervisor cruzado realmente funciona antes de decidir si hace falta tocar código en los puntos 2-4
- [x] KAN-272 (bug encontrado al probar KAN-251): asignar Supervisor de la Visión a una satélite fallaba con `relation "iglesia" does not exist`. NO era multi-tenant: el trigger `fn_iglesia_sin_ciclo` no tenía `SET search_path` propio, heredaba el `search_path=''` de `fn_estructura_asignar_supervisor` y su `FROM iglesia` sin calificar reventaba. Solo con satélites/hijas (únicas que entran al loop del trigger)
- [x] Rastreado en la BD en vivo (solo lectura sobre pg_proc/pg_trigger, `db query --linked`). Fix: `SET search_path=''` + `public.iglesia` en la función. Migración 20260827130000 + mirror harness/03_tenancy.sql
- [x] Aplicado en producción (CREATE OR REPLACE) y verificado con prueba `BEGIN; SET LOCAL search_path=''; UPDATE public.iglesia ... satélites ...; ROLLBACK;` sin error, sin persistir nada. KAN-272 en "En revisión", vincula a KAN-251
- [ ] Falta: que el owner reintente asignar Supervisor a la satélite desde la UI (end-to-end) para pasar KAN-272 a Finalizada
- [x] KAN-273 (auditoría multitenant pedida por el owner): RLS OK en el 100% de las tablas, `fn_mis_iglesias` no ampliada con satélite (aislamiento intacto). Pero encontré 7 triggers con el mismo bug de search_path que KAN-272 (fn_cdp_desactivacion_cierra_membresias, fn_validar_red_desactivacion, fn_reporte_cascada_ingresos, fn_ingreso_moneda_defecto, fn_limpiar_apellido_casada, fn_validar_estado_activo, fn_validar_tema_libro)
- [x] Confirmado ACTIVO en vivo: desactivar una Casa de Paz reventaba con `relation "casa_de_paz_membresia" does not exist` (prueba con rollback). Alcanzables también: eliminar/fusionar Red, cascada de ingresos, etc.
- [x] Fix: `ALTER FUNCTION ... SET search_path=public` en los 7 (no toca cuerpo). Migración 20260827140000 + mirror harness (04_persona, 08_estructura, 10_reporte, 11_estados, 14_finanzas). Aplicado en prod y verificado (desactivar CdP y Red ya no fallan). KAN-273 en "En revisión"
- [ ] Falta: confirmar end-to-end desde el Constructor (eliminar una CdP y una Red reales) para pasar KAN-273 a Finalizada
- [x] KAN-274: bug de vista móvil en el registro público de Afirmación -- el layout se desplazaba en horizontal con el touch y solo se bajaba con la barra lateral. Causa: `FormularioPaginado` (el wizard) tenía `overflow-y-auto` sin `overflow-x`, y CSS computa overflow-x como auto -> scroll horizontal fantasma. Fix: `overflow-x-hidden overflow-y-auto` explícito. `tsc` limpio. KAN-274 en "En revisión"
- [x] Blindados también los 2 modales con el mismo patrón (dialog.tsx base de la app + ActualizacionMembresiaModal) con `overflow-x-hidden overflow-y-auto`
- [x] `npm run build` limpio (8.46s), zip de dist/ armado y entregado al owner para subir por cPanel
- [x] Deploy real hecho por Claude vía cPanel (credenciales compartidas por Matías): el sitio real vive en `app.somoscdv.com/public`, no en `public_html` (landing vieja desactualizada). Encontrado ahí un deploy de Gonzalo a medio terminar (dist-deploy.zip subido 0:54, nunca extraído) -- no se tocó
- [x] Backup de `public` creado antes de subir nada (`public_deploy_backup_20260827_172850`, mismo patrón que ya usa el equipo). Build extraído y sobrescrito
- [x] Confirmado en producción real (no local): bundle servido coincide exacto (`index-DqAspPMJ.js`), y en iPhone emulado sobre la URL pública real ya no hay desborde horizontal. KAN-274 pasado a Finalizada
- [x] KAN-275 (autopsia de 3 fallas reportadas por el owner: evangelismo "No se pudo registrar", CI obligatorio sin explicación en reportes, reenviar invitación roto). Confirmado: NINGUNA la causaron KAN-272/273/274 -- son bugs preexistentes del 25-26 ago
- [x] Raíz #2/#3 (misma causa): `persona.membresia_completada` DEFAULT true + `MEMBRESIA_CI_OBLIGATORIO` subido a true para todas las iglesias el 25-ago + trigger `fn_validar_campos_membresia_persona` exige CI cuando membresia_completada=true. Evangelismo/reportes(agregar persona y visita)/Directorio crean personas "lead" sin setear el campo → toman default true → rechazo. Reproducido en BD con INSERT de prueba (rollback)
- [x] Fix: los 4 flujos de lead ahora insertan `membresia_completada: false` explícito (evangelismo.service.ts, reporte.service.ts x2, persona.service.ts). Confirmado en BD que pasa el trigger. Build compilado y desplegado por SSH (mucho más rápido que cPanel navegador: `ssh -i ~/.ssh/visionhub_deploy -p 2222 matiasf8@sh006.hostgator.net`, clave ya existente en la máquina) -- backup de `public`, extracción, verificado bundle nuevo (`index-GeoeLz4-.js`) y el fix presente en los chunks servidos
- [x] #1 (reenviar invitación Constructor): descartado backend faltante -- las 4 RPC `fn_estructura_datos_reenvio_cargo_*` y la Edge Function `reenviar-invitacion-cargo` ya estaban desplegadas en prod. Falta el error exacto en vivo del owner para diagnosticar
- [x] Housekeeping: reparado el historial de migraciones de prod con `supabase migration repair --status applied` (110000/130000/140000 estaban aplicadas a mano pero no registradas) -- `db push --dry-run` ahora "up to date"
- [x] Punto #1 (reenviar invitación) resuelto: owner confirmó toast "No se pudo reenviar" -> identifica el mecanismo VIEJO (invitacionId). Causa: `fn_correo_invitacion_lider_si_puedo_gestionar` nunca incluyó `fn_es_super_admin()` (así desde el diseño original del harness, no regresión de esta semana) -- a diferencia de `fn_puede_invitar_lider` (flujo nuevo) que sí lo tiene. Un Super Admin sin cargo operativo directo en esa Red/CdP/iglesia quedaba con 403 siempre
- [x] Fix aditivo (no quita permisos, solo agrega `fn_es_super_admin()` como rama más): migración 20260828000000 + mirror harness 42_invitacion_lideres.sql. Aplicado en prod, smoke test OK. Solo backend, sin deploy de frontend
- [ ] Falta: owner confirma en vivo los 3 puntos (evangelismo, agregar persona en reporte, reenviar invitación) para pasar KAN-275 a Finalizada
- [x] KAN-276: redirigida la invitación pendiente de mariajulietavm2020@gmail.com (Líder de CdP, Montero, nunca aceptada) de la CdP de Lidia Bravo (que ya tenía líder) a "Casa de Paz sin líder" en Red Daniel (genuinamente vacía). UPDATE directo en invitacion_lider, probado con rollback antes de aplicar. Finalizada
- [x] Bug reportado por el owner: en el link público de un líder de CdP, a veces el formulario de membresía se completaba a nombre del líder (o de quien lo haya probado antes) en vez de la persona real, duplicando datos. Causa: `usePersistenciaLocal` (localStorage) restauraba en silencio el borrador guardado bajo la clave del slug -- ese link lo llenan distintas personas en el mismo dispositivo
- [x] Fix: `usePersistenciaLocal.ts` ya no restaura solo -- expone `pendiente`/`confirmarPendiente`/`descartarPendiente`. `FormularioMembresiaPublico.tsx` pregunta "Encontramos un formulario sin terminar a nombre de X, ¿es tuyo?" antes de pisar los campos. `tsc` y `oxlint` limpios. Compilado y desplegado (bundle `index-bd0oJUmF.js` verificado igual local/remoto)
- [x] Bug reportado por el owner: personas con rol (líder de Red/CdP) sin datos completos aparecían como "Completa" en Afirmación, y nunca les volvía a aparecer el pedido de datos al entrar. Causa: `fn_crear_persona_si_falta` (Pastor/Supervisor asignado directo desde el Constructor) nunca seteaba `membresia_completada`, tomaba el `DEFAULT true` de la columna -- mismo patrón que KAN-275, aplicado también a personas-lead promovidas directo a líder sin pasar por el formulario
- [x] Fix backend (migración `20260828020000`): función corregida (inserta `membresia_completada=false`) + backfill acotado (sin CI, sin fecha_nacimiento, sin fila en persona_detalle, con cargo vigente) que corrigió 8 personas reales en producción -- verificado antes/después que no tocó a los 52 miembros genuinamente completos ni a los 2 casos con CI/fecha opcional por config de su iglesia. Solo backend, sin deploy de frontend necesario
- [x] Housekeeping: reparado el historial de migraciones (20260828000000/010000/020000 aplicadas a mano, no registradas) con `migration repair --status applied`; `db push --dry-run` confirma "up to date"
- [x] Feature (pedido owner): diezmos POR PERSONA en el reporte de Casa de Paz. Antes el diezmo era un único total agregado; ahora cada diezmante = nombre (buscar en la BD o escribir a mano) + monto + celular opcional, y el total es la suma. Aprovecha `finanzas_ingreso.persona_id` (ya existía, sin uso)
- [x] Backend (migración `20260828030000`): `fn_registrar_diezmos_reporte(reporte, jsonb, moneda)` -- valida `fn_puede_reportar_cdp`, da de baja los DIEZMO previos y reinserta uno por diezmante con persona_id+monto. `fn_registrar_ingresos_reporte` sin cambios (el frontend le pasa diezmos=null → limpia el agregado viejo). Probado en BD con RAISE EXCEPTION (2 diezmantes, total 155.50, revertido sin persistir)
- [x] Frontend: `reporte.types.ts` (nuevo `DiezmoLinea`, reemplaza `totalDiezmos`), `Reportes.tsx` (sección Finanzas: lista de diezmantes reutilizando `BuscadorPersonaCampo` + form de alta manual estilo visita, con monto y total calculado), `reporte.service.ts` (`construirDiezmosPayload` crea persona lead+teléfono por diezmante nuevo igual que las visitas; `obtenerReportePorId` precarga los diezmos por persona en edición). `tsc`/`oxlint`/`build` limpios
- [x] Desplegado por SSH (bundle `index-MLTPMA_I.js` verificado igual local/remoto, backup `public_deploy_backup_20260828_002355`). Migración registrada + `db push --dry-run` up to date
- [x] Auditoría multitenant Parte diezmos: `fn_registrar_diezmos_reporte` usa el mismo gate `fn_puede_reportar_cdp` que ofrendas; personas lead heredan el iglesia_id del reporte (igual que visitas). Sin fuga cruzada
- [x] Feature (pedido owner): Supervisor de la Visión en Acción cruzado madre/satélite "no se posicionaba" en la satélite. Diagnóstico en vivo: Montero (satélite de 4 Anillo) YA tiene 2 supervisores; la cruzada (`jaquelinejbcampos`) ya tenía el rol pero aparecía SIN NOMBRE. Causa: un usuario tiene UNA sola persona (`uq_persona_usuario`), la suya vive en la madre, y `fn_listar_usuarios` resolvía el nombre con join por iglesia_id → NULL
- [x] Fix backend (migración `20260828040000`, basada en la def REAL desplegada -10 cols con membresia_completada- para no cambiar el tipo de retorno): `fn_listar_usuarios` resuelve la persona con preferencia misma-iglesia + fallback a la única persona del usuario (LEFT JOIN LATERAL, nombre calculado dentro). Verificado en vivo: Montero ahora devuelve a "Jaqueline Justiniano Barbosa" con nombre (vía su ficha de 4 Anillo) y Yamil sigue con su ficha de Montero. Solo backend. Espejo harness 113 actualizado
- [x] Auditoría multitenant Parte supervisor: el fallback resuelve SOLO la propia persona del supervisor y solo en iglesias donde tiene rol legítimo; el gate de permiso de `fn_listar_usuarios` no cambió. Sin fuga
- [ ] Falta: crear/actualizar tickets de Jira (URL pública membresía, membresía completada de roles, diezmos por persona, supervisor cruzado) -- pendiente de que el owner autorice el acceso a Atlassian
- [ ] Falta: prueba en vivo en el navegador -- registrar un reporte de CdP con diezmantes reales y ver a la 2da supervisora con nombre en el Constructor de Montero
