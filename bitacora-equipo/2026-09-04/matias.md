# Matías — 2026-09-04

- [x] Auditoría multitenant pedida por el owner: RLS activo en las 71 tablas de `public`, sin políticas permisivas (`USING true`), INSERT/UPDATE/SELECT de las tablas tocadas ayer (`persona`, `telefono`, `casa_de_paz_reporte`, `evangelismo`, etc.) correctamente acotados por `fn_mis_iglesias()`. `fn_buscar_personas` valida explícito que la iglesia esté en el alcance del usuario. 2 tablas sin políticas (`anuncio_encargado`, `invitacion_lider`) verificadas como acceso solo por RPC, no un hueco real. Sin migraciones pendientes. Nada roto
- [x] Pedido del owner: agregar a alguien en Evangelismo ya no asume que asistió -- modal "¿Es un asistente nuevo?" (Sí/No). Sí = cuenta y se ve en "Asistentes nuevos" (mismo mecanismo sin duplicar persona). No = queda solo en Evangelismo. "Asistentes nuevos" sin cambios. De paso, a la persona existente marcada así se le fija `esMenor=false` directo, sin disparar el aviso de "¿es menor?" pensado para otro caso. Desplegado (bundle `index-BeFr0hZf.js`)
- [x] Owner pidió unificar el formulario de "persona nueva" entre Asistentes nuevos y Evangelismo para evitar choque de datos. Antes: Asistentes nuevos pedía Celular + checkbox "es menor"; Evangelismo pedía Domicilio + Teléfono + Fecha de nacimiento. Ahora ambos piden exactamente lo mismo -- es_menor se calcula solo de la fecha (misma edadMinima del reporte). De paso corrigió una pérdida de datos real: la fecha de nacimiento cargada en Evangelismo para una persona nueva confirmada como asistente nunca llegaba a guardarse (el INSERT de `crearReporte` no la incluía). Desplegado (bundle `index-D-zZZbpR.js`)
- [x] Owner preguntó si el reporte exige indicar mayor/menor de edad -- al verificar encontré un bug real que yo mismo introduje con la unificación de formularios: al sacar el checkbox "es menor" y dejar solo "Fecha de nacimiento" (opcional), si se deja en blanco se manda `es_menor=undefined`, y el backend (`fn_validar_asistencia`) rechaza el reporte ENTERO al enviarlo (`ASISTENCIA_EDAD_INDEFINIDA`). Fix: sin fecha, se asume `es_menor=false` por defecto en vez de indefinido. Desplegado (bundle `index-5ubqsm0a.js`)
- [ ] Falta: ticket de Jira (autenticación de Atlassian pendiente de que el owner complete el OAuth); probar en vivo en un celular real
