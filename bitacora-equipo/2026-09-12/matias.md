# Matías — 2026-09-12

- [x] Pedido del owner sobre Membresía de Afirmación (rediseñada por Gonzalo el 09-10/11): que los 15 botones/KPIs se vean como los accesos directos del Dashboard, y separar en pestañas los botones de la vista previa de personas. Antes de tocar código investigué y confirmé con el owner: los botones **ya filtraban de verdad** (server-side, vía `fn_afirmacion_buscar_membresia`) desde ayer -- el pedido era 100% visual, no funcional. Nuevo componente local `CardIndicadorPastelFiltro` (mismo lenguaje visual que `CardIndicadorPastel` de los Dashboards -- caja de ícono en degradado, número grande, blob decorativo -- con anillo de color cuando está activo, ya que el original no tiene ese estado por ser solo de navegación). Reemplaza a `KpiChipFiltro` sin tocar la lógica de ningún botón (mismas 15 llamadas, mismos `onClick`). Pantalla separada en 2 pestañas (`Tabs`, mismo patrón que `Evangelismo.tsx`): "Indicadores" (los 15 botones) y "Membresía" (la tabla). De paso encontré y le pregunté al owner si arreglaba un bug real: las tarjetas "Por URL"/"Por formulario" contaban filas crudas de `persona_llegada` sin descartar duplicados por persona ni excluir Semillas/ocultos, a diferencia de la tabla que sí lo hace -- hoy sin efecto visible en producción (no hay personas con más de 1 fila de llegada todavía) pero era una discrepancia latente. Reescrita `fn_afirmacion_estadisticas_registro` con el mismo criterio exacto de la tabla (LATERAL a la última llegada por persona + exclusión de Semillas/ocultos), y de paso sumé Pastor/Super Admin al chequeo de permiso (antes solo Líder de Afirmación/operativo, inconsistente con el resto de las funciones de Membresía -- un Pastor podía ver la tabla pero esas 2 tarjetas le fallaban). Verificado contra datos reales de producción que el nuevo conteo coincide exactamente con lo que trae la tabla (4 por URL / 2 por formulario en Centro de Vida El Edén). `tsc`/`oxlint`/`build` limpios. Migración aplicada a producción.
