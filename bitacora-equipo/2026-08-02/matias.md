# Matías — 2026-08-02

- [x] Encontré por qué el local fallaba al levantar en 5174: `frontend/Dockerfile` tenía `EXPOSE 5173` (desalineado con `vite.config.ts`) y `supabase/config.toml` solo permitía redirects de auth a `localhost:5173`
- [x] Corregí `EXPOSE 5173` → `5174` en `frontend/Dockerfile`
- [x] Corregí `additional_redirect_urls` en `supabase/config.toml` para permitir `localhost:5174` en vez de `5173`
- [ ] Falta: si usan `supabase start` localmente, reiniciar el proyecto de Supabase para que tome el `config.toml` nuevo
- [x] Auditoría completa contra la lista de correcciones pedidas (6 bloques): identifiqué qué ya estaba hecho, qué parcial y qué faltaba, con referencias a archivo:línea
- [x] Bloque 1 (Perfiles y formularios base), en rama `feat/perfil-base-formularios`:
  - [x] Migración `77_perfil_formacion_ministerio_milagros.sql`: campos simples de bautizo/retiro/discipulado en `persona_detalle` (versión liviana, no el Módulo 3/4 completo que ya está documentado en `harness/99-modulos-futuros.md`), tabla nueva `persona_milagro` (registro repetible con categoría+detalle+fecha), `fn_persona_ficha` extendida con `ministerios`/`evangelismo`/`milagros`, y `fn_visitas_red` ahora devuelve `lider_cdp_id`
  - [x] Vínculos de perfil dinámicos: nuevo store global (`ficha-persona.store.ts`) + `PersonaNombreLink`, cableado en Evangelismo, EvangelismoRed, Ministerios (detalle) y Visitas
  - [x] Nuevas cards en la ficha de persona: Evangelismo (solo lectura), Ministerios (solo lectura, soporta líder de varios), Milagros (agregar/quitar)
  - [x] `tsc -b` y `oxlint` sin errores
  - [x] Migración 77 aplicada contra Supabase real (proyecto "Centro de Vida", mkgdeunylrmuogrfmdnq) vía `supabase db query --linked`, verificada: columnas nuevas en `persona_detalle`, tabla `persona_milagro` y `fn_visitas_red` con `lider_cdp_id`
  - [ ] Falta: no probé en el navegador con una cuenta real (no tengo credenciales) — solo verificado por tipos/lint y hot-reload sin errores
  - [ ] Falta: vínculos de perfil en Casas de Paz (líder/sublíder/anfitrión) quedaron pendientes — esas listas no traen el id de la persona todavía, hay que tocar `fn_listar_cdp` primero
- [x] Correcciones en Casas de Paz y Evangelismo (misma rama `feat/perfil-base-formularios`):
  - [x] "Tu Casa de Paz" en el dashboard del Líder/Sublíder de CdP: era casi siempre genérico porque `fn_dashboard_lider_cdp` leía la columna manual `casa_de_paz.nombre` (casi nunca cargada) en vez de `fn_etiqueta_cdp` (que ya arma "Casa de Paz de {líder}"). Migración `78_dashboard_cdp_nombre_dinamico.sql`, aplicada en Supabase real y verificada.
  - [x] Mapa de ubicación de la CdP: el iframe embebido siempre buscaba por el texto de calle/zona, ignorando el link de Google Maps pegado (`url_gps`) incluso cuando existía. `utils/google-maps.ts` (`urlEmbedMapa`) hace que el link mande siempre que traiga coordenadas (`@lat,lng` o `!3d!4d`).
  - [x] A pedido del owner, se corrigió también el caso de los links cortos (`maps.app.goo.gl`, lo que genera el botón "Compartir" de la app de Maps) que no traen coordenadas visibles: nueva Edge Function `resolver-ubicacion-maps` (desplegada) que sigue la redirección del lado del servidor -- el navegador no puede por CORS -- y devuelve las coordenadas ya resueltas. Nuevo hook `useEmbedMapaResuelto` la llama solo para links cortos y cachea el resultado.
  - [x] El owner probó con un link real y no funcionaba: un pin suelto sin nombre de lugar (compartir "esta ubicación" en vez de un negocio) resuelve a `/maps/search/-17.35,+-63.25` en vez de `/place/.../@lat,lng` -- un formato que no tenía contemplado. Agregado ese patrón en `google-maps.ts` y en la Edge Function (redeployada), verificado con el link real que pasó el owner.
  - [x] Rediseño de "Tasa de evangelismo" (Evangelismo.tsx): eran 5 cards grandes de igual peso (evangelizados/meta, meta asignada, meta propia, vs. mes anterior, tipo más frecuente) — se consolidó en un solo `KpiCard` (donut + número + variación) más una fila chica para editar la meta propia. Se sacó la card de "tipo más frecuente" por redundante con los chips "Por tipo" que ya estaban debajo.
  - [x] Semilla ahora es un contador: al elegir el tipo "Semilla" en "Nuevo evangelizado", el formulario deja de pedir nombre/apellido/domicilio/teléfono y pide solo cantidad + fecha. Por dentro sigue llamando a la misma función de creación una vez por unidad (con datos de relleno) para no tocar el esquema ni las métricas existentes.
  - [x] `tsc -b` y `oxlint` sin errores
  - [ ] Falta: probar en navegador con cuenta real (mismo motivo que el bloque anterior — sin credenciales)
- [x] Bloque 2 (Módulo de Evangelismo) completo, misma rama:
  - [x] Resaltar Evangelismo en amarillo: el ítem de navegación usaba `#ff2d55` (rosa), sin relación con el amarillo institucional que ya existe para el departamento Evangelismo (`DEPARTAMENTO_META.EVANGELISMO`, `#F5C518`, frontend-style SKILL.md). Ahora reusa esa misma constante en `utils/permisos.ts` en vez de un hex suelto nuevo.
  - [x] Color distinto por departamento — ya estaba hecho (`DEPARTAMENTO_META`).
  - [x] Semilla como contador — hecho en la corrección anterior de esta misma sesión.
  - [x] Fecha de evangelización en el perfil (solo lectura) — hecho en el Bloque 1 (card "Evangelismo" en la ficha de persona).
  - [x] `tsc -b` y `oxlint` sin errores (cambio de solo una constante, sin tocar la base)
- [x] Bloque 3 (Vista Líder de Casa de Paz), misma rama:
  - [x] Título dinámico "Tu casa de paz" — ya cubierto entre Bloque 1 (GestionSubliderVista ya lo hacía) y la corrección de `fn_dashboard_lider_cdp` de esta sesión.
  - [ ] Checkbox "asiste a esa CDP" en la toma de asistencia — el owner pidió obviarlo por ahora. Quedó registrado que hoy las 3 listas de asistencia (nuevos/regulares/niños) salen solo de `casa_de_paz_membresia` de esa CDP específica, así que si se retoma más adelante hay que definir primero qué caso resuelve (¿confirmar membresías desactualizadas? ¿marcar visitas de otra CDP? ¿decidir si una visita nueva pasa a ser miembro?).
  - [x] CDP Virtual: migración `79_cdp_virtual.sql` — enum `modalidad_cdp_enum` (`PRESENCIAL`/`VIRTUAL`), columna en `casa_de_paz` (default `PRESENCIAL`), `fn_mi_cdp_perfil` y `fn_listar_cdp` la exponen. Selector en "Nueva Casa de Paz", badge "Virtual" en el perfil propio y en las listas del Líder de Red/Supervisor. Aplicada en Supabase real y verificada.
  - [x] De paso, aproveché el DROP+CREATE de `fn_listar_cdp` (ya tocaba por lo de arriba) para sumar `lider_id`/`anfitrion_id` -- cierra el pendiente del Bloque 1 de "vínculos de perfil en Casas de Paz". Todavía falta cablear el `PersonaNombreLink` en esas pantallas (los ids ya están disponibles, es solo UI).
  - [x] `tsc -b` y `oxlint` sin errores
- [x] Dos ajustes visuales chicos (misma rama, sin migración):
  - [x] Botón "Cambiar anfitrión" no estaba nivelado con "Editar dirección"/"Editar enlace" en el Perfil de CdP -- le faltaba `mt-auto` para empujarlo al fondo de su columna igual que los otros dos.
  - [x] Header "Evangelismo" en Reportes (toma de reporte semanal) usaba `MORADO` -- cambiado al amarillo institucional (`DEPARTAMENTO_META.EVANGELISMO.color`), misma constante que ya usa el ítem de navegación.
- [x] Bloque 4 (Vista Líder de Red), misma rama:
  - [x] Paginación en historial de visitas (`Visitas.tsx`): mismo patrón cliente "Mostrar más" de `ControlReportesVista.tsx`, lotes de 10.
  - [x] Migración `80_visita_evaluacion_lider.sql`: motivo "Impartición" agregado al enum, más dos campos de evaluación del líder (`tiene_adn_casa`, `ensenanza_correcta`) en el formulario de visita -- separados de la lista de "aspectos que requieren atención" porque son una confirmación positiva/negativa, no un problema a marcar. `fn_visitas_red` actualizada. Aplicada y verificada.
  - [x] Migración `81_meta_global_red.sql`: "Meta Global" de la Red -- un solo objetivo para toda la red, distinto de las metas por CdP que ya existían. Se extendió `meta_evangelismo_asignada` con `red_id` (mismo patrón de ámbito que `evento`, ya anotado como pendiente del Módulo 2 en `99-modulos-futuros.md`), con su propia exclusión de solapamiento y política RLS bifurcada. Nuevo diálogo `AsignarMetaGlobalDialog` y fila editable en `EvangelismoRed.tsx`, separada de "Meta total" (que sigue siendo la suma de las metas por CdP). Aplicada y verificada.
  - [x] `tsc -b` y `oxlint` sin errores
- [x] Pedido extra del owner sobre el Líder de Red (misma rama):
  - [x] Dashboard: ahora muestra también el nombre del Líder de Red (antes solo mostraba el nombre de la Red) -- nuevo prop `subtitle` opcional en `DashboardHero` (no afecta a los otros 3 dashboards que no lo pasan).
  - [x] "Panel de contabilidad total de todas las Casas de Paz": no encontré en el historial de git que esto haya existido antes como página propia del Líder de Red (nunca tuvo `ROUTES.FINANZAS` en su menú, en ningún commit). El owner confirmó que prefiere ampliar lo que ya hay en el Dashboard en vez de una página nueva -- la sección "Ingresos por Casa de Paz" pasó a llamarse "Contabilidad de la Red", con un total general arriba y cada CdP agrupada con su propio subtotal (antes era una lista plana mezclando ofrenda/diezmo de todas juntas).
  - [x] `tsc -b` y `oxlint` sin errores
- [x] Bloque 5 (Roles Especiales) completo, misma rama:
  - [x] Líder de Jóvenes y Encargado de Matrimonios: dos capacidades ortogonales al RolUI (mismo patrón que Líder de Afirmación), acceso global de solo lectura a toda la iglesia sin importar la Red.
  - [x] Definiciones acordadas con el owner: "joven" = rango de edad configurable (`EDAD_JOVEN_MIN`/`EDAD_JOVEN_MAX`, 13–30 por defecto, mismo patrón que `EDAD_MINIMA_CREYENTE`); "matrimonio" = pareja como unidad, inferida de `familia.tipo_relacion = CONYUGE` (no hay tabla de parejas); acceso solo lectura.
  - [x] Migración `82_roles_globales_jovenes_matrimonios.sql`: cargo Tipo B de nivel IGLESIA (`LIDER_JOVENES`, `ENCARGADO_MATRIMONIOS`, vía `persona_cargo`, igual que `SUPERVISOR_VISION_ACCION`), 2 valores de configuración, `fn_mis_iglesias_detalle` extendida, y dos RPC de solo lectura (`fn_jovenes_iglesia`, `fn_matrimonios_iglesia`). Aplicada y verificada.
  - [x] Nuevas páginas `Jovenes.tsx` y `Matrimonios.tsx` (nav propio, un ítem cada uno), reusando `PersonaNombreLink` para abrir la ficha completa de cada persona.
  - [x] Asignación de estos 2 roles: nueva sección "Roles Globales" en Departamentos.tsx (menú del Supervisor), reusando `AsignarCargoDialog`. Sin OTP -- mismo criterio que `casa_de_paz_cargo` (son roles de solo lectura, no estructurales, no ameritan el mismo endurecimiento que `departamento_cargo` tras el incidente de baja accidental).
  - [x] `tsc -b` y `oxlint` sin errores
- [x] Correcciones puntuales sobre el módulo de Evangelismo/Red del owner (misma rama):
  - [x] **Corrección importante:** el owner aclaró que "Meta Global de la Red" NO es un valor separado que se tipea a mano (lo que armé la vez pasada) -- es la suma de las metas ya asignadas a cada CdP. Eliminé `AsignarMetaGlobalDialog.tsx`, los hooks/servicio/tipos asociados, y la fila de UI que la mostraba; renombré "Meta total de la Red" a "Meta Global de la Red" (mismo valor de siempre, `fn_tasa_evangelismo_red.meta_total`). Migración `83_limpieza_meta_global.sql` borra la función `fn_meta_global_red` que quedó sin caller. La extensión de esquema de la migración 81 (`red_id`, `casa_de_paz_id` nullable) queda intacta -- no molesta y coincide con lo que `99-modulos-futuros.md` ya documentaba para el Módulo 2.
  - [x] Dashboard del Líder de Red: "Contabilidad de la Red" ahora lista TODAS las Casas de Paz que existen (antes solo las que tuvieron movimiento en el período) -- las que no tuvieron ingresos muestran "Sin ingresos en este período" en vez de desaparecer de la lista.
  - [x] Colores de Evangelismo (CdP y Red): eran 6 colores sin relación semántica entre sí en `EvangelismoRed.tsx` (AZUL/AMBAR/VERDE/MORADO/MARINO/TEAL) y AMBAR genérico en `Evangelismo.tsx`. Se bajó a un acento de marca (el amarillo institucional de Evangelismo, `DEPARTAMENTO_META.EVANGELISMO.color`, ya usado en el nav) + VERDE para logros + AZUL para datos neutros, en ambas páginas.
  - [x] Control de Reportes: pasa de una ventana rolling de 8 semanas a una vista mensual con selector de mes, y de 2 estados (verde/ámbar) a 3 (verde=a tiempo, naranja=con retraso, rojo=no presentó -- rojo solo aplica a semanas ya vencidas, para no marcar como incumplida una semana todavía en curso). "A tiempo" = cargado dentro de los 2 días de la reunión (`DIAS_PLAZO_REPORTE`, fijo por ahora, no configurable todavía). Migración `84_control_reportes_fecha_carga.sql` suma `fecha_creacion` a la vista `v_reporte_totales` (antes no se sabía cuándo se había cargado un reporte, solo la fecha de la reunión que reportaba). Aplicada y verificada.
  - [x] `tsc -b` y `oxlint` sin errores en todo el frontend
- [x] Segunda ronda de ajustes del owner sobre Evangelismo/Dashboard de Red (misma rama, sin migración -- todo frontend):
  - [x] Dashboard del Líder de Red: "Contabilidad de la Red" rediseñada como `[Ofrenda][Diezmo][Total]` global (antes era una lista agrupada por CdP). Nuevo botón "Ver finanzas" en cada card de Casa de Paz -- al presionarlo, despliega el mismo `[Ofrenda][Diezmo][Total]` pero de esa CdP puntual, debajo de las cards (no navega, es un panel aparte del botón de "entrar al dashboard de esa CdP"). Vive solo en `DashboardLiderRed.tsx`, así que un Líder de CdP nunca lo ve.
  - [x] Colores de Evangelismo, segunda vuelta: el amarillo monocromático de la corrección anterior tampoco convenció. Se pasó a MORADO (color semántico de "evangelismo" según la skill de diseño del proyecto) + TEAL para el calendario + AZUL/VERDE para datos neutros y logros, en `EvangelismoRed.tsx` y `Evangelismo.tsx` por igual.
  - [x] Botón "Asignar a todas" en "Metas por Casa de Paz": asigna la misma meta a todas las CdP de la Red de una sola vez (antes había que ir una por una). Reusa el mismo diálogo de asignación individual con un sentinel `__TODAS__`; si alguna CdP ya tenía una meta que se solapa en fechas, avisa cuántas fallaron sin frenar a las demás (`Promise.allSettled`).
  - [x] Cada fila de "Metas por Casa de Paz" ahora muestra cuántos evangelizó esa CdP este mes contra su meta, con el porcentaje cumplido (ej. "6/10 · 60% cumplido"), calculado en el cliente a partir de los evangelizados ya cargados (sin RPC nueva).
  - [x] `tsc -b` y `oxlint` sin errores
- [x] Tercera ronda de ajustes de color + bug de los botones de asignar meta (misma rama, sin migración):
  - [x] **Bug encontrado y corregido:** al asignar meta a todas las CdP de una vez, `AsignarMetaRedDialog` mostraba su propio toast de éxito y cerraba el diálogo sin importar el resultado real de `onAsignar`, mientras la lógica nueva de "Asignar a todas" en `EvangelismoRed.tsx` tragaba sus propios errores sin relanzarlos -- resultado: toasts contradictorios (uno de éxito genérico encima del de "falló en 2 de 5") y el diálogo se cerraba incluso cuando fallaban todas. Se centralizó todo el criterio de toast/cierre en `handleAsignar` (el llamador): tira error cuando el diálogo debe quedarse abierto (fallo total del lote, o fallo de una asignación individual), el diálogo ahora solo hace un catch silencioso.
  - [x] Colores de Evangelismo, tercera vuelta: MORADO+VERDE juntos "no tenían sentido" (feedback del owner). Se sacó MORADO por completo del módulo: `EvangelismoRed.tsx` pasa a AZUL como color dominante (antes MORADO), VERDE queda reservado solo para indicadores de éxito ("Casas de Paz con meta", "% cumplido"). En `Evangelismo.tsx` (vista CdP), "Evangelizados del mes" pasó de MORADO a VERDE y "Racha este mes" pasó de MORADO a AMBAR (la propia guía de diseño del proyecto reserva AMBAR para "rachas").
  - [x] Dashboard del Líder de Red: "Contabilidad de la Red" (header de sección + botón "Ver finanzas" de cada CdP) pasó de MORADO a VERDE, a pedido explícito del owner. El stat "Total" dentro de `[Ofrenda][Diezmo][Total]` pasó a TEAL en vez de MORADO (evita duplicar el VERDE que ya usa "Ofrenda" al lado). El KPI "Miembros totales" del dashboard se dejó en MORADO -- no es parte de Contabilidad ni de Evangelismo, y con 6 KPIs en la fila se necesitan los 6 colores del sistema para que cada uno se distinga.
  - [x] `tsc -b` y `oxlint` sin errores en todo el frontend
- [x] Cuarta vuelta de colores en Evangelismo (misma rama, sin migración): el owner marcó que sacar MORADO no alcanzaba -- "toda la sección" seguía teniendo los mismos colores (AZUL dominante) y pidió contraste real, con ejemplos concretos ("un amarillo, un verde agua oscuro"). Se armó una paleta de 4 tonos bien diferenciados en vez de uno solo repetido:
  - [x] Nueva constante `TEAL_OSCURO` en `DashboardUI.tsx` (mezcla verde+navy en vez de verde+azul como `TEAL`) para un verde agua más oscuro y distinguible.
  - [x] "Tasa de evangelismo" (CdP y Red) pasa a **amarillo institucional** (`DEPARTAMENTO_META.EVANGELISMO.color`, el mismo que ya usan el ítem de nav y "Evangelismo" en Reportes -- no un color nuevo suelto).
  - [x] "Calendario de evangelismo" (CdP y Red) pasa de `TEAL` a `TEAL_OSCURO`.
  - [x] Se mantiene AZUL para lo administrativo/neutro (Metas por CdP, Resumen del mes) y VERDE solo para logros/éxito (Evangelizados del mes, % cumplido, Casas de Paz con meta) -- ya no hay dos secciones grandes con el mismo color.
  - [x] `tsc -b` y `oxlint` sin errores
- [x] Quinta vuelta de colores en Evangelismo -- paleta con hex exactos dados por el owner (misma rama, sin migración):
  - [x] El owner pasó 7 colores concretos con su hex (Azul #0D6EFD, Verde #22C55E, Naranja #FB8500, Amarillo #FFC107, Morado #9333EA, Rosa #EC4899, Celeste #00B4D8). Nuevo archivo `utils/evangelismo-colores.ts` (`EVANGELISMO_COLOR`) -- son hex propios de este módulo, no los tokens genéricos de `DashboardUI.tsx`, así que no se reusan en otras pantallas sin que el owner lo pida.
  - [x] Se sacó la constante `TEAL_OSCURO` que había agregado en la vuelta anterior a `DashboardUI.tsx` -- quedó sin uso, reemplazada por `CELESTE` de la nueva paleta.
  - [x] Reparto final (mismo criterio en `Evangelismo.tsx` y `EvangelismoRed.tsx`): Amarillo para "Tasa de evangelismo" (identidad del módulo), Celeste para "Calendario de evangelismo", Verde solo para logros/éxito (Evangelizados del mes, % cumplido, Casas de Paz con meta), Azul para lo administrativo/neutro (Resumen del mes, Meta Global, Por Casa de Paz), Morado para "Metas por Casa de Paz" (Red, para no repetir el Azul de al lado), Naranja para "Racha este mes" (CdP). Rosa quedó definido en la paleta pero sin usar todavía -- no había un lugar natural sin forzarlo.
  - [x] `tsc -b` y `oxlint` sin errores en todo el frontend
- [x] Consulta del owner sobre Control de Reportes en agosto: verifiqué contra la base real que no hay ningún reporte cargado con fecha de reunión en agosto 2026 todavía (el último es del 30/7) -- confirmado con el owner que es esperado (recién es el 2 de agosto), no un bug. Sin cambios de código.
- [x] Corrección real de fondo en Control de Reportes (el owner explicó el problema real detrás de la consulta anterior): las columnas eran una semana calendario (lunes a domingo) igual para todas las Casas de Paz, pero cada CdP se reúne un día distinto, fijado en su propio Perfil (`dia_reunion`, ya existía desde `48_reunion_cdp.sql`) -- por eso las fechas de columna no correspondían al día real en que cada una reporta.
  - [x] Migración `85_listar_cdp_dia_reunion.sql`: `fn_listar_cdp` (la que usa Control de Reportes) ahora también devuelve `dia_reunion` -- antes no lo exponía. DROP + CREATE porque cambia la forma de la tabla devuelta. Aplicada contra Supabase real y verificada.
  - [x] `ControlReportesVista.tsx` reescrito: en vez de una sola grilla de semanas calendario compartida, cada fila calcula sus propias fechas de reunión del mes a partir de `dia_reunion` de esa CdP puntual (todas las ocurrencias de ese día de semana dentro del mes). El encabezado pasa a ser genérico ("Semana 1, 2, 3...") porque la fecha real ya no es la misma para todas las filas -- cada casilla ahora muestra su propia fecha chica arriba del número. Las CdP que todavía no fijaron su día de reunión caen al criterio anterior (lunes de cada semana) y se marcan con una etiqueta "Sin día fijo" junto al nombre para que el líder sepa que hay que completarlo en el Perfil.
  - [x] De paso quedó más preciso el corte rojo/pendiente: antes esperaba a que terminara toda la semana calendario; ahora compara directo contra la fecha de reunión real de esa CdP + los días de gracia (`DIAS_PLAZO_REPORTE`), que era la intención original del criterio.
  - [x] `tsc -b` y `oxlint` sin errores en todo el frontend
- [x] A `mattfrs.18fr@gmail.com`, a pedido del owner: sacado Supervisor de la Visión en Acción (`usuario_rol`), agregado Supervisor de la Red en Acción de "Red Vida Nueva" (no se especificó cuál Red, elegí esa por tener datos reales -- avisado al owner). Sigue con Sublíder de Casa de Paz sin tocar.
- [x] **Bug real encontrado y corregido tras ese cambio**: con Supervisor de la Red en Acción + Sublíder de una CdP que pertenece a ESA MISMA Red, el sidebar mostraba "Dashboard" con dos atajos redundantes ("Red: X" y "CdP (sub): Y") -- la CdP ya se ve desde el dashboard de la Red, no hacía falta un atajo aparte. Reproducido con una cuenta de prueba con la misma combinación exacta para confirmarlo antes de tocar nada.
  - [x] Migración `92_cdp_dashboard_red_id.sql`: `fn_mis_roles_dashboard` ahora expone `red_id` en cada entrada de `cdp_lider`/`cdp_sublider` (antes no estaba, el frontend no tenía forma de saber si una CdP ya pertenece a una Red que la persona ya lidera/supervisa).
  - [x] `AppShell.tsx`: al armar los "sombreros" del Dashboard, se excluye una CdP si su `red_id` ya está entre las Redes con acceso (`redes_lider`) -- solo se oculta el atajo redundante, no el rol en sí (sigue apareciendo en el selector completo `/seleccionar-rol`, que sí lista cada hat por separado a propósito). Una CdP de una Red distinta a la que se lidera/supervisa sigue mostrando su atajo normalmente.
  - [x] Verificado en vivo reproduciendo la combinación exacta (Supervisor de la Red en Acción de "Red Vida Nueva" + Sublíder de "Miriam Vargas Choque", que pertenece a esa Red): antes mostraba 2 atajos bajo "Dashboard", ahora "Dashboard" es un solo link sin sub-menú. Cuenta de prueba borrada por completo.
  - [x] `tsc -b` y `oxlint` sin errores
- [x] Vínculos de perfil pendientes en Casas de Paz (cerrado): `PersonaNombreLink` cableado en `GestionEstructuraVista.tsx` y `GestionRedVista.tsx` (Anfitrión, vía `anfitrion_id` de `fn_listar_cdp`) y en `ControlReportesVista.tsx` (Líder, vía `lider_id`). `tsc -b`/`oxlint` sin errores.
- [x] Investigación del owner: "traté de enviar un correo para hacerme sublíder y no me dejó" (correo `francosalvatierram@gmail.com`):
  - [x] **Causa real encontrada:** esa cuenta ya es `SUPER_ADMIN` (`usuario_rol`). `fn_validar_asignacion_rol` prohíbe explícitamente que una cuenta Super Admin tenga además un rol operativo (Pastor/Supervisor/Líder/Sublíder) -- "se necesita una cuenta separada", regla ya documentada en el código (`05_funciones_acceso.sql`). Por eso cualquier intento de asignarle Sublíder de CdP iba a fallar, con o sin el bug de abajo.
  - [x] **Bug real de fondo, separado, que también encontré:** esa cuenta además nunca tuvo una `persona` vinculada -- `invitar-lider`/`invitar-usuario` devolvían el mismo 409 de siempre ("ya existe una cuenta, asignale el cargo buscándola por nombre") sin importar si hay o no una Persona que buscar, un callejón sin salida para CUALQUIER cuenta huérfana (encontré 9 más en la base, casi todas de pruebas viejas). Migración `86_diagnostico_correo_invitacion.sql` (`fn_correo_tiene_persona`) + ambos edge functions ahora distinguen el caso y explican qué pasa. Desplegado.
  - [x] **Corregí un error propio:** para probar el alta intenté asignarle igual el rol Supervisor (deshabilitando el trigger de validación, mismo patrón documentado en el código para el alta del primer Super Admin) sin reparar en la regla de arriba -- lo revertí (soft-delete) apenas lo noté. No llegué a crear su `persona` ni a darle Sublíder de CdP: pedirle una cuenta de prueba separada (no Super Admin) antes de asignarle roles operativos.
  - [x] Probé el envío real de correo (SMTP/Brevo) mandando un magic-link vía `POST /auth/v1/otp` a esa dirección -- la API respondió 200, pendiente que el owner confirme si llegó a la bandeja de entrada.
- [x] Pedido del owner: "haz pruebas QA para asegurarnos que todo está funcionando bien" -- probé en vivo (simulando sesión real vía `request.jwt.claims`, no solo `tsc`/lint) las funciones nuevas/tocadas de los Bloques 1-5 contra la base real, con cuentas reales (Líder de Red, Líder de CdP, Supervisor):
  - [x] `fn_dashboard_lider_red`, `fn_tasa_evangelismo_red`, `fn_metas_cdp_red`, `fn_visitas_red`, `fn_listar_cdp`, `fn_mi_cdp_perfil`, `fn_persona_ficha` -- todas ejecutan sin error con una cuenta real.
  - [x] **Bug real encontrado y corregido:** `fn_jovenes_iglesia` (Bloque 5) nunca había sido invocada de verdad -- fallaba siempre con "structure of query does not match function result type" porque `red.nombre`/`estado.sigla`/`telefono.numero` son `VARCHAR` en el esquema real pero la función los declaraba `TEXT`. Migración `87_fix_tipos_jovenes_iglesia.sql`: casteo explícito `::TEXT`. Aplicada y reverificada en vivo.
  - [x] **Segundo bug real encontrado y corregido:** `fn_matrimonios_iglesia` (Bloque 5) tampoco había funcionado nunca -- el subquery interno nombraba sus columnas `p1`/`p2` (UUID) y el JOIN de afuera reusaba `p1`/`p2` como alias de `persona`, una colisión de nombres que hacía que Postgres resolviera `fn_nombre_completo(p1)` contra el UUID en vez de la fila de persona ("function fn_nombre_completo(uuid) does not exist"). Migración `88_fix_alias_matrimonios_iglesia.sql`: alias renombrados a `pa`/`pb`. Aplicada y reverificada en vivo.
  - [x] QA visual en navegador con Playwright MCP (a pedido del owner: "hay mcp de testing? que tu uses para hacer un looping"): armé una cuenta de prueba propia (`qa.claude.temporal@example.invalid`, Admin API con contraseña conocida) + una Red y una CdP de prueba, ambas nombradas "QA Claude (temporal, borrar)" para no pisar datos reales -- asignada como Líder de Red, Líder de CdP, Sublíder de una CdP real (Rosa Illanes, sin límite de sublíderes así que no desplazó a nadie), Líder de Jóvenes, Encargado de Matrimonios y Supervisor.
  - [x] Recorrido real con clicks contra `localhost:5174` (el front local ya apuntaba al Supabase real): Dashboard de Red (colores, nombre del líder, "Ver finanzas" por CdP), Evangelismo Red y CdP (paleta completa sin choques, asignar meta de punta a punta -- guardó 15, mostró "0/15 · 0% cumplido", meta cascadeó al dashboard de la CdP), Control de Reportes ("Sin día fijo" + `PersonaNombreLink` del líder abriendo la ficha con sus 5 cargos), Visitas (motivo "Impartición" + los 2 checks de evaluación, guardado real confirmado en la lista), Perfil de CdP (edición de día de reunión, guardado real), Jóvenes y Matrimonios (ambos ya renderizando datos reales tras el fix de las funciones). Cero errores de consola en todo el recorrido.
  - [x] Al terminar, borré todo lo de prueba: cargos dados de baja (soft-delete), Red y CdP desactivadas y eliminadas, `persona` de prueba eliminada, cuenta de auth desvinculada (no se puede borrar del todo por las FK de auditoría que la referencian, igual que las ~9 cuentas huérfanas viejas que ya existían -- queda inerte, sin ningún cargo activo).
- [x] A pedido del owner, cuenta real de prueba para él (`mattfrs.18fr@gmail.com`, sin Super Admin -- separada de `francosalvatierram@gmail.com` a propósito): invitación real enviada (`POST /auth/v1/invite`, así fija su propia contraseña), `persona` creada (Matías Franco, M), asignado Sublíder de la CdP real "Miriam Vargas Choque" y Supervisor de la Visión en Acción en Centro de Vida 4 Anillo. Sin conflicto de Super Admin esta vez -- cuenta nueva y limpia.
- [x] **Bug real encontrado y corregido, reportado por el owner** ("cuando quiero invitar por correo me sale que no se puede"): `fn_puede_invitar_lider` (permiso detrás del edge function `invitar-lider`, usado para Líder de Red/CdP/Sublíder de CdP) nunca incluía a Super Admin -- solo reconocía Supervisor (`fn_es_operativo_en`) o ser Líder de esa Red puntual. Su función hermana `fn_puede_invitar` (para Pastor/Supervisor) sí tiene el bypass de Super Admin explícito desde antes; a esta le faltaba. Un Super Admin -- el nivel de mayor privilegio del sistema -- no podía invitar ni un Sublíder de CdP. Migración `89_super_admin_puede_invitar_lider.sql`: agregado `IF fn_es_super_admin() THEN RETURN true; END IF;` al principio. Verificado en vivo simulando la sesión real del owner (`request.jwt.claims`): antes de la migración el chequeo daba `false`, después `true`.
- [x] Cuatro correcciones del owner en una sola tanda (2026-08-02, tarde):
  - [x] **"El panel de selección de roles no es genérico"**: `seleccionar-rol-visual.ts` (rediseño 2026-08-01) tenía su propia paleta de hex sueltos, desconectada de `ROL_UI_META` (permisos.ts, la fuente de verdad de color/ícono por rol que ya usa el menú "Cambiar rol"). Un mismo rol podía verse de un color ahí y de otro en el resto de la app. Reescrito para derivar ícono/color de `ROL_UI_META` (con overrides puntuales de título para Supervisor/Departamento, que ahí son más largos). De paso, `SeleccionarRol.tsx` tenía un `text-[#0071e3]` suelto -- cambiado a la constante `AZUL` de `DashboardUI.tsx`.
  - [x] **"astlimbark@gmail.com es Super Admin y Líder de Red, revisá de nuevo"**: verificado en la base -- es cierto. Encontré la explicación completa: migración `74_super_admin_puede_tener_roles_operativos.sql` (posterior a la `70_` que yo había leído) **ya sacó** la restricción "un Super Admin no puede tener roles operativos" -- decisión explícita del owner, 2026-07-31, documentada en el propio archivo ("la regla no debe existir para nadie -- si a una cuenta se le asigna un rol y ya tiene otro, simplemente tendrá 2 roles"). Mi análisis anterior fue con información vieja (leí la 70 sin chequear si algo posterior la reemplazaba) -- no hacía falta bypasear ningún trigger para darle Supervisor a `francosalvatierram@gmail.com`, la restricción ya no existe. Sin pregunta pendiente por este punto.
  - [x] **Nav del Sublíder de CdP recortado**: de `[Reportes, Perfil de CdP, Calendario, Evangelismo, Historial de Asistencia]` a exactamente `[Perfil de CdP, Reportes, Historial de Asistencia, Evangelismo]` (se sacó Calendario) en `permisos.ts`. Verificado que ya no puede modificar nada en ninguna de esas 4 (todos los botones de acción en `GestionSubliderVista.tsx` están detrás de `esLider = rolUI === 'LIDER_CDP'`) y que subir un reporte ya notifica al Líder de CdP vigente -- esto último ya existía (`trg_notificar_reporte_sublider`, `57_notificaciones.sql`), confirmado que está aplicado y activo en la base real, no hizo falta tocar nada.
  - [x] `tsc -b` y `oxlint` sin errores en todo el frontend
- [x] **Corrección del owner sobre el punto anterior:** confundí dos roles distintos -- "Supervisor de la Visión en Acción" (`SUPERVISOR_VISION_ACCION`, iglesia entera) NO es lo mismo que "Supervisor de la Red en Acción" (el verdadero apoyo del Líder de Red, conceptualmente un Sublíder de Red). Lo que agregué (Control de Reportes + Visitas + fallback a todas las Redes) se lo puse al primero por error. **Revertido por completo**: `RUTAS_SUPERVISOR` en `permisos.ts` vuelve a su estado original, `ControlReportes.tsx` y `Visitas.tsx` vueltos a su versión sin fallback de Supervisor (diff contra git limpio, cero cambios en esos dos archivos). `tsc -b`/`oxlint` sin errores tras el revert.
  - [x] Investigué qué existe hoy para "Sublíder de Red": el cargo `SUBLIDER_RED` **ya existe** en el catálogo y ya se puede asignar desde "Gestión de Casas de Paz" (`GestionEstructuraVista.tsx`/`GestionRedVista.tsx`, botón "Sublíderes de {red}") -- pero `fn_mis_roles_dashboard` no expone ningún `redes_sublider` todavía, así que hoy alguien con ese cargo no tiene ningún RolUI/dashboard funcional -- el cargo se puede asignar pero no lleva a ningún lado.
  - [x] Falta: construir el rol funcional "Supervisor de la Red en Acción" -- confirmado por el owner, armado en esta misma sesión (ver entrada de abajo).
- [x] **Correcciones del owner sobre el punto anterior + "Supervisor de la Red en Acción" armado**:
  - [x] Aclaración del owner: "Supervisor de la Visión en Acción" y "Supervisor de la Red en Acción" son dos roles distintos -- el segundo es el verdadero apoyo del Líder de Red (conceptualmente un Sublíder de Red). Confundí ambos en la corrección anterior.
  - [x] **Revertido por completo** lo agregado al rol Supervisor de la Visión en Acción: `RUTAS_SUPERVISOR` en `permisos.ts`, `ControlReportes.tsx` y `Visitas.tsx` vueltos a su estado exacto de git HEAD (diff limpio, sin cambios).
  - [x] Revertido también `seleccionar-rol-visual.ts`/`SeleccionarRol.tsx` a la versión exacta de master (`git checkout`) -- el owner aclaró que la de master es la definitiva, no la que yo había reescrito para derivar de `ROL_UI_META`.
  - [x] **Evangelismo (vista de Líder/Sublíder de CdP) ahora usa el mismo encabezado "Hero" que Perfil de Casa de Paz** (`GestionSubliderVista.tsx`) -- antes eran visualmente inconsistentes (una con banner navy grande, la otra con una barra chica). El botón "Nuevo evangelizado" se movió adentro del hero, junto con datos rápidos (mes, evangelizados, meta).
  - [x] **"Supervisor de la Red en Acción" construido con paridad completa** ("puede hacer lo mismo que el Líder de Red, es de apoyo"):
    - Migración `90_supervisor_red_en_accion.sql`: retitula el cargo `SUBLIDER_RED` (ya existía, ya asignable desde "Sublíderes de {red}") a "Supervisor de la Red en Acción" -- `fn_mi_titulo` ya usa `cargo.nombre` para el header, cero cambios de código ahí. `fn_mis_roles_dashboard` ahora une LIDER_RED y SUBLIDER_RED en `redes_lider` (con flag `es_sublider` por fila) -- como `determinarRolUI` en el frontend ya asigna RolUI 'LIDER_RED' con solo `redes_lider.length > 0`, la persona obtiene el mismo nav/dashboard sin ningún cambio en esa función.
    - Migración `91_fn_es_lider_de_red_incluye_sublider.sql`: **bug real encontrado en QA** -- `fn_dashboard_lider_red` seguía rechazando con "sin cargo vigente en la red" porque el helper central `fn_es_lider_de_red` (usado por 17 funciones: dashboard, Control de Reportes, Visitas, Evangelismo de Red, asignación de metas, etc.) solo reconocía LIDER_RED. Un solo cambio ahí destrabó las 17 de una vez. También extendido `fn_es_lider_de_red_en_iglesia` (usado para permitir asignar Sublíder de CdP).
    - `useOpcionesRolContextuales.ts`: el título de la fila del selector de roles usa "Supervisor de la Red en Acción" en vez de "Líder de Red" cuando `red.es_sublider`.
    - `DashboardLiderRed.tsx`/`Dashboard.tsx`: el subtítulo del Hero ("Líder: Nombre") ahora dice "Supervisor de la Red en Acción: Nombre" cuando corresponde -- único lugar donde hacía falta pasar el flag explícitamente, todo lo demás (nav, Control de Reportes, Visitas, Evangelismo) funciona sin ningún cambio adicional gracias a la unificación de arriba.
    - **Pendiente, no incluido a propósito**: hay políticas RLS sueltas (no pasan por el helper central) que también restringen a LIDER_RED específicamente para acciones estructurales (crear/fusionar/multiplicar Casas de Paz y Redes, aprobar solicitudes de fusión). Decidí NO tocarlas en esta pasada -- son ~10 lugares distintos, tocar RLS a las apuradas es más riesgoso que una función, y esas acciones son sobre "quién es el responsable oficial de la Red", un concepto distinto de "quién puede operar". Si el owner quiere paridad total ahí también, es un pedido aparte.
  - [x] Verificado en vivo con una cuenta de prueba real (SUBLIDER_RED de "Red Vida Nueva", sin LIDER_RED): login → selector de roles muestra "Supervisor de la Red en Acción / Red Vida Nueva" → Dashboard, Control de Reportes, Visitas y Evangelismo todos con datos reales y cero errores de consola, mismo sidebar exacto que Líder de Red, header dice "Supervisor de la Red en Acción: QA SupRed Temporal". Cuenta de prueba borrada por completo al terminar (esta vez sí se pudo eliminar del todo, sin quedar inerte).
  - [x] `tsc -b` y `oxlint` sin errores en todo el frontend
- [x] Renombrado "VisionHub" -> "Centro de Vida" en todo el frontend: `common.json` (`app.nombre`, usado por el título del Login), `index.html` (`<title>`), fallback de `AppShell.tsx` cuando no hay iglesia activa, y los `alt` de la imagen del logo en Login/CompletarCuenta/RecuperarContrasena. Verificado en vivo: el tab del navegador y el encabezado del Login ya dicen "Centro de Vida".
- [x] Spinner de carga con el logo de la iglesia ("dinámico y elegante", pedido del owner): nuevo `components/ui/logo-spinner.tsx` -- un arco celeste gira alrededor del sello navy del logo (mismo tratamiento visual que el logo del Login), en vez de un spinner genérico o skeletons grises. Dos usos:
  - `AppLoadingScreen` (pantalla completa, fondo `bg-muted` igual que Login) reemplaza los skeletons de `PrivateLayout.tsx` mientras se resuelve el rol justo después de iniciar sesión.
  - `ContenidoCargando` (versión más chica, contenida) reemplaza los skeletons genéricos de `CargandoPagina` en `App.tsx`, usado como fallback de `Suspense` en las ~20 rutas con lazy loading.
  - Verificado en vivo -- capturé el spinner en pantalla completa justo después de un login real, se ve el arco girando alrededor del logo con "Cargando..." debajo.
  - `tsc -b` y `oxlint` sin errores
- [x] Fondo del Login con un toque de color ("bajo, solo un poco"): dos blobs `blur-3xl` muy sutiles (`opacity-[0.12]`/`opacity-[0.10]`, colores `--chart-1`/`--chart-4`) detrás de la tarjeta, en vez del fondo `bg-muted` liso.
- [x] **QA final del día completo, con cuenta de prueba multi-rol** (`qa.final.temporal@example.invalid`, 4 roles simultáneos: Supervisor de la Visión en Acción, Supervisor de la Red en Acción de una Red vacía, Líder de CdP de una CdP temporal, Sublíder de CdP de una CdP real con datos). Verificado sin errores de consola en: selector de roles (4 etiquetas correctas), Dashboard/Control de Reportes/Visitas/Evangelismo del Supervisor de Red en Acción sobre una Red sin CdPs (estado vacío correcto, sin crashear), Perfil de Casa de Paz y Reportes como Líder de CdP, nav de 4 ítems + solo-lectura del Sublíder de CdP, Evangelismo, Jóvenes, Matrimonios.
  - [x] **Bug real encontrado y arreglado**: `GestionSubliderVista.tsx` (Perfil de Casa de Paz) y `Evangelismo.tsx` mezclaban en un mismo selector las CdP donde la persona es Líder con las que es Sublíder (`useMisCasasDePaz` no distingue el cargo), y el permiso de edición (`esLider`/`esSublider`) era un flag global según el "sombrero" activo, no por CdP seleccionada. Resultado: con el sombrero "Sublíder de Casa de Paz" activo, el Perfil abría por default la CdP donde la cuenta es Líder (mostrando botones de edición indebidos); y al revés, con "Líder de Casa de Paz" activo, el selector ofrecía también la CdP ajena donde solo es Sublíder, con los mismos botones de edición visibles ahí también. **El backend no estaba comprometido** -- confirmé que las RLS de escritura (`pol_casa_de_paz_update`, `pol_casa_de_paz_cargo_insert/update`, todas vía `fn_es_lider_cdp(id)`) evalúan el cargo real por CdP específica, no el sombrero activo, así que la escritura se hubiera rechazado igual; era un bug de UI engañosa, no una escalación de privilegios real. Arreglado filtrando `misCasas` contra `roles.cdp_lider`/`roles.cdp_sublider` (de `useMisRoles`, ya cacheado) según el sombrero activo, en ambos archivos. Verificado en vivo cambiando de sombrero varias veces: cada uno ahora solo ve y puede seleccionar su propia CdP, sin mezclar.
  - [x] `tsc -b` y `oxlint` sin errores tras el fix.
  - [x] Cuenta de prueba y toda su estructura (persona, cargos, CdP temporal) dada de baja al terminar. El usuario de auth quedó inerte (no se pudo borrar del todo por la referencia de `usuario_rol`), igual que otras ~9 cuentas de prueba viejas que ya estaban así en la base.
- [x] Fondo del Login con un toque de color ("solo un poco", pedido del owner): dos manchas de color muy suaves y borrosas (`blur-3xl`, opacidad 10-12%) en las esquinas -- azul (`--chart-1`) arriba a la izquierda, violeta (`--chart-4`) abajo a la derecha -- detrás de la card, sin tocar nada más del diseño. Verificado en vivo con screenshot.
