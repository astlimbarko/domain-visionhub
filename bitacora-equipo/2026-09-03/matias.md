# Matías — 2026-09-03

- [x] Investigado reporte del owner: el modal que pide datos faltantes de membresía fallaba al guardar mostrando "revisá tu conexión a internet" a algunos usuarios
- [x] Causa raíz encontrada: `fn_completar_membresia_general` había perdido el `ON CONFLICT (persona_id) DO UPDATE` sobre `persona_detalle` -- lo agregó el fix de KAN-213 (20260821040000) pero se perdió cuando `20260825160000_kan252_telefono_membresia.sql` recreó la función entera desde una copia previa a ese fix. Con doble clic en "Finalizar" (dos requests casi simultáneos), el segundo choca contra `uq_persona_detalle` -- error real de BD que el frontend muestra genérico como problema de conexión
- [x] Fix aplicado: migración `20260903000000_fix_completar_membresia_general_upsert_persona_detalle.sql`, restaura el `ON CONFLICT` sin tocar el resto. Aplicada y verificada en producción (función viva confirmada con el `ON CONFLICT` presente)
- [x] El owner mandó el error real de consola (400 en `fn_guardar_actualizacion_telefono`) -- confirmó que el bug de verdad era otro: `fn_validar_campos_membresia_persona` es un trigger `BEFORE INSERT OR UPDATE` sin filtro de columnas, revalida "CI obligatorio" en CUALQUIER update a `persona` mientras `membresia_completada=true`, no solo al completarla. Cualquier UPDATE inocuo (ej. marcar `telefono_declarado=true`) revienta si la persona quedó completa ANTES de que su iglesia activara `MEMBRESIA_CI_OBLIGATORIO` (2026-08-25) y no tiene CI cargado
- [x] Reproducido en vivo impersonando un usuario real (persona `e9f078b4...`) -- confirmado el error exacto y luego confirmado el fix con el mismo método (rollback, no se tocó el dato real)
- [x] Fix aplicado: migración `20260903010000_fix_trigger_membresia_no_revalidar_en_cada_update.sql` -- el chequeo de CI/fecha de nacimiento obligatorios ahora solo corre en la transición a `membresia_completada=true`, no en cada update posterior. Aplicada y verificada en producción
- [x] Impacto: 110 personas en esta condición (completas antes del CI obligatorio, sin CI) que hasta ahora no podían recibir NINGÚN update a su ficha, no solo por este modal
- [x] Pedido del owner: reportes de CdP se enviaban con 0 personas marcadas, molestaba verlos así en el historial. Fix en `Reportes.tsx`: `onSubmit` bloquea con toast si `totalAsistentesActual === 0` (cubre alta y edición, mismo formulario) y el botón "Enviar reporte"/"Guardar cambios" queda deshabilitado en ese caso. `tsc`/`oxlint` limpios
- [x] Owner pidió verificar la sección "Evangelismo" del reporte de CdP en vivo -- confirmado con el código, la config (7/7 iglesias en `true`) y el bundle de producción (`Reportes-C5zDjReL.js`) que el texto "Salieron a evangelizar" SÍ estaba desplegado; el owner reportó que nunca la vio en el sitio real -- causa puntual sin resolver todavía en ese momento
- [x] **Autopsia real (owner pidió investigar por qué "nunca apareció")**: `fn_config_formulario('...', 'FORMULARIO_REPORTE')` -- lo que `Reportes.tsx` usa para armar `campos` -- revienta SIEMPRE, para CUALQUIER iglesia, con `invalid input syntax for type boolean: "2"`. Causa: `DIAS_PLAZO_REPORTE` (el plazo en días de Control de Reportes, un número) quedó mal categorizado bajo `FORMULARIO_REPORTE` -- la misma categoría que `fn_config_formulario` agrega con `fn_config_bool()` fila por fila; al toparse con el "2" numérico, `fn_config_bool` intenta castearlo a boolean y la excepción tira abajo la función entera. Resultado: `campos` nunca cargó, para nadie, en ninguna iglesia -- ni la sección Evangelismo ni ninguno de los "obligatorio" (tema/disertador/testimonios/comentarios) se activaron NUNCA en el formulario de reporte, desde que existe. No tenía nada que ver con el deploy de hoy
- [x] Fix aplicado y verificado en producción: migración `20260903020000_fix_dias_plazo_reporte_categoria.sql` (recategoriza `DIAS_PLAZO_REPORTE` a `CONTROL_REPORTES`, mismo patrón que `EDAD_MINIMA_CREYENTE` en `SSVA`). Confirmado con `fn_config_formulario` en 2 iglesias: ya devuelve el JSON completo sin error. `fn_criterio('DIAS_PLAZO_REPORTE')` sigue devolviendo "2" igual que antes (no depende de la categoría). Ajustado `PanelSupervisor.tsx` para que ese valor no desaparezca del panel de administración (queda en la pestaña "Formularios" igual que antes)
- [x] Empezado (sin terminar de conectar): `BuscadorPersonaMultiple` gana capacidad opcional de agregar persona nueva cuando la búsqueda no encuentra a nadie (separa nombre en Nombre/Apellido paterno/Apellido materno), pedido para "Asistentes nuevos" del reporte -- reemplazaría al botón "+Persona que no está en el sistema". `NuevaVisita` y el insert de `reporte.service.ts` ya soportan `segundo_apellido`. Falta: conectar el prop en `Reportes.tsx`, sacar el botón viejo, probar en vivo
- [x] Owner pidió mergear y desplegar a producción. Commiteado (2 commits: fix trigger membresía + fix mínimo 1 persona/groundwork asistentes nuevos), merge `new_frontend` → `master` (`--no-ff`), push, build, deploy por SSH (`matiasf8@162.241.61.103:2222`, docroot `~/app.somoscdv.com/public`, backup `public_deploy_backup_20260903_145213`). Verificado en vivo: bundle `Reportes-6ufDfEZC.js` con el fix
- [x] Owner confirmó: la sección Evangelismo ya se ve en el reporte real
- [x] Pedido del owner: "Asistentes nuevos" y "Evangelismo" no deben pedir cargar a la misma persona dos veces. Tras aclarar el sentido correcto (unidireccional: persona nueva → suma en Evangelismo, NO al revés) se terminó de conectar el flujo iniciado antes: agregar una persona nueva desde "Asistentes nuevos" la suma automáticamente como asistente Y como evangelizado pendiente (tilda "Salieron a evangelizar" solo si hacía falta), linkeados por una `clave` compartida para que al enviar se cree UNA sola persona (fn_registrar_evangelizado con persona_id, no crea una segunda). El líder elige/cambia el tipo de evangelismo por persona en la lista de Evangelismo (nuevo `onCambiarTipo` en `EvangelismoPendientePanel`, con `soloListado` para que en edición se vea la lista+selector sin reabrir el alta manual). Agregar desde Evangelismo (persona existente encontrada por búsqueda) NO repercute en Asistencia -- eso se dejó fuera a propósito. `tsc`/`oxlint`/build limpios
- [x] Owner confirmó y pidió desplegar -- desplegado y verificado (bundle `index-BMgSIqcC.js`)
- [x] Pedido del owner: "componentes sobrepuestos" en vista móvil. Causa: las cards de Reportes.tsx usan `overflow-hidden` (para recortar el fondo de color de TarjetaHeader a las esquinas redondeadas), pero adentro tienen buscadores con desplegable `position:absolute` (BuscadorPersonaMultiple x3, BuscadorPersonaCampo x2, EvangelismoPendientePanel) -- el desplegable quedaba recortado por el borde de la card, tapando opciones en pantallas chicas. Fix: esas 4 secciones sin `overflow-hidden` en la card, con el recorte movido a un wrapper propio alrededor de cada TarjetaHeader. Desplegado y verificado (bundle `index-BAR1S-iu.js`)
- [x] Owner precisó el bug puntual: en la lista de Evangelismo, el nombre de la persona quedaba tapado por el selector de tipo en móvil. Causa: el span del nombre tenía `truncate` pero le faltaba `min-w-0` (sin eso un flex item no se achica de verdad) -- el texto se salía por encima del selector. Fix: `min-w-0` en el nombre, selector más angosto en móvil, etiquetas "(persona nueva)"/"(asistente nuevo)" ocultas en móvil. Desplegado y verificado (bundle `index-Bs7vGWJy.js`)
- [x] Owner notó: los modales de "persona nueva" del reporte no contemplaban segundo nombre (ni apellido materno en el de Evangelismo). Se agregaron ambos campos a los 2 formularios (Asistentes nuevos y Evangelismo), heurística de autocompletado ahora reconoce 4+ palabras (nombre + segundo nombre + apellido paterno + apellido materno). `fn_registrar_evangelizado` tampoco los insertaba -- migración `20260903030000` aplicada en prod. Desplegado (bundle `index-CeTj4bba.js`)
- [ ] Falta: ticket de Jira (autenticación de Atlassian pendiente de que el owner complete el OAuth); probar en vivo en un celular real
