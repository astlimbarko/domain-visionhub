# Matías — 2026-09-05

- [x] Owner reportó lag general en celulares de gama baja (carga inicial + trabas al interactuar). Estudio encontró 2 causas reales: (1) `Dialog`/`Select`/`Popover`/`DropdownMenu` (componentes base usados en toda la app) animaban `backdrop-blur-xl` junto con el zoom/fade de apertura -- costoso de compositar en GPUs viejas, y casi invisible igual porque el fondo ya es `bg-popover/95` (95% opaco); (2) `zod`+`react-hook-form`+`@hookform` y los ~120 íconos de `lucide-react` caían en el chunk `vendor` catch-all (566 KB) que se descarga en cualquier ruta. Fix: se sacó el `backdrop-blur-xl` de los 4 componentes de UI base, y se separaron esas libs en `vendor-forms`/`vendor-icons` propios en `vite.config.ts` (mismo patrón que ya existía para pdf/motion/flow). Sin cambios de comportamiento, solo de estilo/empaquetado. Desplegado por SSH (bundle `index-BmgkPYMN.js`)
- [ ] Falta: confirmar en vivo la mejora percibida en un dispositivo de gama baja real (no se pudo verificar visualmente en esta sesión -- sin extensión de Chrome conectada); ticket de Jira (OAuth de Atlassian sigue pendiente)
- [x] Owner planteó cómo una persona nueva sin membresía (formulario de membresía solo lo llenan bautizados) podía pasar a "Asistencia regular" tras 2+ asistencias. Análisis: `casa_de_paz_membresia` (pertenece a la CdP) y `persona.membresia_completada` (formulario largo, gateado por bautismo) son conceptos separados en la BD -- confirmado que el trigger de CI (`fn_validar_campos_membresia_persona`) corre solo sobre `persona`, no sobre `casa_de_paz_membresia`, y que el registro público por URL ya crea membresía sin pedir bautismo. El hueco real: "Asistentes nuevos" en `Reportes.tsx` no buscaba a nadie, así que una visita recurrente se duplicaba cada semana. Fix (punto 1 de 2, con el visto bueno del owner): `BuscadorPersonaMultiple` ahora acepta una búsqueda global (`useBuscarPersonas`, mismo mecanismo que ya usan Disertador/Diezmante) antes de ofrecer crear una persona nueva; si la encuentra, se reutiliza su `persona_id` en vez de duplicarla. Sin fecha de nacimiento conocida por esa vía, se le pregunta "¿es menor?" igual que a un miembro sin fecha registrada. Falta el punto 2 (botón manual para promover una visita frecuente a miembro de la CdP) -- no se tocó, es un cambio de producto aparte a acordar. Desplegado por SSH (bundle `index-B_72Nbzq.js`), sin verificación en vivo en navegador (sin extensión de Chrome conectada)
- [ ] Falta: confirmar en vivo el buscador de "Asistentes nuevos"
- [x] Punto 2 confirmado por el owner: umbral fijo en 2 asistencias, aviso + botón manual (no automático), aparece en el reporte junto a la visita. Agregado `SugerenciaMiembroRegular.tsx`: junto a cada visita encontrada por búsqueda, si ya asistió más de 2 veces muestra "¿La agregás como miembro regular?" con botón. Backend: `agregarMiembroRegularCdp` inserta directo en `casa_de_paz_membresia` (la política RLS ya lo permitía al Líder de CdP, `fn_es_lider_cdp` -- no hizo falta RPC nueva), sin tocar `membresia_completada`. Sublíder u otros roles sin permiso (Líder/Supervisor de Red editando, KAN-271) reciben un toast claro en vez de un error genérico. Desplegado por SSH (bundle `index-DbSXLdmr.js`), sin verificación en vivo (sin extensión de Chrome conectada)
- [x] Owner probó en vivo: la sugerencia recién aparecía a la 3ra asistencia ("más de 2"); el criterio real es "2 o más". Ajustado `SugerenciaMiembroRegular.tsx` (`asistencias <= 2` → `asistencias < 2`). Desplegado por SSH (bundle `index-BCM2xcNh.js`)
- [ ] Falta: confirmar en vivo todo el flujo (buscar visita → ver sugerencia a partir de 2 asistencias → confirmar → aparece en Asistencia regular); ticket de Jira
- [x] Owner reportó en mobile: envió el reporte de CdP, quiso volver a tocar "Enviar reporte" y el botón no respondía. Causa encontrada en el código (sin necesitar reproducirlo): al enviar con éxito, el formulario vacía la lista de asistentes (`totalAsistentesActual` vuelve a 0), lo que deshabilita el botón (`disabled:pointer-events-none` en `button.tsx` -- ni siquiera recibe el toque); la única explicación era un `title` (tooltip por hover), invisible en celular. Fix: texto siempre visible debajo del botón cuando está deshabilitado por esto ("Marcá al menos una persona en Asistencia antes de enviar"). De paso, se agregó `blur()` del foco activo al enviar, para que un toast de error (ej. "ya existe un reporte para esa fecha") no quede tapado por el teclado abierto en mobile. Desplegado por SSH (bundle `index-CJb2XlNL.js`), sin verificación en vivo
- [x] Gonzalo pidió traer a `master` y deployar su trabajo (rama `feature/contrasena-temporal` + `fix/kan279-ban-invitacion-cancelada` + `fix/kan280-usuario-rol-al-asignar-cargo`, ya mergeados a `origin/master`): KAN-278 (contraseña temporal sin enlace), KAN-279 (fix ban al cancelar invitación de cuenta confirmada), KAN-280 (asegurar `usuario_rol` al asignar cargo). Pull fast-forward sin conflictos con el WIP local. De paso, todos los cambios sin commitear de hoy (arriba) se commitearon en `fix/lag-duplicacion-boton-mobile-2026-09-05` y se mergearon a `master`
- [x] Bug real encontrado al deployar lo de Gonzalo: las Edge Functions nuevas (`establecer-contrasena-temporal`, `confirmar-cambio-contrasena`) no tenían `deno.json` (import map), así que el deploy fallaba al bundlear `nodemailer` ("not prefixed with / or ./ or ../"). Se agregó el mismo import map + `.npmrc` que usa el resto de las funciones (`crear-iglesia`, etc). Commiteado en `fix/kan278-edge-functions-import-map`, mergeado a `master`
- [x] Deploy completo a producción: migraciones `kan279`/`kan280` aplicadas (`supabase db push --linked`, dry-run confirmó que eran las únicas 2 pendientes), Edge Functions `establecer-contrasena-temporal` y `confirmar-cambio-contrasena` deployadas, frontend buildeado y subido por SSH (bundle `index-BZmHHvIk.js`, con backup previo `public_deploy_backup_20260905_221727`). Sitio responde 200 sirviendo el bundle nuevo
- [ ] Falta: ticket de Jira para todo lo de hoy (OAuth de Atlassian sigue pendiente); confirmar en vivo con el owner los flujos de contraseña temporal, KAN-279 y KAN-280
