# Matías — 2026-08-17

- [x] Instalé `react-zoom-pan-pinch` en el contenedor Docker (estaba en `package.json` pero no en el volumen anónimo de `node_modules`, causaba error de Vite al cargar `ImagenAnuncioZoom.tsx`)
- [x] KAN-212 (En curso → En revisión → Finalizada): Constructor mostraba Casas de Paz/Redes/Supervisor/Pastor como "huérfanos" -- causa real: `useInvalidarEstructura()` en `useCasasDePaz.ts` invalidaba la query key `['estructura']` pero no `['estructura-organizacional', iglesiaId]`, que es la que lee el lienzo. La relación sí se guardaba en la base (verificado en vivo por curl contra los RPC de producción). Confirmado en vivo con `test@somoscdv.com` (rol Líder de Red): guardé el domicilio de una Casa de Paz desde el Constructor y el lienzo se actualizó al instante, sin recargar
- [x] KAN-211 (En curso → En revisión): app cargaba lento -- el chunk `vendor` de Vite mezclaba paquetes livianos (login) con pesados de páginas específicas (@xyflow/react, jspdf, html-to-image, framer-motion, react-zoom-pan-pinch). Separados en chunks propios: vendor bajó de 1.26MB a 565KB. Además comprimí `public/logo.png` de 1024x1024/1.5MB a 256x256/28KB (se mostraba como máximo a 36px en toda la app). Build de producción y prueba visual en Docker local OK, falta confirmar en vivo tras el próximo deploy
- [ ] KAN-208 (bug real encontrado ayer, sin assignee): sigue sin investigar -- confirmé de nuevo por curl que `fn_listar_redes` da 404 en producción (función no existe, probable migración nunca aplicada); se reprodujo también en vivo desde Gestión de Casas de Paz ("Tu Red" mostraba "—" en vez de "KARMA")
- [x] Pregunta de Matías sobre "Red:"/"Supervisión:" duplicados en el sidebar de Líder/Supervisor de Red: confirmado que es el comportamiento intencional de KAN-186 (Gonzalo, 2026-08-13) -- la cuenta de prueba tiene ambos cargos (LIDER_RED y SUBLIDER_RED) vigentes en la misma Red "KARMA". No se tocó código, queda documentado por si alguna vez se decide que ese combo en la misma Red no debería permitirse
- [x] QA en vivo del flujo de Casas de Paz (Docker local → producción, cuenta `test@somoscdv.com`, rol Líder de Red): iglesia de prueba "Centro de Vida Genesis" resultó tener una sola Persona registrada (la propia cuenta, ficha vacía) -- no hay candidatos para probar "buscar y asignar persona existente" ni la creación desde Gestión de Casas de Paz (exige Líder). Sí se probó y confirmó de punta a punta: invitar Líder de CdP por correo (crea la invitación real, verificado y limpiado por RPC) y guardar domicilio del anfitrión (ver KAN-212). La creación de CdP desde el Constructor pide OTP por correo -- no se pudo completar sin acceso a esa bandeja
- [x] PR #32 (`feature/optimizacion-carga-y-fix-constructor` → `master`) creado y mergeado, con KAN-211 y KAN-212
- [x] Deployeé `master` (post-merge PR #32) en producción (`app.somoscdv.com`) vía SSH, siguiendo `harness/DEPLOY.md`. Sin `sshpass`/`plink`/Python disponibles en la máquina, usé un script Node.js con el paquete `ssh2` para conectar por SSH/SFTP sin terminal interactiva
- [x] Backup del `public/` anterior en `~/app.somoscdv.com/public_deploy_backup_20260817_222502` antes de subir el build nuevo (73 archivos)
- [x] Verificado en vivo: `logo.png` sirve la versión de 28KB, `index.html` referencia el bundle nuevo, raíz y `/afirmacion` responden 200, sin Node.js App activa. KAN-211 y KAN-212 pasados a Finalizada en Jira
