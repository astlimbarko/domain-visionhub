# Gonzalo — 2026-08-01

- [x] Diagnostiqué por qué no se veía el panel de Afirmación de `envioskian@gmail.com`: no era problema de rama/merge/deploy (el código está bien mergeado en `master` y desplegado en `app.somoscdv.com`) — su cargo de Líder de Afirmación tenía `fecha_fin = 2026-07-31` (dado de baja el día anterior por una cuenta de prueba, sin querer). Reactivado directo en la base
- [x] Docker local arreglado: el contenedor del `docker-compose.yml` estaba caído; el que se veía "Up" era uno huérfano, sin puerto publicado ni código en vivo (imagen vieja del 2026-07-31). Levantado el correcto, `localhost:5174` sirve el código real de nuevo
- [x] Encontré la causa raíz real del incidente: el botón "quitar" (X) del diálogo compartido `AsignarCargoDialog` nunca pedía OTP, ni en Departamentos ni en Gestión de Redes — un solo click, sin confirmación de ningún tipo, así se dio de baja el cargo de envioskian
- [x] Arreglado en mis áreas (Departamentos, Gestión de Redes), migración 75: `fn_asignar_cargo_departamento`/`fn_quitar_cargo_departamento` nuevas (exigen OTP), RLS de `departamento_cargo` cerrada del todo a escritura directa; `fn_quitar_lider_red_supervisor` nueva y dedicada a Gestión de Redes (sin tocar `red_cargo`/`quitarCargoRed`, que es compartido con las pantallas de autogestión de Matías)
- [x] `AsignarCargoDialog.tsx`: nuevo paso de confirmación con OTP para "quitar", activo solo cuando el padre pasa `onPinChange` — no afecta a Casas de Paz ni a las pantallas de Matías, que no lo pasan
- [x] `invitar-lider` Edge Function: invitar Líder de Departamento por correo ahora también exige OTP (ya lo exigía para Líder de Red)
- [x] `solicitar-otp`: ya no descarta el error en silencio si Brevo rechaza el envío — ahora queda logueado (antes no había forma de diagnosticar una falla de entrega)
- [x] Edge Functions `invitar-lider` y `solicitar-otp` desplegados a Supabase. `tsc -b --noEmit` limpio. Comiteado y mergeado a `master`
- [x] Aclaración de jerarquía de roles (reunión de equipo de hoy, owner): "Supervisor de Red" (nombre nuevo, sin el "de la Visión en Acción") tiene los MISMOS privilegios que Líder de Red — no es un nivel intermedio como se pensó el 2026-07-31. Documentado en memoria para cuando se construya
- [x] Aclaración nueva de tipos de iglesia (owner, hoy): el Supervisor de la Visión en Acción de una iglesia madre es automáticamente también el de sus iglesias satélite (no de las hijas) — ej. Santa Cruz 4to Anillo y Montero comparten el mismo Supervisor. Una satélite depende más de la madre que una hija (que tiene más autonomía). Documentado, no implementado todavía — es contexto para diseño futuro
- [ ] **Falta desplegar el build de producción** (`app.somoscdv.com`) con estos cambios de frontend — local ya los tiene (Docker), producción sigue con el build viejo hasta el próximo despliegue manual (ver `harness/DEPLOY.md`)
- [ ] **Falta pasarle a Matías el mismo hallazgo**: sus pantallas de autogestión (`GestionEstructuraVista.tsx`, `GestionRedVista.tsx`, `GestionSubliderVista.tsx`) tienen el mismo problema — asignar y quitar Líder/Sublíder de CdP y Líder de Red (autogestión) no piden OTP, y la RLS de `red_cargo`/`casa_de_paz_cargo` sigue permitiendo escritura directa. Mensaje preparado para pasarle (ver memoria de la sesión)
- [ ] **Prueba en vivo de envío real de OTP pendiente**: el owner va a disparar un código desde la app (a `astlimbark@gmail.com`) para confirmar si el correo llega — pendiente de su resultado
- [ ] Revisar si conviene bajar el cooldown de reenvío de OTP de 120s a 60s: mi recomendación es dejarlo en 120s (el propio historial de `67_otp_cooldown_120s.sql` documenta que a 60s el owner recibió el correo justo al filo del vencimiento la vez que lo probó) — no resuelve el problema de fondo (demora de entrega de Brevo por reputación de dominio nuevo), que sigue sin causa raíz confirmada
